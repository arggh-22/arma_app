---
phase: 03-dashboard-telegram-cta-announcements
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/features/api/presentation/providers/auth_bootstrap_provider.dart
  - lib/app.dart
  - test/features/api/presentation/providers/auth_bootstrap_provider_test.dart
autonomous: true
gap_closure: true
requirements:
  - TGANN-01
  - TGANN-02
requirements_addressed:
  - TGANN-01
  - TGANN-02
must_haves:
  truths:
    - "On every app open, `/auth/device/` is called so dashboard announcement content is fresh."
    - "Startup bootstrap still prewarms default server keys after fresh device-auth completes."
  artifacts:
    - path: "lib/features/api/presentation/providers/auth_bootstrap_provider.dart"
      provides: "Bootstrap provider forces fresh `/auth/device/` via authStatusRefreshProvider before prewarm."
    - path: "lib/app.dart"
      provides: "App-open bootstrap path always executes the refreshed auth bootstrap flow."
    - path: "test/features/api/presentation/providers/auth_bootstrap_provider_test.dart"
      provides: "Provider-level proof that bootstrap triggers fresh device-auth call and preserves prewarm behavior."
  key_links:
    - from: "auth_bootstrap_provider.dart"
      to: "auth_provider.dart authStatusRefreshProvider"
      via: "explicit refresh callback invocation"
      pattern: "ref\\.read\\(authStatusRefreshProvider\\)\\(\\)"
    - from: "lib/app.dart"
      to: "authBootstrapProvider"
      via: "startup post-frame bootstrap trigger"
      pattern: "ref\\.(invalidate|read)\\(authBootstrapProvider"
---

<objective>
Close the Phase 03 UAT freshness gap by forcing a real `/auth/device/` refresh on every app-open bootstrap.

Purpose: keep announcement data fresh for TGANN-01/TGANN-02 behavior instead of relying on cached token-only bootstrap.
Output: updated bootstrap wiring + provider tests proving device-auth is re-called and default-server prewarm still runs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-dashboard-telegram-cta-announcements/03-CONTEXT.md
@.planning/phases/03-dashboard-telegram-cta-announcements/03-UAT.md
@.planning/phases/03-dashboard-telegram-cta-announcements/03-01-SUMMARY.md
@lib/features/api/presentation/providers/auth_bootstrap_provider.dart
@lib/features/api/presentation/providers/auth_provider.dart
@lib/app.dart
@test/features/api/presentation/providers/auth_bootstrap_provider_test.dart

<interfaces>
From `lib/features/api/presentation/providers/auth_provider.dart`:
```dart
final authStatusRefreshProvider = Provider<AuthStatusRefresh>((ref) {
  return () async {
    final repository = ref.read(authRepositoryProvider);
    final refreshed = await repository.authenticateDevice();
    ref.read(authStateProvider.notifier).setRefreshedState(refreshed);
    return refreshed;
  };
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Force fresh device-auth during app-open bootstrap (UAT gap truth)</name>
  <files>
    - lib/features/api/presentation/providers/auth_bootstrap_provider.dart
    - lib/app.dart
  </files>
  <behavior>
    - Bootstrap must call `/auth/device/` each app open via `authStatusRefreshProvider`, not `authTokenProvider` cache path.
    - Default-server prewarm must still run after fresh auth refresh completes.
    - Startup trigger remains non-blocking and single-fire in `ArmaApp` init flow.
  </behavior>
  <action>
    Update `AuthBootstrap._runBootstrap()` to invoke `ref.read(authStatusRefreshProvider)()` and await it before prewarming `defaultServerKeysProvider` (per UAT gap truth). In `ArmaApp` startup callback, force a fresh bootstrap execution path by invalidating then reading `authBootstrapProvider` before existing one-time trigger guard continues.
  </action>
  <verify>
    <automated>flutter analyze lib/features/api/presentation/providers/auth_bootstrap_provider.dart lib/app.dart</automated>
  </verify>
  <done>`/auth/device/` is guaranteed in app-open bootstrap path and prewarm flow remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add provider tests proving fresh device-auth call is triggered by bootstrap</name>
  <files>
    - test/features/api/presentation/providers/auth_bootstrap_provider_test.dart
    - lib/features/api/presentation/providers/auth_bootstrap_provider.dart
  </files>
  <behavior>
    - Test must fail if bootstrap only reads cached token path.
    - Test asserts bootstrap invokes `authStatusRefreshProvider` callback and prewarms default keys.
    - Existing “manual rerun after refresh” behavior remains covered.
  </behavior>
  <action>
    Refactor bootstrap provider tests to override `authStatusRefreshProvider` with a counter-based fake refresh callback returning `AuthState`. Keep and update once-per-container and provider-refresh rerun assertions so both fresh auth refresh and default key prewarm call counts are proven.
  </action>
  <verify>
    <automated>flutter test test/features/api/presentation/providers/auth_bootstrap_provider_test.dart</automated>
  </verify>
  <done>Provider test suite proves every bootstrap execution performs fresh device-auth + key prewarm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App startup bootstrap -> `/auth/device/` auth refresh | Network response controls announcement freshness shown in dashboard UI |
| Bootstrap provider -> default server key prewarm | Startup flow chains auth refresh to additional data prewarm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T (Tampering) | Startup auth freshness path | mitigate | Replace cached-token bootstrap read with explicit `authStatusRefreshProvider` invocation that calls `authenticateDevice()`. |
| T-03-08 | D (Denial of Service) | Startup bootstrap sequencing | mitigate | Keep bootstrap non-blocking and single-trigger in `ArmaApp`; preserve async prewarm after auth refresh. |
| T-03-09 | R (Repudiation) | Gap-regression detection | mitigate | Lock behavior with provider-level tests asserting refresh callback call counts per bootstrap execution. |
</threat_model>

<verification>
- `flutter analyze lib/features/api/presentation/providers/auth_bootstrap_provider.dart lib/app.dart`
- `flutter test test/features/api/presentation/providers/auth_bootstrap_provider_test.dart`
</verification>

<success_criteria>
- UAT gap truth satisfied: on every app open bootstrap, `/auth/device/` runs to refresh announcement fields.
- TGANN-01 remains reliable because announcement visibility uses freshly fetched auth payload.
- TGANN-02 remains reliable because read-more content comes from freshly fetched announcement text.
- Provider-level automated tests lock this behavior against regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-telegram-cta-announcements/03-02-SUMMARY.md`
</output>
