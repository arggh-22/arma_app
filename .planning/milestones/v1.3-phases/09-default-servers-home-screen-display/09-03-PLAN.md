---
phase: 09-default-servers-home-screen-display
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - lib/features/dashboard/presentation/widgets/default_servers_section.dart
  - lib/features/dashboard/presentation/widgets/default_servers_sheet.dart
  - lib/features/dashboard/presentation/screens/dashboard_screen.dart
  - lib/core/l10n/app_en.arb
  - lib/core/l10n/app_fa.arb
  - lib/core/l10n/app_hy.arb
  - lib/core/l10n/app_ru.arb
  - lib/core/l10n/app_zh.arb
  - test/features/dashboard/presentation/widgets/default_servers_section_test.dart
autonomous: true
requirements:
  - UI-01
  - UI-02
  - API-03
  - REL-01
must_haves:
  truths:
    - "Dashboard shows default servers inline below traffic stats with top-3 preview and Show all sheet."
    - "Refresh action shows icon spinner without removing current list."
    - "Users can only tap active servers; disconnected tap selects and connected tap switches by reconnecting."
    - "Offline/cache and failure states are clearly visible with specific snackbar messages."
  artifacts:
    - path: "lib/features/dashboard/presentation/widgets/default_servers_section.dart"
      provides: "Dashboard section with top-3 list, header refresh, and empty/offline states"
    - path: "lib/features/dashboard/presentation/widgets/default_servers_sheet.dart"
      provides: "Show all modal/bottom sheet list"
    - path: "test/features/dashboard/presentation/widgets/default_servers_section_test.dart"
      provides: "Widget verification for refresh, tapability, and error rendering"
  key_links:
    - from: "dashboard_screen.dart"
      to: "default_servers_section.dart"
      via: "inline composition below traffic stats"
      pattern: "DefaultServersSection"
    - from: "default_servers_section.dart"
      to: "active_server_provider.dart"
      via: "selection path"
      pattern: "activeServerProvider"
    - from: "default_servers_section.dart"
      to: "connection_provider.dart"
      via: "disconnect/reconnect flow"
      pattern: "connectionProvider"
---

<objective>
Deliver the full dashboard default-server experience: inline section, show-all sheet, refresh UX, tap-to-connect behavior, and localized failure feedback.

Purpose: complete UI-01/UI-02 and finalize API-03/REL-01 user-facing behavior.
Output: production UI widgets wired to existing connection and active-server providers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
@.planning/phases/09-default-servers-home-screen-display/09-RESEARCH.md
@.planning/phases/09-default-servers-home-screen-display/09-UI-SPEC.md
@lib/features/dashboard/presentation/providers/default_servers_provider.dart
@lib/features/connection/presentation/providers/connection_provider.dart
@lib/features/server/presentation/providers/active_server_provider.dart
@lib/features/server/presentation/widgets/server_card.dart
@lib/features/dashboard/presentation/screens/dashboard_screen.dart

<interfaces>
From `lib/features/connection/domain/entities/connection_status.dart`:
```dart
class Disconnected extends ConnectionStatus {}
class Connected extends ConnectionStatus {}
```

From `lib/features/server/presentation/providers/active_server_provider.dart`:
```dart
Future<void> selectServer(ServerConfig? server);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build default server section and sheet widgets (D-01..D-12, D-16, D-17, D-18)</name>
  <read_first>
    - .planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
    - .planning/phases/09-default-servers-home-screen-display/09-UI-SPEC.md
    - lib/features/server/presentation/widgets/server_card.dart
  </read_first>
  <files>
    - lib/features/dashboard/presentation/widgets/default_servers_section.dart
    - lib/features/dashboard/presentation/widgets/default_servers_sheet.dart
    - lib/features/dashboard/presentation/screens/dashboard_screen.dart
    - lib/core/l10n/app_en.arb
    - lib/core/l10n/app_fa.arb
    - lib/core/l10n/app_hy.arb
    - lib/core/l10n/app_ru.arb
    - lib/core/l10n/app_zh.arb
  </files>
  <behavior>
    - Section is inline under traffic stats with top-3 preview and Show all action opening modal/bottom sheet.
    - Header refresh is icon-only with spinner while retaining current list content.
    - Cards show name/status/traffic progress; non-active rows are disabled and non-tappable.
  </behavior>
  <action>Implement dashboard UI per locked layout/interaction decisions: inline placement below traffic stats (D-01), top-3 preview + Show all sheet (D-02/D-03), and ServerCard-style visual language (D-04). Render name/status/traffic progress (D-05/D-08), fixed status colors (D-06), disabled non-active rows (D-07), and header refresh icon spinner without replacing content (D-09/D-10). Keep refresh success silent and show snackbar only on failures with specific localized copy by failure type (D-11/D-18); retry remains the same header action (D-12). Show offline badge for cached data (D-16) and empty-state guidance when cache is absent (D-17).</action>
  <acceptance_criteria>
    - Dashboard includes `DefaultServersSection` below traffic card region.
    - Top list is capped at three items with Show all sheet displaying full list.
    - All new strings are localized in existing ARB locales and include refresh semantic label.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart -r compact</automated>
  </verify>
  <done>Dashboard renders the full default-server UI contract with localized state/error messaging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire tap-to-connect behavior through existing providers (D-13, D-14, D-15)</name>
  <read_first>
    - lib/features/connection/presentation/providers/connection_provider.dart
    - lib/features/server/presentation/providers/active_server_provider.dart
    - .planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
  </read_first>
  <files>
    - lib/features/dashboard/presentation/widgets/default_servers_section.dart
    - test/features/dashboard/presentation/widgets/default_servers_section_test.dart
  </files>
  <behavior>
    - Disconnected + active default server tap selects server via `activeServerProvider` only.
    - Connected + tap on different active default server performs disconnect then connect to new server.
    - Non-active/default parse-failed items remain non-interactive.
  </behavior>
  <action>Implement tap handlers that always route through `activeServerProvider` (D-15): when disconnected, set selected server only (D-13); when connected to a different server, call existing connection notifier disconnect then connect sequence for immediate switch (D-14). Do not create any parallel “default active” state. Keep guard clauses so non-active statuses and parse-invalid items cannot trigger selection or connection (D-07).</action>
  <acceptance_criteria>
    - Connection behavior differs correctly between disconnected and connected states.
    - Active-server selection is performed before any connect/reconnect attempt.
    - Widget tests cover disabled taps and reconnect branch.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart -r compact</automated>
  </verify>
  <done>Default server taps are fully integrated with existing selection/connection providers and respect status constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider state -> UI actions | Untrusted failure/status states drive user-facing decisions and button availability |
| UI tap -> VPN connection layer | User input can trigger disconnect/reconnect operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | E | tap-to-connect handler | mitigate | Gate taps to active/parse-valid items only; route all connects through existing provider guards |
| T-09-08 | I | snackbar/error copy | mitigate | Use typed failure mapping from provider to avoid leaking raw exceptions or ambiguous errors |
| T-09-09 | D | reconnect branch | mitigate | Reuse existing `ConnectionNotifier` connect/disconnect sequence with state checks to avoid duplicate connects |
</threat_model>

<verification>
- `flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart -r compact`
- `flutter gen-l10n`
- `flutter analyze lib/features/dashboard lib/core/l10n`
</verification>

<success_criteria>
- Home screen shows default servers in the locked placement and visual format.
- Refresh UX and offline/error states match decision contract.
- Tap behavior uses existing active-server and connection providers exactly as specified.
</success_criteria>

<output>
After completion, create `.planning/phases/09-default-servers-home-screen-display/09-default-servers-home-screen-display-03-SUMMARY.md`
</output>
