---
phase: 09-default-servers-home-screen-display
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - lib/core/storage/app_hive_bootstrap.dart
  - lib/main.dart
  - test/core/storage/app_hive_bootstrap_test.dart
  - test/features/dashboard/presentation/providers/default_servers_startup_regression_test.dart
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - API-03
  - REL-01
must_haves:
  truths:
    - "Dashboard default-servers provider no longer throws ProviderException/HiveError on cold start."
    - "default_server_cache Hive box is opened during app bootstrap before provider reads."
    - "Startup regression test proves failure path degrades to state output, not crash."
  artifacts:
    - path: "lib/core/storage/app_hive_bootstrap.dart"
      provides: "Single bootstrap entry point that opens required Hive boxes including default_server_cache."
    - path: "lib/main.dart"
      provides: "App startup wired to await Hive bootstrap before runApp."
    - path: "test/features/dashboard/presentation/providers/default_servers_startup_regression_test.dart"
      provides: "Regression coverage for provider cold-start cache-read path."
  key_links:
    - from: "main.dart"
      to: "app_hive_bootstrap.dart"
      via: "await bootstrap before ProviderScope/runApp"
      pattern: "await bootstrapAppHiveStorage"
    - from: "default_servers_provider.dart"
      to: "default_server_cache_provider.dart"
      via: "catch-path cache read"
      pattern: "defaultServerCacheDatasourceProvider"
---

<objective>
Close the UAT blocker where dashboard default-servers flow crashes with ProviderException/HiveError because `default_server_cache` is read before its Hive box is opened.
</objective>

<context>
@.planning/phases/09-default-servers-home-screen-display/09-HUMAN-UAT.md
@lib/main.dart
@lib/features/api/presentation/providers/default_server_cache_provider.dart
@lib/features/dashboard/presentation/providers/default_servers_provider.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit Hive bootstrap for default-server cache before app providers initialize</name>
  <read_first>
    - lib/main.dart
    - lib/features/api/presentation/providers/default_server_cache_provider.dart
    - .planning/phases/09-default-servers-home-screen-display/09-HUMAN-UAT.md
  </read_first>
  <files>
    - lib/core/storage/app_hive_bootstrap.dart
    - lib/main.dart
    - test/core/storage/app_hive_bootstrap_test.dart
  </files>
  <action>Extract startup Hive open sequence into `bootstrapAppHiveStorage(...)` and explicitly open `default_server_cache` before `runApp`. Keep existing encrypted auth box behavior intact and wire `main()` to call this bootstrap entrypoint prior to ProviderScope initialization.</action>
  <acceptance_criteria>
    - `main()` awaits bootstrap before `runApp`.
    - `default_server_cache` box open call is present in bootstrap path.
    - Existing auth/storage startup behavior remains intact.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/core/storage/app_hive_bootstrap_test.dart -r compact</automated>
  </verify>
  <done>Cold-start storage bootstrap always opens `default_server_cache` before provider reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add startup regression test for defaultServersProvider offline/error path without ProviderException</name>
  <read_first>
    - lib/features/dashboard/presentation/providers/default_servers_provider.dart
    - lib/features/api/presentation/providers/default_server_cache_provider.dart
    - .planning/phases/09-default-servers-home-screen-display/09-HUMAN-UAT.md
  </read_first>
  <files>
    - test/features/dashboard/presentation/providers/default_servers_startup_regression_test.dart
  </files>
  <action>Add regression test that runs bootstrap first, drives `defaultServersProvider` through failing API path, and asserts provider returns state/failure output without ProviderException/HiveError.</action>
  <acceptance_criteria>
    - Test reproduces previous failure pre-fix and passes after fix.
    - No `HiveError: Box not found` thrown on provider read path.
    - Failure maps to typed provider state instead of unhandled exception.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/providers/default_servers_startup_regression_test.dart -r compact</automated>
  </verify>
  <done>Automated regression test proves the startup crash path is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| app startup -> local storage | Provider graph reads persistent cache from Hive at boot |
| network failure path -> cache fallback | Runtime failure enters provider catch-path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-GAP-01 | D | startup storage init | mitigate | Open required Hive boxes before runApp to prevent provider crash loop |
| T-09-GAP-02 | R | regression surface | mitigate | Add deterministic test reproducing cold-start failure path |
| T-09-GAP-03 | I | provider error propagation | mitigate | Ensure failures map to typed state, not crash |
</threat_model>

<output>
After completion, create `.planning/phases/09-default-servers-home-screen-display/09-04-SUMMARY.md`
</output>
