---
phase: 09-default-servers-home-screen-display
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - lib/features/dashboard/presentation/providers/default_servers_provider.dart
  - lib/features/dashboard/presentation/providers/default_servers_provider.g.dart
  - test/features/dashboard/presentation/providers/default_servers_provider_test.dart
autonomous: true
requirements:
  - API-02
  - API-03
  - REL-01
must_haves:
  truths:
    - "Dashboard provider can load default servers from API and keep cached fallback when API fails."
    - "Manual refresh preserves visible data while exposing refresh-in-progress state."
    - "Offline refresh failures are queued and retried with bounded exponential backoff."
  artifacts:
    - path: "lib/features/dashboard/presentation/providers/default_servers_provider.dart"
      provides: "Unified load/refresh/offline-fallback state machine"
    - path: "test/features/dashboard/presentation/providers/default_servers_provider_test.dart"
      provides: "Automated reliability/error-state coverage"
  key_links:
    - from: "default_servers_provider.dart"
      to: "default_server_keys_provider.dart"
      via: "primary API fetch path"
      pattern: "ref\\.read\\(defaultServerKeysProvider\\.future\\)"
    - from: "default_servers_provider.dart"
      to: "default_server_cache_provider.dart"
      via: "offline fallback + cache persistence"
      pattern: "defaultServerCache"
---

<objective>
Implement the dashboard default-server provider state machine with API fetch, cache fallback, full API-02 field preservation, typed failure mapping, and queued refresh retries.

Purpose: satisfy API-03 and REL-01 behavior before UI widgets are added.
Output: tested provider powering loading/refresh/offline/error decisions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
@.planning/phases/09-default-servers-home-screen-display/09-RESEARCH.md
@lib/features/api/data/datasources/api_client.dart
@lib/features/api/presentation/providers/default_server_keys_provider.dart
@lib/features/api/presentation/providers/auth_provider.dart
@lib/features/api/presentation/providers/default_server_cache_provider.dart
@lib/features/dashboard/data/mappers/default_server_item_mapper.dart
@lib/features/api/domain/entities/default_server_key.dart

<interfaces>
From `lib/features/api/data/datasources/api_client.dart`:
```dart
enum ApiClientErrorType { timeout, network, unauthorized, client, server, malformedResponse, unknown }
class ApiClientException implements Exception { final ApiClientErrorType type; }
```

From `lib/features/api/presentation/providers/default_server_keys_provider.dart`:
```dart
@riverpod
Future<List<DefaultServerKey>> defaultServerKeys(Ref ref) async;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build default servers provider state machine (D-10, D-16, D-17, D-19)</name>
  <read_first>
    - .planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
    - lib/features/api/presentation/providers/default_server_keys_provider.dart
    - lib/features/api/presentation/providers/default_server_cache_provider.dart
  </read_first>
  <files>
    - lib/features/dashboard/presentation/providers/default_servers_provider.dart
    - lib/features/dashboard/presentation/providers/default_servers_provider.g.dart
    - test/features/dashboard/presentation/providers/default_servers_provider_test.dart
  </files>
  <behavior>
    - Initial load prefers live API; on failure uses cache if available and marks source as offline.
    - Provider-exposed items keep API-02 field coverage end-to-end, including `subscriptionUrl` and `expireDate`.
    - Refresh keeps current list visible while `isRefreshing=true`.
    - Unauthorized path relies on existing one-shot silent re-auth; if still unauthorized exposes auth failure state.
  </behavior>
  <action>Create a keep-alive Riverpod notifier that exposes dashboard-ready state (`items`, `isRefreshing`, `isOfflineData`, `lastFailureType`) and drives initial load + manual refresh. Use live `defaultServerKeysProvider` fetch first, persist successful snapshots, and on fetch failure show cached data with explicit offline marker (D-16) or empty/error state when no cache exists (D-17). Keep API-02 payload fidelity through provider outputs (no dropping `subscriptionUrl` or `expireDate` from mapped items). Keep list visible during refresh spinner state (D-10). Preserve Phase 08 silent re-auth behavior and only surface auth error if retry still fails (D-19).</action>
  <acceptance_criteria>
    - Provider state can represent live data, offline cached data, and empty error.
    - Provider state items retain `subscriptionUrl` and `expireDate` for both live and cached paths.
    - Refresh does not clear currently displayed items.
    - Unauthorized-after-retry is surfaced distinctly from timeout/offline/server errors.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/providers/default_servers_provider_test.dart -r compact</automated>
  </verify>
  <done>Dashboard provider fully controls load/refresh/offline/error logic with deterministic states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add queued refresh retry with exponential backoff (REL-01, D-12, D-18)</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/09-default-servers-home-screen-display/09-RESEARCH.md
    - lib/features/dashboard/presentation/providers/default_servers_provider.dart
  </read_first>
  <files>
    - lib/features/dashboard/presentation/providers/default_servers_provider.dart
    - test/features/dashboard/presentation/providers/default_servers_provider_test.dart
  </files>
  <behavior>
    - Manual refresh while offline enqueues one pending retry job.
    - Retry attempts use bounded exponential delays (1s, 2s, 4s) and stop after max attempts.
    - Failure type mapping supports timeout/offline/unauthorized/server snackbar copy selection.
  </behavior>
  <action>Extend the provider with a foreground retry queue for failed manual refresh requests to satisfy REL-01 without introducing deferred background scheduling. When refresh fails due to network/timeout, queue retry attempts with exponential backoff and cancel queue once a fetch succeeds. Keep retry trigger tied to the same header refresh action (D-12) and emit a typed failure enum used by UI for specific snackbar messaging (D-18).</action>
  <acceptance_criteria>
    - Queue stores pending refresh intent and retry attempt count.
    - Exponential backoff is bounded and testable.
    - Failure type mapping is explicit and exhaustive for timeout/offline/unauthorized/server/client/malformed cases.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/providers/default_servers_provider_test.dart -r compact</automated>
  </verify>
  <done>Provider supports queued refresh retries and typed error outcomes for UI feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/provider boundary | Network failures and malformed payloads cross into UI state machine |
| retry queue timer -> provider state | Automated retry logic can amplify requests if not bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | D | `default_servers_provider.dart` | mitigate | Bound retry attempts + exponential backoff ceiling to prevent retry storms |
| T-09-05 | R | failure classification | mitigate | Map `ApiClientException.type` to explicit failure enum to preserve auditable UX behavior |
| T-09-06 | E | unauthorized recovery | mitigate | Keep one-shot silent re-auth path only; propagate auth failure after retry exhaustion |
</threat_model>

<verification>
- `flutter test test/features/dashboard/presentation/providers/default_servers_provider_test.dart -r compact`
- `flutter analyze lib/features/dashboard/presentation/providers/default_servers_provider.dart`
</verification>

<success_criteria>
- Provider keeps UI data visible during refresh and distinguishes offline-cache vs no-cache failure.
- Refresh failures can queue and retry with bounded exponential backoff.
- Failure types are explicit and ready for decision-specific snackbar copy.
</success_criteria>

<output>
After completion, create `.planning/phases/09-default-servers-home-screen-display/09-default-servers-home-screen-display-02-SUMMARY.md`
</output>
