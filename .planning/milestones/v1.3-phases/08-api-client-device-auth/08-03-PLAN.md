---
phase: 08-api-client-device-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - lib/features/api/domain/repositories/auth_repository.dart
  - lib/features/api/data/datasources/api_client.dart
  - lib/features/api/data/repositories/auth_repository_impl.dart
  - lib/features/api/presentation/providers/auth_provider.dart
  - lib/features/api/presentation/providers/default_server_keys_provider.dart
  - test/features/api/data/datasources/api_client_test.dart
  - test/features/api/data/repositories/auth_repository_impl_test.dart
  - test/features/api/presentation/providers/auth_provider_test.dart
autonomous: true
requirements:
  - API-01
  - SEC-01
user_setup:
  - service: vpn_api
    why: "Real backend values are needed to replace placeholder base URL and X-API-Key."
    env_vars:
      - name: VPN_API_BASE_URL
        source: "Backend deployment endpoint"
      - name: VPN_API_KEY
        source: "Backend-issued app key for X-API-Key header"
must_haves:
  truths:
    - "App can perform device authentication with HWID + device metadata."
    - "Expired/invalid token path re-authenticates and retries once."
    - "Transient network failures auto-retry once, then surface manual retry through AsyncValue error."
  artifacts:
    - path: "lib/features/api/data/datasources/api_client.dart"
      provides: "HTTP client with endpoint methods and retry policy"
    - path: "lib/features/api/data/repositories/auth_repository_impl.dart"
      provides: "Token lifecycle orchestration"
    - path: "lib/features/api/presentation/providers/auth_provider.dart"
      provides: "Riverpod auth state notifier + token provider"
    - path: "lib/features/api/presentation/providers/default_server_keys_provider.dart"
      provides: "FutureProvider for `/keys/` with ref.refresh manual retry contract"
  key_links:
    - from: "auth_provider.dart"
      to: "auth_repository_impl.dart"
      via: "provider dependency"
      pattern: "ref\\.watch\\(authRepositoryProvider\\)"
    - from: "default_server_keys_provider.dart"
      to: "api_client.dart"
      via: "tokenized keys fetch"
      pattern: "getKeys\\("
    - from: "auth_repository_impl.dart"
      to: "auth_local_datasource.dart"
      via: "persist refreshed token state"
      pattern: "writeAuthState\\("
---

<objective>
Deliver the end-to-end device auth flow: API client, token lifecycle orchestration, and Riverpod providers ready for Phase 09 UI consumption.

Purpose: complete API-01 and SEC-01(partial) behavior with resilient retry and explicit 401 re-auth.
Output: tested API/repository/provider stack with AsyncValue-based retry UX contract.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-api-client-device-auth/08-CONTEXT.md
@.planning/phases/08-api-client-device-auth/08-RESEARCH.md
@docs/api_documentation.md
@lib/features/server/presentation/providers/subscription_provider.dart
@lib/features/server/presentation/providers/server_list_provider.dart

<interfaces>
From `lib/features/server/presentation/providers/server_list_provider.dart`:
```dart
@riverpod
class ServerListNotifier extends _$ServerListNotifier {
  Future<List<ServerConfig>> build() async;
}
```

From `lib/features/server/domain/entities/server_config.dart`:
```dart
@freezed
abstract class ServerConfig with _$ServerConfig { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement API client with strict timeout and retry policy</name>
  <read_first>
    - docs/api_documentation.md
    - lib/config/app_config.dart
    - .planning/phases/08-api-client-device-auth/08-CONTEXT.md
  </read_first>
  <files>
    - lib/features/api/data/datasources/api_client.dart
    - test/features/api/data/datasources/api_client_test.dart
  </files>
  <behavior>
    - `/auth/device/` sends JSON `{device_id, os_type, app_version}` with `X-API-Key`.
    - `/keys/` sends `Authorization: Token <token>`.
    - Timeout/network/5xx path retries once with 1s backoff; 4xx auth/client errors do not auto-retry.
  </behavior>
  <action>Build `ApiClient` using `http.Client` per D-01 and D-04 with connect timeout 5s/read timeout 10s from `AppConfig`, one silent retry for transient classes, and redacted diagnostics (token/HWID masked). Do not add refresh-token endpoint assumptions; treat 401 as re-auth signal.</action>
  <acceptance_criteria>
    - Auth request body and headers exactly match API spec.
    - Retry policy executes at most 2 total attempts for transient failures.
    - Logs/messages never include full token or full device ID values.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/datasources/api_client_test.dart -r compact</automated>
  </verify>
  <done>API client behavior is deterministic and tested for success, transient retry, and 401/non-retry paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth repository token lifecycle + 401 re-auth</name>
  <read_first>
    - lib/features/api/data/datasources/auth_local_datasource.dart
    - lib/features/api/data/services/device_id_service.dart
    - .planning/phases/08-api-client-device-auth/08-RESEARCH.md
  </read_first>
  <files>
    - lib/features/api/domain/repositories/auth_repository.dart
    - lib/features/api/data/repositories/auth_repository_impl.dart
    - test/features/api/data/repositories/auth_repository_impl_test.dart
  </files>
  <behavior>
    - Near-expiry token triggers lazy re-auth before protected API call.
    - 401 from `/keys/` clears stale token, re-authenticates device, then retries original call once.
    - Re-auth failure surfaces typed failure to provider layer.
  </behavior>
  <action>Create repository abstraction and implementation that orchestrates device ID resolution (D-03), auth state persistence (D-02), lazy token validity checks, and one-time replay of failed protected requests after successful re-auth (D-04). Keep guest-mode and announcement handling deferred per context.</action>
  <acceptance_criteria>
    - Repository has explicit methods for `authenticateDevice`, `getValidToken`, and `executeWithAuthRetry`.
    - Token expiration buffer uses 5-minute pre-expiry refresh threshold.
    - 401 path performs at most one re-auth cycle and one replay.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/repositories/auth_repository_impl_test.dart -r compact</automated>
  </verify>
  <done>Token lifecycle and 401 recovery are implemented without infinite retry loops.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Riverpod providers for auth and default-server key fetch</name>
  <read_first>
    - lib/features/server/presentation/providers/subscription_provider.dart
    - lib/features/api/data/repositories/auth_repository_impl.dart
    - .planning/phases/08-api-client-device-auth/08-CONTEXT.md
  </read_first>
  <files>
    - lib/features/api/presentation/providers/auth_provider.dart
    - lib/features/api/presentation/providers/default_server_keys_provider.dart
    - test/features/api/presentation/providers/auth_provider_test.dart
  </files>
  <behavior>
    - `authStateNotifierProvider` restores persisted state on init.
    - `authTokenProvider` returns valid token or triggers re-auth flow.
    - `defaultServerKeysProvider` exposes AsyncValue for loading/error/data and supports manual retry via `ref.refresh`.
  </behavior>
  <action>Implement Riverpod provider graph per D-01 and D-02: keep-alive auth notifier synced to encrypted datasource, token FutureProvider for API callers, and keys FutureProvider for Phase 09. Ensure provider errors are retryable manually (hybrid retry UX contract) and avoid UI-screen coupling in this phase.</action>
  <acceptance_criteria>
    - Providers compile with generated Riverpod artifacts.
    - Manual retry path is documented by provider usage (`ref.refresh(defaultServerKeysProvider)`).
    - No provider logs full secrets.
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter test test/features/api/presentation/providers/auth_provider_test.dart -r compact</automated>
  </verify>
  <done>Phase 09 can consume auth token + default key providers without additional auth plumbing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> remote VPN API | Untrusted network transport and server responses |
| provider layer -> persistent auth store | Sensitive token state crosses memory/storage boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | R | API request handling | mitigate | Structured typed errors + bounded retry count for deterministic request auditability |
| T-08-06 | I | Provider/repository logging | mitigate | Redact token/HWID in diagnostics; enforce no plaintext secret logging in API and provider code |
| T-08-07 | D | Retry policy | mitigate | One silent retry max on transient failures; no retry for 4xx to prevent request storms |
</threat_model>

<verification>
- `flutter test test/features/api/data/datasources/api_client_test.dart`
- `flutter test test/features/api/data/repositories/auth_repository_impl_test.dart`
- `dart run build_runner build --delete-conflicting-outputs`
- `flutter analyze lib/features/api`
</verification>

<success_criteria>
- Device auth flow executes with required payload and headers.
- Token lifecycle (near-expiry and 401) is implemented and tested.
- Riverpod providers expose AsyncValue contracts ready for Phase 09 UI.
</success_criteria>

<output>
After completion, create `.planning/phases/08-api-client-device-auth/08-api-client-device-auth-03-SUMMARY.md`
</output>

