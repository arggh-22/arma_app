---
phase: 08-api-client-device-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - lib/main.dart
  - lib/features/api/data/datasources/auth_local_datasource.dart
  - lib/features/api/data/services/device_id_service.dart
  - test/features/api/data/datasources/auth_local_datasource_test.dart
  - test/features/api/data/services/device_id_service_test.dart
autonomous: true
requirements:
  - API-01
  - SEC-01
must_haves:
  truths:
    - "Device ID persists and is reused on next app launch."
    - "Token/HWID state is encrypted at rest and can be restored to memory."
  artifacts:
    - path: "lib/features/api/data/services/device_id_service.dart"
      provides: "Stable device_id resolution with persisted fallback"
    - path: "lib/features/api/data/datasources/auth_local_datasource.dart"
      provides: "Encrypted Hive persistence for auth state"
    - path: "lib/main.dart"
      provides: "Auth box initialization during app bootstrap"
  key_links:
    - from: "lib/main.dart"
      to: "auth_state Hive box"
      via: "open encrypted box on startup"
      pattern: "openBox<.*>\\('auth_state'"
    - from: "device_id_service.dart"
      to: "auth_local_datasource.dart"
      via: "persisted deviceId read/write"
      pattern: "saveDeviceId|getDeviceId"
---

<objective>
Implement secure local auth storage and stable device identity persistence needed before remote API auth calls.

Purpose: satisfy API-01 + SEC-01 persistence guarantees and prevent token/HWID plaintext storage.
Output: encrypted auth datasource, device ID service, app bootstrap wiring, and tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-api-client-device-auth/08-CONTEXT.md
@.planning/phases/08-api-client-device-auth/08-RESEARCH.md
@lib/main.dart
@lib/features/server/presentation/providers/server_list_provider.dart
@lib/features/settings/data/datasources/settings_local_datasource.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement encrypted auth local datasource</name>
  <read_first>
    - lib/main.dart
    - lib/features/settings/data/datasources/settings_local_datasource.dart
    - .planning/phases/08-api-client-device-auth/08-RESEARCH.md
  </read_first>
  <files>
    - lib/features/api/data/datasources/auth_local_datasource.dart
    - lib/main.dart
    - test/features/api/data/datasources/auth_local_datasource_test.dart
  </files>
  <behavior>
    - Persisted auth payload reloads as equivalent `AuthState`.
    - Datasource removes auth token cleanly on sign-out/reset.
    - Missing/corrupt payload returns unauthenticated default state instead of crash.
  </behavior>
  <action>Create `auth_state` encrypted Hive box (AES cipher key sourced from `flutter_secure_storage`) and datasource methods for load/save/clear auth state plus load/save device ID per D-02 and D-03. Wire `main.dart` startup to open the encrypted box before `runApp()`. Never log raw token/device_id values (SEC-01).</action>
  <acceptance_criteria>
    - Auth box uses encrypted cipher, not plain `Hive.openBox` defaults.
    - Datasource exposes explicit methods: `readAuthState`, `writeAuthState`, `clearAuthState`, `readDeviceId`, `writeDeviceId`.
    - `main.dart` initializes auth box alongside existing boxes without breaking startup.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/datasources/auth_local_datasource_test.dart -r compact</automated>
  </verify>
  <done>Auth/device persistence is encrypted and recoverable across app restarts with test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement device ID resolution with persisted fallback</name>
  <read_first>
    - docs/api_documentation.md
    - lib/features/api/data/datasources/auth_local_datasource.dart
    - .planning/phases/08-api-client-device-auth/08-CONTEXT.md
  </read_first>
  <files>
    - lib/features/api/data/services/device_id_service.dart
    - test/features/api/data/services/device_id_service_test.dart
  </files>
  <behavior>
    - If stored device ID exists, service returns it without platform call.
    - If no stored ID and Android ID exists, service stores/returns Android ID.
    - If platform ID unavailable, service stores/returns generated UUID fallback.
  </behavior>
  <action>Build `DeviceIdService` using `device_info_plus` + datasource persistence per D-03 and `uuid` per D-07: resolve Android identifier first, persist once, and when platform value is unavailable generate fallback via `const Uuid().v4()` and persist it for reuse. Keep factory-reset behavior as accepted limitation from research; do not introduce deferred multi-device linking flow.</action>
  <acceptance_criteria>
    - Service returns stable value across repeated calls in same test run.
    - Service writes fallback UUID exactly once when platform identifier is null/empty, generated through the `uuid` package path.
    - Tests validate no plaintext logging of full identifiers.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/services/device_id_service_test.dart -r compact</automated>
  </verify>
  <done>Device ID is deterministic, persisted, and resilient to platform-ID failure path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secure keystore -> Hive cipher key | Encryption key material crosses storage boundary |
| device platform APIs -> app auth layer | Device identifier retrieved from platform and persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | I | auth_local_datasource | mitigate | Use encrypted Hive box key from secure storage; never store token/HWID plaintext |
| T-08-04 | S | device_id_service | mitigate | Persist first resolved ID and reuse it; avoid caller-supplied HWID injection paths |
</threat_model>

<verification>
- `flutter test test/features/api/data/datasources/auth_local_datasource_test.dart`
- `flutter test test/features/api/data/services/device_id_service_test.dart`
- `flutter analyze lib/main.dart lib/features/api/data`
</verification>

<success_criteria>
- `auth_state` persistence is encrypted.
- Device ID is stable and fallback-safe.
- No token/HWID plaintext logs are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-api-client-device-auth/08-api-client-device-auth-02-SUMMARY.md`
</output>
