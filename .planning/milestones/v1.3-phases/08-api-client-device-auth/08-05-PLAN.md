---
phase: 08-api-client-device-auth
plan: 05
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - lib/features/api/data/services/device_id_service.dart
  - test/features/api/data/services/device_id_service_test.dart
  - test/features/api/data/services/device_id_reinstall_semantics_test.dart
autonomous: true
gap_closure: true
requirements:
  - API-01
  - SEC-01
must_haves:
  truths:
    - "HWID resolution uses a stable Android identifier strategy rather than Build.ID-first semantics."
    - "Reinstall simulation with unchanged platform stable ID yields the same HWID."
    - "Fallback UUID path is deterministic and persisted when stable platform ID is unavailable."
  artifacts:
    - path: "lib/features/api/data/services/device_id_service.dart"
      provides: "Stable HWID resolver with migration from legacy stored values"
    - path: "test/features/api/data/services/device_id_reinstall_semantics_test.dart"
      provides: "Automated reinstall/update semantics proof"
  key_links:
    - from: "device_id_service.dart"
      to: "auth_local_datasource.dart"
      via: "read/write persisted device id"
      pattern: "readDeviceId|writeDeviceId"
---

<objective>
Close verification gap #2 by making HWID persistence behavior explicit and test-proven for reinstall/update semantics.
</objective>

<context>
@.planning/phases/08-api-client-device-auth/08-VERIFICATION.md
@.planning/phases/08-api-client-device-auth/08-RESEARCH.md
@lib/features/api/data/services/device_id_service.dart
@test/features/api/data/services/device_id_service_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden device identifier strategy and migration</name>
  <read_first>
    - lib/features/api/data/services/device_id_service.dart
    - .planning/phases/08-api-client-device-auth/08-VERIFICATION.md
    - .planning/phases/08-api-client-device-auth/08-RESEARCH.md
  </read_first>
  <files>
    - lib/features/api/data/services/device_id_service.dart
    - test/features/api/data/services/device_id_service_test.dart
  </files>
  <action>Refactor resolver to prioritize stable Android identifier semantics (`androidInfo.androidId`) and stop using `androidInfo.id` (`Build.ID`) as primary identity source. If legacy stored value differs and stable ID is available, migrate persisted value once to stable ID. Keep `Uuid().v4()` fallback only when stable platform ID is null/empty.</action>
  <acceptance_criteria>
    - Code path no longer treats `Build.ID` as primary HWID source.
    - Migration branch updates legacy persisted values to stable Android ID when available.
    - Fallback UUID branch is only used when stable platform ID is unavailable.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/services/device_id_service_test.dart -r compact</automated>
  </verify>
  <done>HWID resolver prioritizes stable Android ID semantics, with legacy migration and UUID fallback only when needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reinstall/update semantics tests</name>
  <read_first>
    - test/features/api/data/services/device_id_service_test.dart
    - lib/features/api/data/datasources/auth_local_datasource.dart
  </read_first>
  <files>
    - test/features/api/data/services/device_id_reinstall_semantics_test.dart
    - test/features/api/data/services/device_id_service_test.dart
  </files>
  <action>Add automated tests for: (1) fresh-storage reinstall simulation returning same HWID when stable platform ID is unchanged, (2) update simulation preserving existing stored HWID, (3) platform-ID-unavailable branch generating deterministic persisted UUID fallback.</action>
  <acceptance_criteria>
    - Test file includes explicit reinstall semantics scenario with two isolated datasource instances.
    - Tests assert stable-ID path and fallback UUID path behavior independently.
    - All tests are automated and reproducible (no manual verification steps).
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/services/device_id_service_test.dart test/features/api/data/services/device_id_reinstall_semantics_test.dart -r compact</automated>
  </verify>
  <done>Automated tests prove update/reinstall identity behavior and deterministic fallback behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android platform ID API -> app auth layer | nullable/untrusted platform ID enters persistent identity flow |
| local encrypted storage -> auth repository | persisted HWID reused for server device identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-GAP-03 | S | device_id_service | mitigate | use stable Android ID semantics with migration from weak legacy IDs |
| T-08-GAP-04 | R | reinstall claims | mitigate | executable reinstall/update simulation tests as objective evidence |
</threat_model>

<output>
After completion, create `.planning/phases/08-api-client-device-auth/08-05-SUMMARY.md`
</output>
