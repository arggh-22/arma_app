---
phase: 13-telegram-link-ux-hardening-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/dashboard/presentation/screens/telegram_link_guide_screen.dart
  - test/features/dashboard/presentation/screens/telegram_link_guide_screen_test.dart
autonomous: true
requirements:
  - TGUI-03
  - TGREL-01
must_haves:
  truths:
    - "User sees a submitting/loading state while Telegram link request is in flight."
    - "User sees success feedback and returns to dashboard only on linked outcome."
    - "User sees clear error feedback and can retry with the same primary action."
  artifacts:
    - path: "lib/features/dashboard/presentation/screens/telegram_link_guide_screen.dart"
      provides: "Outcome-driven submit UX state rendering and retry-ready controls"
      contains: "_submit"
    - path: "test/features/dashboard/presentation/screens/telegram_link_guide_screen_test.dart"
      provides: "Widget coverage for loading/success/failure/retry states"
      contains: "isSubmitting"
  key_links:
    - from: "telegram_link_guide_screen.dart"
      to: "telegramLinkProvider.notifier.submit"
      via: "_submit"
      pattern: "submit\\("
    - from: "telegram_link_guide_screen_test.dart"
      to: "TelegramLinkGuideScreen"
      via: "widget tests"
      pattern: "find\\.byKey\\(const Key\\('telegram-link-submit-button'\\)\\)"
---

<objective>
Harden Telegram linking result UX so loading, success, and recoverable failure paths are explicit and retry-safe.

Purpose: Complete TGUI-03 state behavior and lock in TGREL-01 widget reliability expectations.
Output: Hardened screen behavior + expanded widget regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-telegram-link-ux-hardening-validation/13-UI-SPEC.md
@.planning/phases/12-telegram-link-ui-guided-flow/12-02-SUMMARY.md
@lib/features/dashboard/presentation/screens/telegram_link_guide_screen.dart
@test/features/dashboard/presentation/screens/telegram_link_guide_screen_test.dart

<interfaces>
From lib/features/api/domain/entities/telegram_link_outcome.dart:
```dart
enum TelegramLinkOutcomeType {
  linked, alreadyLinked, invalidId, unauthorized, network, server, unknown,
}
class TelegramLinkOutcome {
  const TelegramLinkOutcome({required this.type, this.message});
  final TelegramLinkOutcomeType type;
  final String? message;
}
```

From lib/features/api/presentation/providers/telegram_link_provider.dart:
```dart
class TelegramLinkState {
  final bool isSubmitting;
  final TelegramLinkOutcome? lastOutcome;
  final String? lastSubmittedId;
}
Future<TelegramLinkOutcome> submit(String telegramId);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce resilient result-state UX in Telegram guide submit flow</name>
  <files>lib/features/dashboard/presentation/screens/telegram_link_guide_screen.dart</files>
  <behavior>
    - Test 1: While submit is in flight, input/paste/submit are disabled and submit shows spinner.
    - Test 2: linked outcome shows success feedback then pops route.
    - Test 3: alreadyLinked/invalidId/network/server/unknown/unauthorized outcomes keep user on screen with retry-ready controls.
  </behavior>
  <action>Refine `_submit` + state rendering per TGUI-03 and UI-SPEC: preserve existing provider in-flight guard, keep retry path on the same primary button (no alternate dialog), and ensure controls always re-enable after any non-success outcome.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/screens/telegram_link_guide_screen_test.dart --plain-name "in-flight"</automated>
  </verify>
  <done>Guide screen reliably exposes loading/success/error behavior and recoverable retry path for all outcome types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand widget regression suite for result states and retry</name>
  <files>test/features/dashboard/presentation/screens/telegram_link_guide_screen_test.dart</files>
  <behavior>
    - Test 1: Submitting state disables controls and shows progress indicator.
    - Test 2: Success path pops back to previous route.
    - Test 3: Failure outcomes show mapped message, remain on guide screen, and allow a second submit.
  </behavior>
  <action>Add targeted widget tests covering full outcome matrix and explicit retry-after-failure flow; keep assertions bound to existing keys (`telegram-id-input`, `telegram-paste-button`, `telegram-link-submit-button`) to match current Flutter test pattern.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/screens/telegram_link_guide_screen_test.dart</automated>
  </verify>
  <done>Widget tests prove hardening behavior end-to-end and prevent regressions in core Telegram linking UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → provider submit | Untrusted Telegram ID crosses into link action |
| provider outcome → UI feedback | Server/network outcomes influence user decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | telegram_link_guide_screen.dart submit flow | mitigate | Keep provider validation + trim path and block invalid IDs before network call; assert via widget tests for invalid input behavior. |
| T-13-02 | D | submit button/UI controls | mitigate | Disable controls during in-flight requests and verify retry resumes only after completion to prevent request flooding from rapid taps. |
</threat_model>

<verification>
- flutter test test/features/dashboard/presentation/screens/telegram_link_guide_screen_test.dart
</verification>

<success_criteria>
- Loading, success, and failure states are explicit and deterministic in Telegram guide screen.
- Retry path is available immediately after recoverable failures without navigation resets.
- Widget test suite covers happy path plus key failures in TGUI-03/TGREL-01.
</success_criteria>

<output>
After completion, create `.planning/phases/13-telegram-link-ux-hardening-validation/13-01-SUMMARY.md`
</output>
