---
phase: 12-telegram-link-ui-guided-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/router/app_router.dart
  - lib/features/dashboard/presentation/screens/dashboard_screen.dart
  - lib/features/dashboard/presentation/screens/telegram_link_guide_screen.dart
  - test/features/dashboard/presentation/screens/dashboard_screen_telegram_link_fab_test.dart
autonomous: true
requirements: [TGUI-01]
must_haves:
  truths:
    - "Dashboard shows an explicit Link action with Telegram icon."
    - "Link action hides on downward scroll and reappears on upward scroll."
    - "Tapping Link opens the Telegram linking route."
  artifacts:
    - path: "lib/features/dashboard/presentation/screens/dashboard_screen.dart"
      provides: "Extended FAB Link entry with Telegram icon and scroll-aware visibility"
    - path: "lib/core/router/app_router.dart"
      provides: "Registered /telegram-link route from dashboard flow"
    - path: "lib/features/dashboard/presentation/screens/telegram_link_guide_screen.dart"
      provides: "Route target screen shell for Telegram linking"
  key_links:
    - from: "dashboard_screen.dart"
      to: "/telegram-link"
      via: "context.push('/telegram-link') from FAB onPressed"
      pattern: "push\\('/telegram-link'\\)"
    - from: "app_router.dart"
      to: "telegram_link_guide_screen.dart"
      via: "GoRoute builder"
      pattern: "path: '/telegram-link'"
---

<objective>
Add the home-screen Telegram Link entry and navigation wiring.

Purpose: Deliver the phase entrypoint so users can start Telegram linking from Dashboard.
Output: Dashboard FAB behavior + route registration + widget coverage for navigation behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-telegram-link-ui-guided-flow/12-CONTEXT.md
@.planning/phases/12-telegram-link-ui-guided-flow/12-RESEARCH.md
@.planning/phases/12-telegram-link-ui-guided-flow/12-UI-SPEC.md
@lib/core/router/app_router.dart
@lib/features/dashboard/presentation/screens/dashboard_screen.dart

<interfaces>
From `lib/core/router/app_router.dart`:
```dart
final goRouter = GoRouter(
  initialLocation: '/dashboard',
  routes: [/* ... */],
);
```

From `lib/features/dashboard/presentation/screens/dashboard_screen.dart`:
```dart
class DashboardScreen extends ConsumerWidget {
  const DashboardScreen({super.key});
}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Register Telegram link route and screen contract</name>
  <files>
    - lib/core/router/app_router.dart
    - lib/features/dashboard/presentation/screens/telegram_link_guide_screen.dart
    - test/features/dashboard/presentation/screens/dashboard_screen_telegram_link_fab_test.dart
  </files>
  <behavior>
    - Router exposes `/telegram-link` as a concrete GoRoute target.
    - Route builds a real `TelegramLinkGuideScreen` widget class (not inline anonymous widget).
    - Navigation test harness can pump Dashboard and resolve route push without runtime route errors.
  </behavior>
  <action>Add `TelegramLinkGuideScreen` as a dedicated screen class and register `/telegram-link` route in `app_router.dart` per D-01. Keep route registration in the same centralized router file (per existing go_router pattern), and add/extend widget tests to assert that route push from dashboard context resolves successfully.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/screens/dashboard_screen_telegram_link_fab_test.dart -r compact</automated>
  </verify>
  <done>Dashboard-triggered navigation has a valid router destination and test coverage proves route resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Dashboard Link FAB with Telegram icon and scroll-hide behavior</name>
  <files>
    - lib/features/dashboard/presentation/screens/dashboard_screen.dart
    - test/features/dashboard/presentation/screens/dashboard_screen_telegram_link_fab_test.dart
  </files>
  <behavior>
    - Dashboard shows `FloatingActionButton.extended` labeled `Link` with Telegram icon (D-01, D-02).
    - FAB hides while user scrolls down and reappears on scroll up (D-03).
    - FAB tap pushes `/telegram-link` route (TGUI-01).
  </behavior>
  <action>Refactor `DashboardScreen` to a stateful Riverpod widget variant as needed for scroll-direction tracking, then implement an extended FAB labeled `Link` with Telegram icon per D-02. Wire `onPressed` to `context.push('/telegram-link')` (D-01) and implement deterministic hide/show state updates driven by scroll notifications/controller direction (D-03). Cover FAB label/icon visibility, scroll-hide/show transitions, and tap navigation with widget tests.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/screens/dashboard_screen_telegram_link_fab_test.dart -r compact</automated>
  </verify>
  <done>Home screen exposes the Link Telegram entry with required icon/behavior and verified navigation to linking screen.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard scroll/tap input → navigation | Untrusted user interaction drives route transitions and UI visibility changes. |
| dashboard route push → router table | Navigation requests must resolve only to registered internal routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | D | dashboard_screen.dart scroll listener | mitigate | Keep FAB visibility as lightweight local UI state with bounded updates on scroll direction only (D-03). |
| T-12-02 | T | app_router.dart route mapping | mitigate | Register explicit static `/telegram-link` path and verify push resolution in widget test to prevent broken/incorrect routing. |
</threat_model>

<verification>
- flutter test test/features/dashboard/presentation/screens/dashboard_screen_telegram_link_fab_test.dart -r compact
</verification>

<success_criteria>
- Dashboard shows Link action with Telegram icon.
- Link action hides/reappears based on scroll direction.
- Link action opens Telegram linking route successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/12-telegram-link-ui-guided-flow/12-01-SUMMARY.md`
</output>
