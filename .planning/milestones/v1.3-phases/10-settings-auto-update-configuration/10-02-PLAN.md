---
phase: 10-settings-auto-update-configuration
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/features/api/data/services/default_server_refresh_service.dart
  - lib/features/dashboard/presentation/providers/default_servers_provider.dart
  - test/features/api/data/services/default_server_refresh_service_test.dart
  - test/features/dashboard/presentation/providers/default_servers_provider_test.dart
autonomous: true
requirements: [DATA-03, COMPAT-01]
must_haves:
  truths:
    - "Successful sync removes expired default servers immediately."
    - "Refresh path does not break existing connect/reconnect behavior."
  artifacts:
    - path: "lib/features/api/data/services/default_server_refresh_service.dart"
      provides: "single refresh+prune pipeline"
  key_links:
    - from: "default_server_refresh_service.dart"
      to: "default_server_cache_datasource.dart"
      via: "pruned cache write"
      pattern: "write\\("
---

<objective>
Implement shared refresh service with expiry pruning and keep connection compatibility intact.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build refresh service with expire-date pruning</name>
  <read_first>
    - lib/features/api/data/datasources/default_server_cache_datasource.dart
    - lib/features/api/presentation/providers/default_server_keys_provider.dart
    - .planning/phases/10-settings-auto-update-configuration/10-CONTEXT.md
  </read_first>
  <files>
    - lib/features/api/data/services/default_server_refresh_service.dart
    - test/features/api/data/services/default_server_refresh_service_test.dart
  </files>
  <action>Implement `refreshNow()` that fetches keys via existing auth-aware path, removes expired entries immediately after successful fetch, writes pruned cache, and records sync timestamp.</action>
  <acceptance_criteria>
    - Expired entries are removed before final cache write.
    - Unauthorized behavior follows one silent re-auth policy via existing auth repository path.
    - Active connection is not force-disconnected by cleanup.
  </acceptance_criteria>
  <verify><automated>flutter test test/features/api/data/services/default_server_refresh_service_test.dart -r compact</automated></verify>
  <done>Shared refresh service enforces cleanup and metadata updates safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route provider refresh through shared service</name>
  <read_first>
    - lib/features/dashboard/presentation/providers/default_servers_provider.dart
    - lib/features/api/data/services/default_server_refresh_service.dart
  </read_first>
  <files>
    - lib/features/dashboard/presentation/providers/default_servers_provider.dart
    - test/features/dashboard/presentation/providers/default_servers_provider_test.dart
  </files>
  <action>Refactor default server provider refresh/load paths to use shared refresh service output while preserving existing tap/connect compatibility behavior.</action>
  <acceptance_criteria>
    - Provider no longer duplicates fetch+prune logic.
    - Existing connect/reconnect behavior remains unchanged.
    - Provider tests pass with new service wiring.
  </acceptance_criteria>
  <verify><automated>flutter test test/features/dashboard/presentation/providers/default_servers_provider_test.dart -r compact</automated></verify>
  <done>Provider uses one canonical refresh path and remains COMPAT-01-safe.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-03 | T | prune logic | mitigate | strict prune-before-write flow with tests |
| T-10-04 | E | unauthorized retry | mitigate | one-shot auth retry only |
</threat_model>
