---
phase: 17-dashboard-35-65-layout-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/dashboard/presentation/screens/dashboard_screen.dart
  - lib/features/dashboard/presentation/widgets/active_server_card.dart
  - test/features/dashboard/presentation/screens/dashboard_screen_layout_grouping_test.dart
  - test/features/dashboard/presentation/screens/dashboard_screen_top_panel_test.dart
  - test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart
  - test/features/dashboard/presentation/screens/dashboard_screen_announcement_section_test.dart
autonomous: true
requirements:
  - DLAY-01
  - DLAY-02
  - DLAY-03
  - DLAY-04
must_haves:
  truths:
    - "Dashboard top visual group reads as the 35% area while keeping a single scroll composition."
    - "Top group contains connect button, status/timer, selected server card, and dashboard stats (when preference enabled)."
    - "Bottom group renders announcement block first (only when content exists), then default-server cards."
    - "Selected server card is visually parked/highlighted while preserving existing navigation/tap behavior."
  artifacts:
    - path: "lib/features/dashboard/presentation/screens/dashboard_screen.dart"
      provides: "Top/bottom grouped dashboard composition with announcement/default ordering"
    - path: "lib/features/dashboard/presentation/widgets/active_server_card.dart"
      provides: "Parked selected-server visual treatment using existing card component"
    - path: "test/features/dashboard/presentation/screens/dashboard_screen_layout_grouping_test.dart"
      provides: "Widget regression coverage for grouped 35/65 visual composition cues"
    - path: "test/features/dashboard/presentation/screens/dashboard_screen_top_panel_test.dart"
      provides: "Widget regression coverage for top-panel required widgets"
    - path: "test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart"
      provides: "Widget regression coverage for parked/highlight selected-server styling"
  key_links:
    - from: "dashboard_screen.dart"
      to: "active_server_card.dart"
      via: "top visual group child composition"
      pattern: "Top.*ActiveServerCard|ActiveServerCard.*top"
    - from: "dashboard_screen.dart"
      to: "default_servers_section.dart"
      via: "bottom visual group ordering after announcement gate"
      pattern: "hasAnnouncement.*dashboard-announcement-card.*DefaultServersSection"
    - from: "active_server_card.dart"
      to: "activeServerProvider"
      via: "selected-state styling bound to provider value"
      pattern: "server == null|server != null"
---

<objective>
Implement Phase 17 dashboard visual refresh so the screen reads as 35/65 top-bottom composition, keeps behavior parity, and clearly parks/highlights the selected server.

Purpose: Deliver DLAY-01..04 without changing provider contracts or interaction flows.
Output: Updated dashboard composition + parked-card styling + targeted widget regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-CONTEXT.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-UI-SPEC.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-RESEARCH.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-VALIDATION.md
@lib/features/dashboard/presentation/screens/dashboard_screen.dart
@lib/features/dashboard/presentation/widgets/active_server_card.dart
@lib/features/dashboard/presentation/widgets/default_servers_section.dart
@test/features/dashboard/presentation/screens/dashboard_screen_announcement_section_test.dart

<interfaces>
From `dashboard_screen.dart`:
```dart
class DashboardScreen extends ConsumerStatefulWidget
final hasAnnouncement = hasAnnouncementTitle || hasAnnouncementText;
```

From `active_server_card.dart`:
```dart
class ActiveServerCard extends ConsumerWidget
final server = ref.watch(activeServerProvider);
```

From `default_servers_section.dart`:
```dart
class DefaultServersSection extends ConsumerStatefulWidget
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave-0 dashboard layout/parked-style regression tests before implementation</name>
  <files>test/features/dashboard/presentation/screens/dashboard_screen_layout_grouping_test.dart, test/features/dashboard/presentation/screens/dashboard_screen_top_panel_test.dart, test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart</files>
  <behavior>
    - RED test proves dashboard has explicit top and bottom visual groups and keeps scroll root (DLAY-01, per D-17-01 and D-17-02).
    - RED test proves top group includes connect button + status/timer + active server + statistics visibility path (DLAY-02, per D-17-07).
    - RED test proves selected-server state in `ActiveServerCard` gets parked/highlight style while null state stays neutral (DLAY-04, per D-17-03 and D-17-04).
  </behavior>
  <action>Create the three missing widget test files from 17-VALIDATION.md Wave 0 map. Keep assertions structural and behavior-safe: verify grouping keys/containers, ordering anchors, and selected-vs-unselected visual deltas; do not introduce any provider/business-logic rewrites. These tests should fail before production changes and become green after Tasks 2-3.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/screens/dashboard_screen_layout_grouping_test.dart && flutter test test/features/dashboard/presentation/screens/dashboard_screen_top_panel_test.dart && flutter test test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart</automated>
  </verify>
  <done>Wave-0 tests exist and exercise DLAY-01, DLAY-02, and DLAY-04 expectations explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor DashboardScreen into top/bottom visual groups with announcement-first bottom composition</name>
  <files>lib/features/dashboard/presentation/screens/dashboard_screen.dart, test/features/dashboard/presentation/screens/dashboard_screen_layout_grouping_test.dart, test/features/dashboard/presentation/screens/dashboard_screen_top_panel_test.dart, test/features/dashboard/presentation/screens/dashboard_screen_announcement_section_test.dart</files>
  <behavior>
    - Dashboard remains a `SingleChildScrollView` and uses visual grouping cues (spacing/min-height/container rhythm) to emulate 35/65 instead of hard split math (DLAY-01, per D-17-01 and D-17-02).
    - Top group contains connect button, status/timer, active server card, and statistics card path (DLAY-02, per D-17-07).
    - Bottom group renders announcement first only when content exists, then default servers section; empty announcement is hidden so defaults move up (DLAY-03, per D-17-05 and D-17-06).
  </behavior>
  <action>In `dashboard_screen.dart`, wrap existing sections into explicit top and bottom visual group containers (with stable test keys) while preserving existing provider reads, connect status text mapping, read-more bottom sheet behavior, and FAB behavior unchanged (per D-17-07 and D-17-08). Keep announcement gating logic content-driven and keep `DefaultServersSection` directly after announcement block in bottom group. Do not introduce strict `MediaQuery * 0.35/0.65` hard heights.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/screens/dashboard_screen_layout_grouping_test.dart && flutter test test/features/dashboard/presentation/screens/dashboard_screen_top_panel_test.dart && flutter test test/features/dashboard/presentation/screens/dashboard_screen_announcement_section_test.dart</automated>
  </verify>
  <done>Dashboard visually reads as grouped 35/65 composition with top/bottom content requirements met and announcement/default ordering preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply parked highlight treatment to ActiveServerCard and run focused parity suite</name>
  <files>lib/features/dashboard/presentation/widgets/active_server_card.dart, test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart</files>
  <behavior>
    - Existing `ActiveServerCard` remains the selected-server surface and tap navigation shell logic stays unchanged (DLAY-04, per D-17-03 and D-17-07).
    - Selected state gets clear parked treatment (accent border/tint emphasis) while keeping readability and accessibility contrast (DLAY-04, per D-17-04).
    - No changes to default-server tap behavior, connection flow, or provider contracts (parity guardrail per D-17-07).
  </behavior>
  <action>Modify only card styling in `active_server_card.dart` to differentiate selected (`server != null`) from unselected state (accent border/background tint and optional subtle label treatment within existing structure). Keep card shape, tap target, and routing behavior intact. Make the parked-style test green and run a focused dashboard regression suite plus analyze on touched production files.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart && flutter test test/features/dashboard/presentation/screens/dashboard_screen_announcement_section_test.dart && flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart && flutter analyze lib/features/dashboard/presentation/screens/dashboard_screen.dart lib/features/dashboard/presentation/widgets/active_server_card.dart</automated>
  </verify>
  <done>Selected server is visibly parked/highlighted with behavior parity preserved and focused regression checks passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard provider state → UI composition | Untrusted/variable provider values drive ordering/visibility in grouped dashboard containers |
| User tap on selected server card → navigation | UI event crosses into app navigation and must preserve intended route behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T | `dashboard_screen.dart` grouped composition | mitigate | Keep existing provider reads and interaction handlers unchanged; enforce ordering/visibility with widget tests (`layout_grouping`, `announcement_section`) so layout refactor cannot tamper with behavior. |
| T-17-02 | S | `active_server_card.dart` selected-state affordance | mitigate | Bind parked highlight strictly to `activeServerProvider` selected value (`server != null`), not arbitrary UI flags; assert selected/unselected styling in dedicated widget test. |
| T-17-03 | D | Dashboard scroll composition | accept | Single scroll view remains by design (per D-17-01); no new loops/background work introduced, low DoS risk in UI-only scope. |
| T-17-04 | I | Announcement/default content rendering | accept | Phase keeps existing data contract and does not expose new sensitive fields; render-only re-grouping with current localized copy. |
</threat_model>

<verification>
- `flutter test test/features/dashboard/presentation/screens/dashboard_screen_layout_grouping_test.dart`
- `flutter test test/features/dashboard/presentation/screens/dashboard_screen_top_panel_test.dart`
- `flutter test test/features/dashboard/presentation/screens/dashboard_screen_announcement_section_test.dart`
- `flutter test test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart`
- `flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart`
- `flutter analyze lib/features/dashboard/presentation/screens/dashboard_screen.dart lib/features/dashboard/presentation/widgets/active_server_card.dart`
</verification>

<success_criteria>
- DLAY-01: Dashboard clearly presents top/bottom visual groups that emulate 35/65 while keeping scroll-based composition.
- DLAY-02: Top group contains connect button, status/timer, selected server card, and statistics path (when enabled).
- DLAY-03: Bottom group shows announcement first when present, hides announcement when empty, and then shows default servers.
- DLAY-04: Selected server is clearly parked/highlighted using `ActiveServerCard` without changing navigation or provider behavior.
- Deferred ideas remain out of scope (no adaptive device ratio logic, no behavior/data-contract rewrites).
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard-35-65-layout-refresh/17-01-SUMMARY.md`
</output>
