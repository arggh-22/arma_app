---
phase: 02-vpn-engine-core-connection
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt
  - android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt
  - lib/features/connection/domain/entities/connection_status.dart
  - lib/features/connection/domain/entities/traffic_stats.dart
  - lib/features/connection/data/datasources/vpn_platform_service.dart
  - lib/features/connection/presentation/providers/connection_provider.dart
  - lib/features/connection/presentation/providers/connection_provider.g.dart
  - lib/features/connection/presentation/providers/traffic_stats_provider.dart
  - lib/features/connection/presentation/providers/traffic_stats_provider.g.dart
autonomous: true
requirements: [ENG-03, ENG-04, MON-01, MON-02]

must_haves:
  truths:
    - "MainActivity bridges Flutter MethodChannel/EventChannel to VPN process via Messenger IPC"
    - "Flutter can invoke startVpn, stopVpn, isRunning, requestVpnPermission via MethodChannel"
    - "Flutter receives connection state changes and traffic stats via EventChannel stream"
    - "ConnectionNotifier manages state machine: Disconnected → Connecting → Connected → Disconnecting → Disconnected"
    - "TrafficStatsNotifier receives traffic events and exposes formatted speeds"
    - "Connection timer tracks elapsed time since connected"
  artifacts:
    - path: "android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt"
      provides: "Messenger-based IPC bridge between main process and VPN process"
      contains: "VpnServiceConnection"
    - path: "android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt"
      provides: "MethodChannel + EventChannel handlers, VPN permission flow"
      contains: "com.arma.vpn/method"
    - path: "lib/features/connection/domain/entities/connection_status.dart"
      provides: "Sealed class for VPN connection states"
      contains: "sealed class ConnectionStatus"
    - path: "lib/features/connection/domain/entities/traffic_stats.dart"
      provides: "Traffic stats data class with formatted speeds"
      contains: "TrafficStats"
    - path: "lib/features/connection/data/datasources/vpn_platform_service.dart"
      provides: "MethodChannel/EventChannel wrapper for native VPN operations"
      contains: "VpnPlatformService"
    - path: "lib/features/connection/presentation/providers/connection_provider.dart"
      provides: "Riverpod notifier for connection state machine"
      exports: ["connectionProvider"]
    - path: "lib/features/connection/presentation/providers/traffic_stats_provider.dart"
      provides: "Riverpod notifier for traffic stats streaming"
      exports: ["trafficStatsProvider"]
  key_links:
    - from: "lib/features/connection/presentation/providers/connection_provider.dart"
      to: "lib/features/connection/data/datasources/vpn_platform_service.dart"
      via: "Uses VpnPlatformService for native calls"
      pattern: "VpnPlatformService"
    - from: "lib/features/connection/presentation/providers/connection_provider.dart"
      to: "lib/xray/xray_config_builder.dart"
      via: "Builds config JSON before connecting"
      pattern: "XrayConfigBuilder.build"
    - from: "android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt"
      to: "android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt"
      via: "Binds to VPN service for IPC"
      pattern: "VpnServiceConnection"
    - from: "android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt"
      to: "android/app/src/main/kotlin/com/arma/vpn/service/ArmaVpnService.kt"
      via: "Starts service via Intent and binds for Messenger IPC"
      pattern: "ArmaVpnService"
---

<objective>
Build the complete communication bridge: Kotlin IPC between main process and VPN process (Messenger), Flutter platform channels (MethodChannel/EventChannel), VPN permission flow, and Dart-side connection state management with Riverpod providers.

Purpose: This plan connects all the pieces — the native VPN engine (Plan 03) becomes controllable from Flutter (Plan 02's config builder feeds it). After this plan, Flutter code can call connect/disconnect and receive real-time state updates and traffic stats.

Output: Working Flutter ↔ Kotlin ↔ VPN process communication bridge, connection state machine, traffic stats streaming.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-vpn-engine-core-connection/02-CONTEXT.md
@.planning/phases/02-vpn-engine-core-connection/02-RESEARCH.md
@.planning/phases/02-vpn-engine-core-connection/02-02-SUMMARY.md
@.planning/phases/02-vpn-engine-core-connection/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 02: Config builder -->
From lib/xray/xray_config_builder.dart:
```dart
class XrayConfigBuilder {
  XrayConfigBuilder._();
  static String build(ServerConfig server) { /* returns JSON string */ }
}
```

From lib/xray/formatters/speed_formatter.dart:
```dart
String formatSpeed(int bytesPerSecond) { /* returns "X.X KB/s" etc */ }
```

<!-- From Plan 03: ArmaVpnService constants -->
From ArmaVpnService.kt companion object:
```kotlin
const val ACTION_START = "com.arma.vpn.START"
const val ACTION_STOP = "com.arma.vpn.STOP"
const val EXTRA_CONFIG = "config"
const val EXTRA_SERVER_NAME = "server_name"
const val MSG_REGISTER_CLIENT = 1
const val MSG_VPN_STATUS = 2
const val MSG_TRAFFIC_STATS = 3
const val MSG_COMMAND_START = 4
const val MSG_COMMAND_STOP = 5
const val MSG_IS_RUNNING = 6
```

<!-- From existing codebase: active server provider pattern -->
From lib/features/server/presentation/providers/active_server_provider.dart:
```dart
@riverpod
class ActiveServerNotifier extends _$ActiveServerNotifier {
  @override
  ServerConfig? build() { /* ... */ }
  Future<void> selectServer(ServerConfig? server) async { /* ... */ }
}
```

<!-- Platform channel contract from RESEARCH.md -->
MethodChannel: "com.arma.vpn/method"
  - startVpn({"config": String, "serverName": String}) → bool
  - stopVpn() → bool
  - isRunning() → bool
  - requestVpnPermission() → bool

EventChannel: "com.arma.vpn/vpn_status"
  - {"type": "status", "state": "connecting"|"connected"|"disconnected"|"error", "message": "..."}
  - {"type": "stats", "uplink": int, "downlink": int}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IPC ServiceConnection + MainActivity platform channels</name>
  <files>android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt, android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt</files>
  <read_first>
    - android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt (being modified — currently just `class MainActivity : FlutterActivity()`)
    - android/app/src/main/kotlin/com/arma/vpn/service/ArmaVpnService.kt (Plan 03 output — need to reference MSG_ constants and ACTION_ constants)
    - .planning/phases/02-vpn-engine-core-connection/02-RESEARCH.md §Pattern 1: Two-Hop IPC Bridge (complete implementation pattern)
    - .planning/phases/02-vpn-engine-core-connection/02-RESEARCH.md §Platform Channel Contract (MethodChannel methods + EventChannel events)
    - .planning/phases/02-vpn-engine-core-connection/02-RESEARCH.md §VPN Permission Flow (onActivityResult pattern)
  </read_first>
  <action>
**Create `android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt`:**

Package: `com.arma.vpn.ipc`

Class `VpnServiceConnection` with constructor param `onStatusUpdate: (Map<String, Any>) -> Unit`:

```kotlin
private var vpnServiceMessenger: Messenger? = null
private val incomingHandler = Handler(Looper.getMainLooper()) { msg ->
    when (msg.what) {
        ArmaVpnService.MSG_VPN_STATUS -> {
            val status = msg.data.getString("status") ?: "disconnected"
            onStatusUpdate(mapOf("type" to "status", "state" to status))
        }
        ArmaVpnService.MSG_TRAFFIC_STATS -> {
            val up = msg.data.getLong("uplink", 0)
            val down = msg.data.getLong("downlink", 0)
            onStatusUpdate(mapOf("type" to "stats", "uplink" to up, "downlink" to down))
        }
        ArmaVpnService.MSG_IS_RUNNING -> {
            val running = msg.data.getBoolean("running", false)
            onStatusUpdate(mapOf("type" to "isRunning", "running" to running))
        }
    }
    true
}

val serviceConnection = object : android.content.ServiceConnection {
    override fun onServiceConnected(name: ComponentName?, service: IBinder?) {
        vpnServiceMessenger = Messenger(service)
        // Register this process as reply target
        val msg = Message.obtain(null, ArmaVpnService.MSG_REGISTER_CLIENT)
        msg.replyTo = Messenger(incomingHandler)
        vpnServiceMessenger?.send(msg)
    }
    override fun onServiceDisconnected(name: ComponentName?) {
        vpnServiceMessenger = null
    }
}

val isConnected: Boolean get() = vpnServiceMessenger != null

fun sendStart(config: String, serverName: String) {
    val msg = Message.obtain(null, ArmaVpnService.MSG_COMMAND_START)
    msg.data = Bundle().apply {
        putString("config", config)
        putString("serverName", serverName)
    }
    vpnServiceMessenger?.send(msg)
}

fun sendStop() {
    val msg = Message.obtain(null, ArmaVpnService.MSG_COMMAND_STOP)
    vpnServiceMessenger?.send(msg)
}

fun queryIsRunning(replyTo: Messenger) {
    val msg = Message.obtain(null, ArmaVpnService.MSG_IS_RUNNING)
    msg.replyTo = replyTo
    vpnServiceMessenger?.send(msg)
}
```

Imports: `android.content.ComponentName`, `android.os.*`, `com.arma.vpn.service.ArmaVpnService`.

**Rewrite `android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt`:**

Package: `com.arma.vpn`

Replace the entire file. Class `MainActivity` extends `FlutterActivity()`:

```kotlin
private lateinit var methodChannel: MethodChannel
private var eventSink: EventChannel.EventSink? = null
private var vpnPermissionResult: MethodChannel.Result? = null
private lateinit var vpnConnection: VpnServiceConnection

companion object {
    private const val METHOD_CHANNEL = "com.arma.vpn/method"
    private const val EVENT_CHANNEL = "com.arma.vpn/vpn_status"
    private const val VPN_PERMISSION_REQUEST_CODE = 24
}

override fun configureFlutterEngine(flutterEngine: FlutterEngine) {
    super.configureFlutterEngine(flutterEngine)
    
    // Initialize IPC bridge
    vpnConnection = VpnServiceConnection { event ->
        runOnUiThread { eventSink?.success(event) }
    }
    
    // Bind to VPN service
    bindVpnService()
    
    // MethodChannel setup
    methodChannel = MethodChannel(flutterEngine.dartExecutor.binaryMessenger, METHOD_CHANNEL)
    methodChannel.setMethodCallHandler { call, result ->
        when (call.method) {
            "startVpn" -> {
                val config = call.argument<String>("config")
                val serverName = call.argument<String>("serverName")
                if (config == null || serverName == null) {
                    result.error("INVALID_ARGS", "config and serverName required", null)
                    return@setMethodCallHandler
                }
                startVpnService(config, serverName)
                result.success(true)
            }
            "stopVpn" -> {
                vpnConnection.sendStop()
                result.success(true)
            }
            "isRunning" -> {
                // For simplicity, start service intent to check
                // In practice, the EventChannel keeps state in sync
                result.success(vpnConnection.isConnected)
            }
            "requestVpnPermission" -> {
                requestVpnPermission(result)
            }
            else -> result.notImplemented()
        }
    }
    
    // EventChannel setup
    val eventChannel = EventChannel(flutterEngine.dartExecutor.binaryMessenger, EVENT_CHANNEL)
    eventChannel.setStreamHandler(object : EventChannel.StreamHandler {
        override fun onListen(arguments: Any?, events: EventChannel.EventSink?) {
            eventSink = events
        }
        override fun onCancel(arguments: Any?) {
            eventSink = null
        }
    })
}

private fun bindVpnService() {
    val intent = Intent(this, ArmaVpnService::class.java)
    bindService(intent, vpnConnection.serviceConnection, Context.BIND_AUTO_CREATE)
}

private fun startVpnService(config: String, serverName: String) {
    val intent = Intent(this, ArmaVpnService::class.java).apply {
        action = ArmaVpnService.ACTION_START
        putExtra(ArmaVpnService.EXTRA_CONFIG, config)
        putExtra(ArmaVpnService.EXTRA_SERVER_NAME, serverName)
    }
    startForegroundService(intent)
    
    // Also send via Messenger if bound
    if (vpnConnection.isConnected) {
        vpnConnection.sendStart(config, serverName)
    }
}

private fun requestVpnPermission(result: MethodChannel.Result) {
    val intent = android.net.VpnService.prepare(this)
    if (intent != null) {
        vpnPermissionResult = result
        startActivityForResult(intent, VPN_PERMISSION_REQUEST_CODE)
    } else {
        result.success(true)  // Already granted
    }
}

override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {
    if (requestCode == VPN_PERMISSION_REQUEST_CODE) {
        vpnPermissionResult?.success(resultCode == RESULT_OK)
        vpnPermissionResult = null
        return
    }
    super.onActivityResult(requestCode, resultCode, data)
}

override fun onDestroy() {
    try { unbindService(vpnConnection.serviceConnection) } catch (e: Exception) { /* ignore */ }
    super.onDestroy()
}
```

Imports: `android.content.Context`, `android.content.Intent`, `android.os.Bundle`, `io.flutter.embedding.android.FlutterActivity`, `io.flutter.embedding.engine.FlutterEngine`, `io.flutter.plugin.common.EventChannel`, `io.flutter.plugin.common.MethodChannel`, `com.arma.vpn.ipc.VpnServiceConnection`, `com.arma.vpn.service.ArmaVpnService`.
  </action>
  <acceptance_criteria>
    - `grep -c "class VpnServiceConnection" android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt` returns 1
    - `grep -c "MSG_VPN_STATUS" android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt` returns at least 1
    - `grep -c "MSG_TRAFFIC_STATS" android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt` returns at least 1
    - `grep -c "sendStart" android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt` returns at least 1
    - `grep -c "sendStop" android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt` returns at least 1
    - `grep '"com.arma.vpn/method"' android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` matches
    - `grep '"com.arma.vpn/vpn_status"' android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` matches
    - `grep -c "startVpn" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 1
    - `grep -c "stopVpn" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 1
    - `grep -c "isRunning" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 1
    - `grep -c "requestVpnPermission" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 2
    - `grep -c "VPN_PERMISSION_REQUEST_CODE" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 2
    - `grep -c "onActivityResult" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 1
    - `grep -c "configureFlutterEngine" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns 1
    - `grep -c "VpnServiceConnection" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 2
    - `grep -c "bindService" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns at least 1
    - `grep -c "startForegroundService" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt` returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && grep -q "com.arma.vpn/method" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt && grep -q "com.arma.vpn/vpn_status" android/app/src/main/kotlin/com/arma/vpn/MainActivity.kt && grep -q "VpnServiceConnection" android/app/src/main/kotlin/com/arma/vpn/ipc/ServiceConnection.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ServiceConnection bridges Messenger IPC between processes with sendStart/sendStop/queryIsRunning. MainActivity registers MethodChannel ("com.arma.vpn/method") with startVpn/stopVpn/isRunning/requestVpnPermission handlers, EventChannel ("com.arma.vpn/vpn_status") streaming status+stats events, VPN permission flow with onActivityResult, and binds to VpnService on engine configure.</done>
</task>

<task type="auto">
  <name>Task 2: Dart connection entities + platform service + Riverpod providers</name>
  <files>lib/features/connection/domain/entities/connection_status.dart, lib/features/connection/domain/entities/traffic_stats.dart, lib/features/connection/data/datasources/vpn_platform_service.dart, lib/features/connection/presentation/providers/connection_provider.dart, lib/features/connection/presentation/providers/traffic_stats_provider.dart</files>
  <read_first>
    - lib/features/server/presentation/providers/active_server_provider.dart (Riverpod provider pattern — follow same @riverpod annotation style)
    - lib/features/server/domain/entities/server_config.dart (ServerConfig entity — used by ConnectionNotifier.connect())
    - lib/xray/xray_config_builder.dart (Plan 02 — called by ConnectionNotifier to build config JSON)
    - lib/xray/formatters/speed_formatter.dart (Plan 02 — used for formatting traffic speeds)
    - .planning/phases/02-vpn-engine-core-connection/02-RESEARCH.md §Pattern 2: Connection State Machine (sealed class pattern)
    - .planning/phases/02-vpn-engine-core-connection/02-RESEARCH.md §VpnPlatformService (Dart-side channel wrapper)
    - .planning/phases/02-vpn-engine-core-connection/02-RESEARCH.md §ConnectionNotifier (Riverpod) (reference implementation)
  </read_first>
  <action>
**Create directory structure:**
```bash
mkdir -p lib/features/connection/domain/entities
mkdir -p lib/features/connection/data/datasources
mkdir -p lib/features/connection/presentation/providers
mkdir -p lib/features/connection/presentation/widgets
```

**Create `lib/features/connection/domain/entities/connection_status.dart`:**

```dart
/// Sealed class modeling VPN connection states.
/// Valid transitions: Disconnected → Connecting → Connected → Disconnecting → Disconnected
///                    Connecting → Disconnected (on error)
sealed class ConnectionStatus {
  const ConnectionStatus();
}

class Disconnected extends ConnectionStatus {
  final String? lastError;
  const Disconnected([this.lastError]);
}

class Connecting extends ConnectionStatus {
  final String serverName;
  const Connecting(this.serverName);
}

class Connected extends ConnectionStatus {
  final String serverName;
  final DateTime connectedAt;
  const Connected({required this.serverName, required this.connectedAt});
}

class Disconnecting extends ConnectionStatus {
  const Disconnecting();
}
```

**Create `lib/features/connection/domain/entities/traffic_stats.dart`:**

```dart
/// Real-time traffic statistics from VPN engine.
class TrafficStats {
  final int uplinkBytesPerSecond;
  final int downlinkBytesPerSecond;
  
  const TrafficStats({
    this.uplinkBytesPerSecond = 0,
    this.downlinkBytesPerSecond = 0,
  });
  
  static const zero = TrafficStats();
}
```

**Create `lib/features/connection/data/datasources/vpn_platform_service.dart`:**

```dart
import 'package:flutter/services.dart';

class VpnPlatformService {
  static const _methodChannel = MethodChannel('com.arma.vpn/method');
  static const _eventChannel = EventChannel('com.arma.vpn/vpn_status');

  Future<bool> startVpn(String configJson, String serverName) async {
    return await _methodChannel.invokeMethod<bool>(
      'startVpn', {'config': configJson, 'serverName': serverName}
    ) ?? false;
  }

  Future<bool> stopVpn() async {
    return await _methodChannel.invokeMethod<bool>('stopVpn') ?? false;
  }

  Future<bool> get isRunning async {
    return await _methodChannel.invokeMethod<bool>('isRunning') ?? false;
  }
  
  Future<bool> requestVpnPermission() async {
    return await _methodChannel.invokeMethod<bool>('requestVpnPermission') ?? false;
  }

  Stream<Map<String, dynamic>> get vpnEvents {
    return _eventChannel.receiveBroadcastStream().map(
      (event) => Map<String, dynamic>.from(event as Map),
    );
  }
}
```

**Create `lib/features/connection/presentation/providers/connection_provider.dart`:**

```dart
import 'dart:async';
import 'package:riverpod_annotation/riverpod_annotation.dart';
import 'package:arma_proxy_vpn_client/features/connection/domain/entities/connection_status.dart';
import 'package:arma_proxy_vpn_client/features/connection/data/datasources/vpn_platform_service.dart';
import 'package:arma_proxy_vpn_client/features/server/domain/entities/server_config.dart';
import 'package:arma_proxy_vpn_client/xray/xray_config_builder.dart';

part 'connection_provider.g.dart';

@Riverpod(keepAlive: true)
class ConnectionNotifier extends _$ConnectionNotifier {
  final VpnPlatformService _platformService = VpnPlatformService();
  StreamSubscription? _eventSubscription;
  Timer? _durationTimer;
  
  @override
  ConnectionStatus build() {
    _eventSubscription = _platformService.vpnEvents
        .where((e) => e['type'] == 'status')
        .listen(_handleStatusEvent);
    
    ref.onDispose(() {
      _eventSubscription?.cancel();
      _durationTimer?.cancel();
    });
    
    // Sync with native on build (app resume)
    _syncWithNative();
    
    return const Disconnected();
  }
  
  Future<void> connect(ServerConfig server) async {
    if (state is Connecting || state is Connected) return;
    
    state = Connecting(server.name);
    
    // 1. Request VPN permission if needed
    final hasPermission = await _platformService.requestVpnPermission();
    if (!hasPermission) {
      state = const Disconnected('VPN permission denied');
      return;
    }
    
    // 2. Build Xray JSON config (D-02: all config logic in Dart)
    final configJson = XrayConfigBuilder.build(server);
    
    // 3. Start VPN via platform channel
    try {
      final started = await _platformService.startVpn(configJson, server.name);
      if (!started) {
        state = const Disconnected('Failed to start VPN');
      }
      // Connected state will be set by EventChannel callback
    } catch (e) {
      state = Disconnected('Error: ${e.toString()}');
    }
  }
  
  Future<void> disconnect() async {
    if (state is Disconnected || state is Disconnecting) return;
    state = const Disconnecting();
    try {
      await _platformService.stopVpn();
      // Disconnected state will be set by EventChannel callback
    } catch (e) {
      state = const Disconnected();
    }
  }
  
  void _handleStatusEvent(Map<String, dynamic> event) {
    final status = event['state'] as String?;
    switch (status) {
      case 'connecting':
        if (state is! Connecting) {
          state = const Connecting('...');
        }
      case 'connected':
        final serverName = state is Connecting ? (state as Connecting).serverName : 'Active';
        state = Connected(serverName: serverName, connectedAt: DateTime.now());
      case 'disconnected':
        _durationTimer?.cancel();
        state = Disconnected(event['message'] as String?);
      case 'error':
        _durationTimer?.cancel();
        state = Disconnected(event['message'] as String? ?? 'Connection error');
    }
  }
  
  Future<void> _syncWithNative() async {
    try {
      final running = await _platformService.isRunning;
      if (running && state is Disconnected) {
        state = Connected(serverName: 'Active', connectedAt: DateTime.now());
      }
    } catch (_) {
      // Platform channel not ready yet — ignore
    }
  }
}
```

**Create `lib/features/connection/presentation/providers/traffic_stats_provider.dart`:**

```dart
import 'dart:async';
import 'package:riverpod_annotation/riverpod_annotation.dart';
import 'package:arma_proxy_vpn_client/features/connection/domain/entities/traffic_stats.dart';
import 'package:arma_proxy_vpn_client/features/connection/data/datasources/vpn_platform_service.dart';

part 'traffic_stats_provider.g.dart';

@Riverpod(keepAlive: true)
class TrafficStatsNotifier extends _$TrafficStatsNotifier {
  final VpnPlatformService _platformService = VpnPlatformService();
  StreamSubscription? _eventSubscription;
  
  @override
  TrafficStats build() {
    _eventSubscription = _platformService.vpnEvents
        .where((e) => e['type'] == 'stats')
        .listen((event) {
      state = TrafficStats(
        uplinkBytesPerSecond: (event['uplink'] as num?)?.toInt() ?? 0,
        downlinkBytesPerSecond: (event['downlink'] as num?)?.toInt() ?? 0,
      );
    });
    
    ref.onDispose(() => _eventSubscription?.cancel());
    
    return TrafficStats.zero;
  }
  
  void reset() {
    state = TrafficStats.zero;
  }
}
```

**Run code generation:**
```bash
dart run build_runner build --delete-conflicting-outputs
```
This generates `connection_provider.g.dart` and `traffic_stats_provider.g.dart`.

**Verify generated files contain provider functions:**
- `connection_provider.g.dart` should contain `connectionProvider`
- `traffic_stats_provider.g.dart` should contain `trafficStatsProvider`
  </action>
  <acceptance_criteria>
    - `grep -c "sealed class ConnectionStatus" lib/features/connection/domain/entities/connection_status.dart` returns 1
    - `grep -c "class Disconnected extends ConnectionStatus" lib/features/connection/domain/entities/connection_status.dart` returns 1
    - `grep -c "class Connecting extends ConnectionStatus" lib/features/connection/domain/entities/connection_status.dart` returns 1
    - `grep -c "class Connected extends ConnectionStatus" lib/features/connection/domain/entities/connection_status.dart` returns 1
    - `grep -c "class Disconnecting extends ConnectionStatus" lib/features/connection/domain/entities/connection_status.dart` returns 1
    - `grep -c "class TrafficStats" lib/features/connection/domain/entities/traffic_stats.dart` returns 1
    - `grep -c "static const zero" lib/features/connection/domain/entities/traffic_stats.dart` returns 1
    - `grep "'com.arma.vpn/method'" lib/features/connection/data/datasources/vpn_platform_service.dart` matches
    - `grep "'com.arma.vpn/vpn_status'" lib/features/connection/data/datasources/vpn_platform_service.dart` matches
    - `grep -c "class ConnectionNotifier" lib/features/connection/presentation/providers/connection_provider.dart` returns 1
    - `grep -c "XrayConfigBuilder.build" lib/features/connection/presentation/providers/connection_provider.dart` returns 1
    - `grep -c "requestVpnPermission" lib/features/connection/presentation/providers/connection_provider.dart` returns at least 1
    - `grep -c "class TrafficStatsNotifier" lib/features/connection/presentation/providers/traffic_stats_provider.dart` returns 1
    - `grep -c "keepAlive: true" lib/features/connection/presentation/providers/connection_provider.dart` returns 1
    - `test -f lib/features/connection/presentation/providers/connection_provider.g.dart && echo "generated"` prints "generated"
    - `test -f lib/features/connection/presentation/providers/traffic_stats_provider.g.dart && echo "generated"` prints "generated"
    - `grep "connectionProvider" lib/features/connection/presentation/providers/connection_provider.g.dart` matches
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter analyze lib/features/connection/ 2>&1 | tail -3</automated>
  </verify>
  <done>ConnectionStatus sealed class with 4 states, TrafficStats data class, VpnPlatformService wrapping MethodChannel/EventChannel, ConnectionNotifier (Riverpod keepAlive) with connect/disconnect/state machine/native sync, TrafficStatsNotifier streaming uplink/downlink stats. All generated .g.dart files present. flutter analyze passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter ↔ Kotlin MethodChannel | Serialized method calls cross FFI boundary |
| Main process ↔ VPN process | Messenger IPC across Android process boundary |
| User input → VPN connection | ServerConfig data flows through config builder to native engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Spoofing | MethodChannel messages | accept | Platform channels are intra-process (main process only). No external app can call them. The VpnServiceConnection is bound with BIND_AUTO_CREATE to our own service. |
| T-02-13 | Information Disclosure | EventChannel data | accept | Traffic stats (bytes/sec) and connection state are not sensitive. They stay within the app process. No PII in events. |
| T-02-14 | Repudiation | Connection state desync | mitigate | ConnectionNotifier syncs with native `isRunning` on build (app resume). EventChannel provides real-time state updates. If EventChannel drops, provider still re-syncs via MethodChannel. |
| T-02-15 | Denial of Service | VPN permission denied | mitigate | ConnectionNotifier checks permission result and sets Disconnected('VPN permission denied') state. User sees clear error. No crash or hang. |
</threat_model>

<verification>
- `flutter analyze lib/features/connection/` — no errors
- All Dart files compile and generated files exist
- `flutter build apk --debug` compiles successfully (full native + Dart build)
- ConnectionNotifier has connect/disconnect methods that call platform channels
- VpnPlatformService uses correct channel names matching Kotlin side
</verification>

<success_criteria>
- IPC bridge: ServiceConnection handles Messenger communication between processes
- Platform channels: MainActivity registers "com.arma.vpn/method" and "com.arma.vpn/vpn_status" with all 4 MethodChannel handlers
- VPN permission: onActivityResult flow returns result to Flutter
- ConnectionStatus: sealed class with Disconnected/Connecting/Connected/Disconnecting states
- ConnectionNotifier: Riverpod keepAlive provider with connect/disconnect/state machine/native sync
- TrafficStatsNotifier: streams traffic stats from EventChannel
- All generated .g.dart files present and valid
</success_criteria>

<output>
After completion, create `.planning/phases/02-vpn-engine-core-connection/02-04-SUMMARY.md`
</output>
