---
phase: 17-dashboard-35-65-layout-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/dashboard/presentation/widgets/default_servers_section.dart
  - test/features/dashboard/presentation/widgets/default_servers_section_test.dart
autonomous: true
gap_closure: true
requirements:
  - DLAY-04
must_haves:
  truths:
    - "When a server is selected, the matching tile in DefaultServersSection is visibly highlighted (parity with ActiveServerCard selected treatment)."
    - "Only the tile whose `serverConfig.id` matches `activeServerProvider` is highlighted; all other tiles stay neutral."
    - "Default-server tile tap behavior remains unchanged: disconnected selects only, connected-and-different triggers disconnect+connect."
  artifacts:
    - path: "lib/features/dashboard/presentation/widgets/default_servers_section.dart"
      provides: "Selected-state propagation from activeServerProvider into default-server tile rendering with highlighted styling branch."
    - path: "test/features/dashboard/presentation/widgets/default_servers_section_test.dart"
      provides: "Regression coverage for selected/highlighted tile parity and unchanged tap/connect behavior."
  key_links:
    - from: "default_servers_section.dart"
      to: "active_server_provider.dart"
      via: "active selection read in build and `isSelected` derivation per tile"
      pattern: "ref\\.watch\\(activeServerProvider\\)|item\\.serverConfig\\?\\.id == activeServer\\?\\.id"
    - from: "default_servers_section.dart"
      to: "_DefaultServerTile"
      via: "selected-state argument passed from section loop into tile widget"
      pattern: "_DefaultServerTile\\(.*isSelected:"
    - from: "default_servers_section_test.dart"
      to: "default_servers_section.dart"
      via: "widget assertions for selected vs neutral tile visual treatment"
      pattern: "find\\.byType\\(_DefaultServerTile\\)|Material\\(|shape:"
---

<objective>
Close UAT gap #4 by adding selected/highlighted state parity for default-server list tiles in the dashboard bottom section.

Purpose: Ensure selected-server visibility is consistent across both ActiveServerCard and DefaultServersSection per diagnosed UAT failure.
Output: Selected-state-aware default tile rendering + widget tests proving highlight parity and tap/connect regression safety.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-UAT.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-CONTEXT.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-RESEARCH.md
@.planning/phases/17-dashboard-35-65-layout-refresh/17-01-SUMMARY.md
@lib/features/dashboard/presentation/widgets/default_servers_section.dart
@lib/features/dashboard/presentation/widgets/active_server_card.dart
@test/features/dashboard/presentation/widgets/default_servers_section_test.dart

<interfaces>
From `active_server_provider.dart`:
```dart
class ActiveServerNotifier extends _$ActiveServerNotifier {
  ServerConfig? build();
  Future<void> selectServer(ServerConfig? server);
}
```

From `default_servers_section.dart`:
```dart
class DefaultServersSection extends ConsumerStatefulWidget
Future<void> _onTapItem(DefaultServerItem item)
class _DefaultServerTile extends StatelessWidget
```

From `default_server_item.dart`:
```dart
class DefaultServerItem {
  final ServerConfig? serverConfig;
  bool get isConnectable => isActive && serverConfig != null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED widget tests for selected tile highlight parity in default servers list</name>
  <files>test/features/dashboard/presentation/widgets/default_servers_section_test.dart</files>
  <behavior>
    - When `activeServerProvider` matches a preview tile `serverConfig.id`, that tile renders selected/highlighted treatment (per D-17-04, D-17-07).
    - Non-matching tiles stay neutral in the same render pass (per D-17-07 parity guardrail).
    - Existing tap/connect tests still express current behavior contract (disconnected select-only, connected different server reconnect path).
  </behavior>
  <action>Add focused widget tests in `default_servers_section_test.dart` that seed two connectable items and an active server override, then assert visual delta between selected and non-selected tiles (e.g., border/surface tint branch) before implementation. Keep all existing interaction-path assertions intact and extend only what is needed for this UAT gap. Scope explicitly to UAT missing items: selected-state propagation + selected visual treatment + regression coverage.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart</automated>
  </verify>
  <done>Test suite fails on selected-tile parity expectations before production change and documents expected selected vs neutral rendering outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate active selection into `_DefaultServerTile` and render highlighted selected styling</name>
  <files>lib/features/dashboard/presentation/widgets/default_servers_section.dart, test/features/dashboard/presentation/widgets/default_servers_section_test.dart</files>
  <behavior>
    - Section watches `activeServerProvider` and derives tile selection by `item.serverConfig?.id == activeServer?.id` (per D-17-07).
    - Selected tile receives clear visual highlight treatment aligned with ActiveServerCard semantics (accent border/tint) while unselected remains current neutral style (per D-17-04).
    - `_onTapItem` connect behavior remains unchanged and existing reconnect tests stay green (per D-17-07).
  </behavior>
  <action>In `default_servers_section.dart`, read current active server in build, pass a new `isSelected` flag into `_DefaultServerTile`, and implement selected-vs-neutral material styling branch (accent border + tinted background with current shape/radius preserved). Do not change selection/connect workflow in `_onTapItem`, refresh flow, show-all flow, or provider contracts. Make Task 1 RED tests green while preserving prior behavior tests.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart</automated>
  </verify>
  <done>Matching default tile is visibly highlighted based on active selection and all existing interaction tests continue to pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused UAT-gap verification suite and analyzer checks</name>
  <files>lib/features/dashboard/presentation/widgets/default_servers_section.dart, test/features/dashboard/presentation/widgets/default_servers_section_test.dart</files>
  <action>Execute focused verification commands for selected-tile parity and run analyzer on touched production widget to ensure no lint/type regressions. Re-run ActiveServerCard parked-style test as parity guard to confirm selected-state visual contract remains coherent across both selected surfaces.</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart && flutter test test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart && flutter analyze lib/features/dashboard/presentation/widgets/default_servers_section.dart</automated>
  </verify>
  <done>Gap-closure checks and analyze pass, proving selected highlight parity exists in DefaultServersSection without behavioral regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider selection state → default tile styling | Active server identity influences UI highlighting and must not drift from canonical provider state |
| Default tile tap → connection transitions | User tap can trigger disconnect/connect side effects and must preserve existing guarded behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | T | `default_servers_section.dart` selected-state branch | mitigate | Bind selected styling exclusively to `activeServerProvider` + server ID equality and cover with widget tests for selected vs neutral tiles. |
| T-17-06 | S | Default tile identity mapping | mitigate | Compare by canonical `serverConfig.id` instead of display text to prevent wrong tile from being highlighted. |
| T-17-07 | D | `_onTapItem` connection flow | accept | No new loops/background work introduced; retain existing reconnect guards and verify with current tap-path tests. |
| T-17-08 | I | Default server tile rendering | accept | Styling-only change; no additional data exposure beyond existing item metadata. |
</threat_model>

<verification>
- `flutter test test/features/dashboard/presentation/widgets/default_servers_section_test.dart`
- `flutter test test/features/dashboard/presentation/widgets/active_server_card_parked_style_test.dart`
- `flutter analyze lib/features/dashboard/presentation/widgets/default_servers_section.dart`
</verification>

<success_criteria>
- UAT gap closed: selected state is highlighted in both ActiveServerCard and matching DefaultServersSection tile.
- Non-selected default tiles remain neutral while selected tile is clearly distinguished.
- Existing default-server tap/connect parity remains unchanged and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard-35-65-layout-refresh/17-02-SUMMARY.md`
</output>
