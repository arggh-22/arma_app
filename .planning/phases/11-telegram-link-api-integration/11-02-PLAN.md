---
phase: 11-telegram-link-api-integration
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - lib/features/api/presentation/providers/auth_provider.dart
  - lib/features/api/presentation/providers/telegram_link_provider.dart
  - test/features/api/presentation/providers/telegram_link_provider_test.dart
autonomous: true
requirements: [TGAPI-02, TGCOMP-01]
must_haves:
  truths:
    - "Telegram ID is validated client-side before any network request."
    - "Duplicate in-flight submits are blocked from triggering duplicate API calls."
    - "Provider exposes structured outcome objects with no new persisted Telegram-link storage."
  artifacts:
    - path: "lib/features/api/presentation/providers/telegram_link_provider.dart"
      provides: "submit entrypoint with validation + in-flight guard + typed outcome state"
    - path: "lib/features/api/presentation/providers/auth_provider.dart"
      provides: "provider wiring for Telegram link repository integration"
  key_links:
    - from: "telegram_link_provider.dart"
      to: "telegram_link_repository.dart"
      via: "submit() calls repository.linkTelegram"
      pattern: "linkTelegram"
    - from: "auth_provider.dart"
      to: "telegram_link_repository_impl.dart"
      via: "Riverpod provider graph wiring"
      pattern: "telegramLinkRepositoryProvider"
---

<objective>
Expose Phase-11 submit contract through Riverpod with locked validation rules and duplicate-submit protection.

Purpose: Provide a UI-ready (Phase 12) integration surface while staying inside API/domain scope.
Output: Telegram link providers + validation/in-flight guard tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-telegram-link-api-integration/11-CONTEXT.md
@lib/features/api/presentation/providers/auth_provider.dart
@lib/features/dashboard/presentation/providers/default_servers_provider.dart
@lib/features/api/data/repositories/auth_repository_impl.dart
@.planning/phases/11-telegram-link-api-integration/11-01-PLAN.md

<interfaces>
From `auth_provider.dart`:
```dart
@Riverpod(keepAlive: true)
AuthRepository authRepository(Ref ref) { ... }
```

From Phase 11 Plan 01 output:
```dart
abstract class TelegramLinkRepository {
  Future<TelegramLinkOutcome> linkTelegram(String telegramId);
}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire Telegram link repository into provider graph</name>
  <files>
    - lib/features/api/presentation/providers/auth_provider.dart
    - lib/features/api/presentation/providers/telegram_link_provider.dart
    - test/features/api/presentation/providers/telegram_link_provider_test.dart
  </files>
  <behavior>
    - Provider graph resolves `TelegramLinkRepositoryImpl` with `ApiClient` + `AuthRepository` dependencies.
    - Submit path uses existing auth retry flow from repository implementation (per D-04, D-05).
  </behavior>
  <action>Add keep-alive provider wiring for Telegram link repository in `auth_provider.dart` and consume it from `telegram_link_provider.dart`; do not add alternate auth/token path outside existing providers (per TGCOMP-01, D-04).</action>
  <verify>
    <automated>flutter test test/features/api/presentation/providers/telegram_link_provider_test.dart -r compact</automated>
  </verify>
  <done>Telegram link provider path is fully wired through the existing auth/provider graph.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement submit controller with validation + duplicate in-flight guard</name>
  <files>
    - lib/features/api/presentation/providers/telegram_link_provider.dart
    - test/features/api/presentation/providers/telegram_link_provider_test.dart
  </files>
  <behavior>
    - Input is trimmed before validation (D-01).
    - Only digits are accepted; non-digit characters fail validation (D-02).
    - Valid length is 5..20 digits; invalid length returns `invalid_id` without network call (D-03).
    - While submit is in-flight, repeated submit does not trigger a second repository request (TGAPI-02).
    - State/outcome remains structured and non-persistent (D-08).
  </behavior>
  <action>Create `TelegramLinkNotifier` (keepAlive) with deterministic state object (e.g., `isSubmitting`, `lastOutcome`, `lastSubmittedId`). Enforce locked validation before repository call (D-01/D-02/D-03), map failures to typed outcomes from plan 11-01, and guard duplicate in-flight submits by short-circuiting additional calls while current Future is active. Cover each rule with unit tests, including “no duplicate request count increase while busy.”</action>
  <verify>
    <automated>flutter test test/features/api/presentation/providers/telegram_link_provider_test.dart -r compact</automated>
  </verify>
  <done>Phase 12 can call one provider submit entrypoint that validates IDs, prevents duplicate submits, and emits typed outcomes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI input → provider submit | User-entered Telegram ID is untrusted input entering app domain logic. |
| provider → repository | Submit controller crosses into network/auth path and must avoid duplicate request storms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | T | telegram_link_provider.dart input handling | mitigate | Apply strict trim/digits/length validation before repository call (D-01/D-02/D-03). |
| T-11-05 | D | submit handler concurrency | mitigate | Enforce in-flight guard to suppress duplicate submit taps (TGAPI-02). |
| T-11-06 | R | provider graph integration | mitigate | Wire through existing auth repository path and assert with provider tests (TGCOMP-01). |
</threat_model>

<verification>
- flutter test test/features/api/presentation/providers/telegram_link_provider_test.dart -r compact
</verification>

<success_criteria>
- Invalid Telegram IDs are rejected pre-network under locked validation rules.
- Submit concurrency guard prevents duplicate network calls during in-flight operation.
- Provider exposes structured non-persistent outcome state for downstream UI phases.
</success_criteria>

<output>
After completion, create `.planning/phases/11-telegram-link-api-integration/11-02-SUMMARY.md`
</output>
