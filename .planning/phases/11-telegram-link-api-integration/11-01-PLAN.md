---
phase: 11-telegram-link-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/api/data/datasources/api_client.dart
  - lib/features/api/data/models/telegram_link_response.dart
  - lib/features/api/domain/entities/telegram_link_outcome.dart
  - lib/features/api/domain/repositories/telegram_link_repository.dart
  - lib/features/api/data/repositories/telegram_link_repository_impl.dart
  - test/features/api/data/datasources/api_client_test.dart
  - test/features/api/data/repositories/telegram_link_repository_impl_test.dart
autonomous: true
requirements: [TGAPI-01, TGCOMP-01]
must_haves:
  truths:
    - "App can call POST /auth/telegram/link/ with bearer auth and telegram_id payload."
    - "Link operation returns typed outcomes for success and failures."
    - "Unauthorized-after-retry follows existing auth lifecycle behavior."
  artifacts:
    - path: "lib/features/api/data/datasources/api_client.dart"
      provides: "telegram link endpoint call through existing retry/redaction pipeline"
    - path: "lib/features/api/data/repositories/telegram_link_repository_impl.dart"
      provides: "auth-retry aware mapping to TelegramLinkOutcome"
  key_links:
    - from: "telegram_link_repository_impl.dart"
      to: "auth_repository.dart"
      via: "executeWithAuthRetry"
      pattern: "executeWithAuthRetry"
    - from: "api_client.dart"
      to: "/auth/telegram/link/"
      via: "POST JSON telegram_id"
      pattern: "auth/telegram/link"
---

<objective>
Implement Telegram link API + repository contracts with typed outcomes and existing auth-retry compatibility.

Purpose: Satisfy backend integration and auth lifecycle compatibility before any Phase 12 UI work.
Output: API datasource method, Telegram link domain contracts, repository implementation, and passing tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-telegram-link-api-integration/11-CONTEXT.md
@.planning/phases/11-telegram-link-api-integration/11-RESEARCH.md
@lib/features/api/data/datasources/api_client.dart
@lib/features/api/data/repositories/auth_repository_impl.dart
@lib/features/api/domain/repositories/auth_repository.dart

<interfaces>
From `auth_repository.dart`:
```dart
Future<T> executeWithAuthRetry<T>(Future<T> Function(String token) action);
```

From `api_client.dart`:
```dart
enum ApiClientErrorType { timeout, network, unauthorized, client, server, malformedResponse, unknown }
class ApiClientException implements Exception { ... }
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Telegram link endpoint DTO + ApiClient method</name>
  <files>
    - lib/features/api/data/datasources/api_client.dart
    - lib/features/api/data/models/telegram_link_response.dart
    - test/features/api/data/datasources/api_client_test.dart
  </files>
  <behavior>
    - `linkTelegram(token, telegramId)` sends `POST /auth/telegram/link/` with `Authorization: Bearer ...` and body `{"telegram_id":"<id>"}` (per D-04, D-09).
    - Non-2xx status maps to existing `ApiClientException` types; transient retry behavior stays unchanged (per D-06).
    - Diagnostics output remains sanitized for authorization/token fields (per D-09).
  </behavior>
  <action>Add `TelegramLinkResponse` DTO parser and `ApiClient.linkTelegram(...)` that reuses `_sendWithRetry`, `_decodeJsonMap`, and existing sanitizer path; do not add custom HTTP stack or bypass `_send` (per D-06, D-09). Extend datasource tests for request contract, transient retry, and unauthorized behavior.</action>
  <verify>
    <automated>flutter test test/features/api/data/datasources/api_client_test.dart -r compact</automated>
  </verify>
  <done>`ApiClient` exposes a tested Telegram link endpoint call using the existing retry + redaction pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth-retry-aware Telegram link repository with typed outcomes</name>
  <files>
    - lib/features/api/domain/entities/telegram_link_outcome.dart
    - lib/features/api/domain/repositories/telegram_link_repository.dart
    - lib/features/api/data/repositories/telegram_link_repository_impl.dart
    - test/features/api/data/repositories/telegram_link_repository_impl_test.dart
  </files>
  <behavior>
    - Repository calls Telegram link API only through `authRepository.executeWithAuthRetry(...)` (per D-04, D-05).
    - Outcomes map to locked set: `linked`, `already_linked`, `invalid_id`, `unauthorized`, `network`, `server`, `unknown` (per D-07).
    - `AuthRepositoryException.unauthorizedAfterRetry` maps to `unauthorized` outcome (per D-05, TGCOMP-01).
  </behavior>
  <action>Create `TelegramLinkOutcome` typed contract and repository abstraction/impl. In impl, call `executeWithAuthRetry`, map API/auth exceptions deterministically, and keep unknown backend variants as `unknown` while preserving `already_linked` mapping when response/status indicates it (per D-07). Add repository tests for success, unauthorized-after-retry, transient/network/server/client mappings, and already-linked mapping branch.</action>
  <verify>
    <automated>flutter test test/features/api/data/repositories/telegram_link_repository_impl_test.dart -r compact</automated>
  </verify>
  <done>Telegram link operation is auth-lifecycle compatible and returns typed outcomes required by Phase 11 consumers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app repository → VPN API | Untrusted network responses cross into domain mapping logic. |
| auth retry wrapper → protected endpoint | Bearer-auth request may receive unauthorized/transient errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | telegram_link_response.dart parsing | mitigate | Strict response parsing + explicit fallback to `unknown` outcome when payload variant is unrecognized. |
| T-11-02 | I | api_client.dart diagnostics | mitigate | Reuse existing `_sanitizeHeaders/_sanitizeRawBody`; never log raw Authorization token (D-09). |
| T-11-03 | D | telegram_link_repository_impl.dart | mitigate | Keep one-shot auth retry via `executeWithAuthRetry` and preserve transient handling policy (D-04, D-05, D-06). |
</threat_model>

<verification>
- flutter test test/features/api/data/datasources/api_client_test.dart -r compact
- flutter test test/features/api/data/repositories/telegram_link_repository_impl_test.dart -r compact
</verification>

<success_criteria>
- API layer submits Telegram link request with required endpoint/body/auth header.
- Repository returns only the locked outcome set and maps unauthorized-after-retry correctly.
- Existing API diagnostics redaction and retry behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/11-telegram-link-api-integration/11-01-SUMMARY.md`
</output>
