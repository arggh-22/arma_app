---
phase: 10-settings-auto-update-configuration
plan: 05
type: execute
wave: 5
depends_on: [10-03, 10-04]
files_modified:
  - lib/features/settings/presentation/screens/settings_screen.dart
  - lib/core/l10n/app_en.arb
  - lib/core/l10n/app_fa.arb
  - lib/core/l10n/app_hy.arb
  - lib/core/l10n/app_ru.arb
  - lib/core/l10n/app_zh.arb
  - test/features/settings/presentation/screens/settings_screen_auto_update_test.dart
autonomous: true
gap_closure: true
requirements: [DATA-01, DATA-02, COMPAT-02]
must_haves:
  truths:
    - "After an overdue fallback refresh succeeds, Settings shows a subtle non-blocking updated-state indicator."
    - "Indicator is driven by scheduler state (`hasRecentOverdueRefresh`/`lastOverdueRefreshAt`) rather than static UI."
  artifacts:
    - path: "lib/features/settings/presentation/screens/settings_screen.dart"
      provides: "visible subtle updated-state indicator in Arma VPN settings"
    - path: "test/features/settings/presentation/screens/settings_screen_auto_update_test.dart"
      provides: "widget proof that overdue fallback state renders indicator"
  key_links:
    - from: "default_server_refresh_scheduler_provider.dart"
      to: "settings_screen.dart"
      via: "ref.watch(defaultServerRefreshSchedulerProvider)"
      pattern: "hasRecentOverdueRefresh|lastOverdueRefreshAt"
---

<objective>
Close the remaining verification gap by rendering a subtle updated-state indicator in Settings after overdue fallback refresh succeeds.

Purpose: Fulfill D-02 visibility requirement and complete DATA-01/DATA-02 UX loop.
Output: Scheduler-state-driven indicator UI + widget test proving it appears.
</objective>

<context>
@.planning/phases/10-settings-auto-update-configuration/10-VERIFICATION.md
@.planning/phases/10-settings-auto-update-configuration/10-CONTEXT.md
@lib/features/api/presentation/providers/default_server_refresh_scheduler_provider.dart
@lib/features/settings/presentation/screens/settings_screen.dart
@test/features/settings/presentation/screens/settings_screen_auto_update_test.dart

<interfaces>
From `default_server_refresh_scheduler_provider.dart`:
```dart
class DefaultServerRefreshSchedulerState {
  const DefaultServerRefreshSchedulerState({
    this.lastOverdueRefreshAt,
    this.hasRecentOverdueRefresh = false,
  });
  final DateTime? lastOverdueRefreshAt;
  final bool hasRecentOverdueRefresh;
}
final defaultServerRefreshSchedulerProvider =
  NotifierProvider<DefaultServerRefreshSchedulerNotifier,
    DefaultServerRefreshSchedulerState>(...);
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render subtle overdue-refresh updated indicator in Settings (per D-02)</name>
  <files>
    - lib/features/settings/presentation/screens/settings_screen.dart
    - lib/core/l10n/app_en.arb
    - lib/core/l10n/app_fa.arb
    - lib/core/l10n/app_hy.arb
    - lib/core/l10n/app_ru.arb
    - lib/core/l10n/app_zh.arb
  </files>
  <behavior>
    - Indicator is hidden when `hasRecentOverdueRefresh == false`.
    - Indicator is shown in Arma VPN settings when `hasRecentOverdueRefresh == true`.
    - If `lastOverdueRefreshAt` is present, indicator copy includes a compact “updated” timestamp/microcopy.
  </behavior>
  <action>In `SettingsScreen`, watch `defaultServerRefreshSchedulerProvider` and render a subtle, non-blocking indicator directly under the auto-update controls (not snackbar/dialog, per D-02 and D-04). Add localized microcopy keys in all supported ARB files for the indicator label/timestamp phrasing; avoid hardcoded text and avoid adding new setting controls.</action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/settings/presentation/screens/settings_screen_auto_update_test.dart -r compact</automated>
  </verify>
  <done>Settings surface now consumes scheduler overdue-refresh state and shows subtle updated indicator only after successful overdue fallback refresh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add widget test proving indicator appears after overdue fallback success</name>
  <files>
    - test/features/settings/presentation/screens/settings_screen_auto_update_test.dart
  </files>
  <behavior>
    - Given scheduler state with `hasRecentOverdueRefresh: true`, the indicator is visible.
    - Given default scheduler state, indicator is absent.
  </behavior>
  <action>Extend existing settings widget tests by overriding scheduler provider state to simulate post-overdue-refresh success and assert indicator visibility/microcopy. Add paired assertion for default state absence so the indicator is not always-on.</action>
  <verify>
    <automated>flutter test test/features/settings/presentation/screens/settings_screen_auto_update_test.dart -r compact</automated>
  </verify>
  <done>Automated widget coverage reproduces the failed truth from 10-VERIFICATION and proves it is now satisfied.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider state → UI | Internal scheduler state is rendered to user-visible text in Settings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | T | settings_screen.dart indicator binding | mitigate | Render indicator only from typed provider fields (`hasRecentOverdueRefresh`, `lastOverdueRefreshAt`), no ad-hoc mutable flag. |
| T-10-10 | R | overdue refresh UX proof | mitigate | Widget tests assert both visible and hidden states to prevent regressions or misleading always-on status. |
</threat_model>

<verification>
- `flutter gen-l10n`
- `flutter test test/features/settings/presentation/screens/settings_screen_auto_update_test.dart -r compact`
</verification>

<success_criteria>
- Remaining gap truth in `10-VERIFICATION.md` is demonstrably satisfied in Settings UI.
- Indicator appears only when overdue fallback refresh succeeds and remains subtle/non-blocking.
- Automated widget tests cover both indicator-present and indicator-absent states.
</success_criteria>

<output>
After completion, create `.planning/phases/10-settings-auto-update-configuration/10-05-SUMMARY.md`.
</output>
