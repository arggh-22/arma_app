---
phase: 03-subscriptions-server-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - lib/features/server/domain/entities/subscription.dart
  - lib/features/server/data/models/subscription_model.dart
  - lib/features/server/data/datasources/subscription_local_datasource.dart
  - lib/features/server/domain/repositories/subscription_repository.dart
  - lib/features/server/data/repositories/subscription_repository_impl.dart
  - lib/features/server/data/parsers/subscription_userinfo_parser.dart
  - lib/main.dart
  - lib/core/l10n/app_en.arb
  - lib/core/l10n/app_fa.arb
  - lib/core/l10n/app_ru.arb
  - lib/core/l10n/app_zh.arb
autonomous: true
requirements:
  - CONF-02
  - CONF-04
  - CONF-08
  - SERV-08

must_haves:
  truths:
    - "Subscription freezed entity exists with all required fields (id, name, url, userAgent, uploadBytes, downloadBytes, totalBytes, expireDate, lastUpdated, addedAt, autoUpdate)"
    - "SubscriptionModel Hive adapter uses typeId 1 without colliding with ServerConfigModel typeId 0"
    - "All Phase 3 l10n keys exist in all 4 locale ARB files and Dart code compiles"
    - "CAMERA permission declared in AndroidManifest.xml for QR scanning"
  artifacts:
    - path: "lib/features/server/domain/entities/subscription.dart"
      provides: "Subscription freezed entity"
      contains: "@freezed"
    - path: "lib/features/server/data/models/subscription_model.dart"
      provides: "Hive-persisted subscription model"
      contains: "@HiveType(typeId: 1)"
    - path: "lib/features/server/data/datasources/subscription_local_datasource.dart"
      provides: "Hive CRUD for subscriptions"
      contains: "Box<SubscriptionModel>"
    - path: "lib/features/server/data/parsers/subscription_userinfo_parser.dart"
      provides: "subscription-userinfo header parser"
      contains: "parseSubscriptionUserinfo"
  key_links:
    - from: "lib/features/server/data/models/subscription_model.dart"
      to: "lib/features/server/domain/entities/subscription.dart"
      via: "toDomain() and fromDomain() converters"
      pattern: "toDomain|fromDomain"
    - from: "lib/main.dart"
      to: "lib/features/server/data/models/subscription_model.dart"
      via: "Hive adapter registration and box opening"
      pattern: "SubscriptionModelAdapter|openBox.*subscriptions"
---

<objective>
Create the subscription data foundation for Phase 3: Subscription freezed entity, Hive model (typeId: 1), local datasource, repository, subscription-userinfo header parser, all new pub/Gradle dependencies, CAMERA permission, and ALL Phase 3 l10n keys across 4 locales.

Purpose: Every subsequent plan depends on this foundation — subscription model for storage, l10n keys for UI text, dependencies for QR/HTTP/YAML/sharing functionality.
Output: Compilable project with new dependencies, Subscription data layer, and complete l10n coverage for Phase 3.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscriptions-server-intelligence/03-CONTEXT.md
@.planning/phases/03-subscriptions-server-intelligence/03-RESEARCH.md
@.planning/phases/03-subscriptions-server-intelligence/03-UI-SPEC.md

<interfaces>
<!-- Existing patterns to follow exactly -->

From lib/features/server/domain/entities/server_config.dart:
```dart
@freezed
abstract class ServerConfig with _$ServerConfig {
  const factory ServerConfig({
    required String id,
    required String name,
    // ...fields with @Default annotations...
    String? subscriptionId,
    @Default('Manual') String groupName,
    required DateTime addedAt,
  }) = _ServerConfig;
}
```

From lib/features/server/data/models/server_config_model.dart:
```dart
@HiveType(typeId: 0)
class ServerConfigModel extends HiveObject {
  @HiveField(0) final String id;
  // ...fields with intentional gaps for future extension...
}
```

From lib/features/server/data/datasources/server_local_datasource.dart:
```dart
class ServerLocalDatasource {
  final Box<ServerConfigModel> _box;
  ServerLocalDatasource(this._box);
  List<ServerConfigModel> getAll() => _box.values.toList();
  ServerConfigModel? getById(String id) { ... }
  Future<void> save(ServerConfigModel model) async { ... }
  Future<void> delete(String id) async { ... }
}
```

From lib/main.dart (Hive init pattern):
```dart
await Hive.initFlutter();
Hive.registerAdapter(ServerConfigModelAdapter());
await Hive.openBox<ServerConfigModel>('configs');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies + Subscription entity, model, datasource, repository</name>
  <files>
    pubspec.yaml,
    android/app/build.gradle.kts,
    android/app/src/main/AndroidManifest.xml,
    lib/features/server/domain/entities/subscription.dart,
    lib/features/server/data/models/subscription_model.dart,
    lib/features/server/data/datasources/subscription_local_datasource.dart,
    lib/features/server/domain/repositories/subscription_repository.dart,
    lib/features/server/data/repositories/subscription_repository_impl.dart,
    lib/features/server/data/parsers/subscription_userinfo_parser.dart,
    lib/main.dart
  </files>
  <read_first>
    pubspec.yaml,
    android/app/build.gradle.kts,
    android/app/src/main/AndroidManifest.xml,
    lib/main.dart,
    lib/features/server/domain/entities/server_config.dart,
    lib/features/server/data/models/server_config_model.dart,
    lib/features/server/data/datasources/server_local_datasource.dart,
    lib/features/server/domain/repositories/server_repository.dart,
    lib/features/server/data/repositories/server_repository_impl.dart,
    lib/hive_registrar.g.dart
  </read_first>
  <action>
**1. Add pub dependencies** to pubspec.yaml under `dependencies:`:
```yaml
mobile_scanner: ^7.2.0
qr_flutter: ^4.1.0
http: ^1.6.0
yaml: ^3.1.3
share_plus: ^12.0.2
path_provider: ^2.1.5
```
Run `flutter pub get`.

**2. Add kotlinx-coroutines** to `android/app/build.gradle.kts` in the `dependencies` block:
```kotlin
implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.9.0")
```

**3. Add CAMERA permission** to `android/app/src/main/AndroidManifest.xml` (per D-06, CONF-02). Add before the `<application>` tag:
```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.camera" android:required="false" />
```
The `required="false"` ensures the app installs on devices without cameras.

**4. Create Subscription freezed entity** at `lib/features/server/domain/entities/subscription.dart`:
```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'subscription.freezed.dart';
part 'subscription.g.dart';

@freezed
abstract class Subscription with _$Subscription {
  const factory Subscription({
    required String id,
    required String name,
    required String url,
    @Default('') String userAgent,  // CONF-08: custom UA per subscription
    int? uploadBytes,       // from subscription-userinfo header
    int? downloadBytes,     // from subscription-userinfo header
    int? totalBytes,        // from subscription-userinfo header
    DateTime? expireDate,   // from subscription-userinfo header
    required DateTime lastUpdated,
    required DateTime addedAt,
    @Default(true) bool autoUpdate,  // CONF-07: auto-refresh toggle
  }) = _Subscription;

  factory Subscription.fromJson(Map<String, dynamic> json) =>
      _$SubscriptionFromJson(json);
}
```

**5. Create SubscriptionModel** at `lib/features/server/data/models/subscription_model.dart` following ServerConfigModel pattern with typeId: 1 and intentional field gaps:
```dart
import 'package:hive_ce/hive_ce.dart';
import 'package:arma_proxy_vpn_client/features/server/domain/entities/subscription.dart';

part 'subscription_model.g.dart';

@HiveType(typeId: 1)
class SubscriptionModel extends HiveObject {
  @HiveField(0)  final String id;
  @HiveField(1)  final String name;
  @HiveField(2)  final String url;
  @HiveField(3)  final String userAgent;
  // GAP: 4-5 reserved for future fields
  @HiveField(6)  final int? uploadBytes;
  @HiveField(7)  final int? downloadBytes;
  @HiveField(8)  final int? totalBytes;
  @HiveField(9)  final int? expireMillis;
  // GAP: 10-14 reserved
  @HiveField(15) final int lastUpdatedMillis;
  @HiveField(16) final int addedAtMillis;
  @HiveField(17) final bool autoUpdate;

  SubscriptionModel({
    required this.id,
    required this.name,
    required this.url,
    this.userAgent = '',
    this.uploadBytes,
    this.downloadBytes,
    this.totalBytes,
    this.expireMillis,
    required this.lastUpdatedMillis,
    required this.addedAtMillis,
    this.autoUpdate = true,
  });

  Subscription toDomain() => Subscription(
    id: id,
    name: name,
    url: url,
    userAgent: userAgent,
    uploadBytes: uploadBytes,
    downloadBytes: downloadBytes,
    totalBytes: totalBytes,
    expireDate: expireMillis != null
        ? DateTime.fromMillisecondsSinceEpoch(expireMillis!)
        : null,
    lastUpdated: DateTime.fromMillisecondsSinceEpoch(lastUpdatedMillis),
    addedAt: DateTime.fromMillisecondsSinceEpoch(addedAtMillis),
    autoUpdate: autoUpdate,
  );

  factory SubscriptionModel.fromDomain(Subscription s) => SubscriptionModel(
    id: s.id,
    name: s.name,
    url: s.url,
    userAgent: s.userAgent,
    uploadBytes: s.uploadBytes,
    downloadBytes: s.downloadBytes,
    totalBytes: s.totalBytes,
    expireMillis: s.expireDate?.millisecondsSinceEpoch,
    lastUpdatedMillis: s.lastUpdated.millisecondsSinceEpoch,
    addedAtMillis: s.addedAt.millisecondsSinceEpoch,
    autoUpdate: s.autoUpdate,
  );
}
```

**6. Create SubscriptionLocalDatasource** at `lib/features/server/data/datasources/subscription_local_datasource.dart` — follow ServerLocalDatasource pattern exactly:
- Constructor takes `Box<SubscriptionModel>`
- `getAll()`, `getById(String id)`, `save(SubscriptionModel)`, `delete(String id)`, `exists(String id)` methods
- Wrap write operations in try-catch throwing `StorageException`
- Add `deleteBySubscriptionId(String subscriptionId)` — not needed here, but add `Future<void> saveAll(List<SubscriptionModel> models)` for batch saves

**7. Create SubscriptionRepository interface** at `lib/features/server/domain/repositories/subscription_repository.dart`:
```dart
import 'package:arma_proxy_vpn_client/features/server/domain/entities/subscription.dart';

abstract class SubscriptionRepository {
  List<Subscription> getAllSubscriptions();
  Subscription? getSubscriptionById(String id);
  Future<void> saveSubscription(Subscription subscription);
  Future<void> deleteSubscription(String id);
  Future<void> updateSubscriptionInfo(String id, {int? uploadBytes, int? downloadBytes, int? totalBytes, DateTime? expireDate});
}
```

**8. Create SubscriptionRepositoryImpl** at `lib/features/server/data/repositories/subscription_repository_impl.dart` following ServerRepositoryImpl pattern — converts between domain entities and Hive models.

**9. Create subscription-userinfo header parser** at `lib/features/server/data/parsers/subscription_userinfo_parser.dart` (per D-03, SERV-08):
```dart
/// Parses the subscription-userinfo HTTP response header.
/// Format: upload=1234; download=5678; total=10000; expire=1700000000
/// Returns null if header is absent or empty.
class SubscriptionUserinfo {
  final int? uploadBytes;
  final int? downloadBytes;
  final int? totalBytes;
  final DateTime? expireDate;

  const SubscriptionUserinfo({this.uploadBytes, this.downloadBytes, this.totalBytes, this.expireDate});
}

SubscriptionUserinfo? parseSubscriptionUserinfo(String? header) {
  if (header == null || header.isEmpty) return null;
  int? upload, download, total;
  DateTime? expire;
  for (final part in header.split(';').map((p) => p.trim())) {
    final kv = part.split('=');
    if (kv.length != 2) continue;
    final key = kv[0].trim().toLowerCase();
    final value = kv[1].trim();
    switch (key) {
      case 'upload':   upload = int.tryParse(value);
      case 'download': download = int.tryParse(value);
      case 'total':    total = int.tryParse(value);
      case 'expire':
        final ts = int.tryParse(value);
        if (ts != null) expire = DateTime.fromMillisecondsSinceEpoch(ts * 1000);
    }
  }
  return SubscriptionUserinfo(uploadBytes: upload, downloadBytes: download, totalBytes: total, expireDate: expire);
}
```

**10. Register Hive adapter in main.dart** — add after existing `ServerConfigModelAdapter()` registration:
```dart
import 'package:arma_proxy_vpn_client/features/server/data/models/subscription_model.dart';
// ...
Hive.registerAdapter(SubscriptionModelAdapter());
await Hive.openBox<SubscriptionModel>('subscriptions');
```

**11. Run build_runner** to generate freezed + Hive adapter code:
```bash
dart run build_runner build --delete-conflicting-outputs
```
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter pub get && dart run build_runner build --delete-conflicting-outputs && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `mobile_scanner: ^7.2.0`
    - pubspec.yaml contains `qr_flutter: ^4.1.0`
    - pubspec.yaml contains `http: ^1.6.0`
    - pubspec.yaml contains `yaml: ^3.1.3`
    - pubspec.yaml contains `share_plus: ^12.0.2`
    - pubspec.yaml contains `path_provider: ^2.1.5`
    - android/app/build.gradle.kts contains `kotlinx-coroutines-android`
    - AndroidManifest.xml contains `android.permission.CAMERA`
    - AndroidManifest.xml contains `android.hardware.camera`
    - subscription.dart contains `@freezed` and `abstract class Subscription`
    - subscription.dart contains `required String url`
    - subscription.dart contains `@Default('') String userAgent`
    - subscription.dart contains `@Default(true) bool autoUpdate`
    - subscription_model.dart contains `@HiveType(typeId: 1)`
    - subscription_model.dart contains `toDomain()`
    - subscription_model.dart contains `fromDomain`
    - subscription_local_datasource.dart contains `Box<SubscriptionModel>`
    - subscription_repository.dart contains `abstract class SubscriptionRepository`
    - subscription_repository_impl.dart contains `class SubscriptionRepositoryImpl`
    - subscription_userinfo_parser.dart contains `parseSubscriptionUserinfo`
    - main.dart contains `SubscriptionModelAdapter()`
    - main.dart contains `openBox<SubscriptionModel>`
    - `flutter analyze --no-fatal-infos` exits with 0 errors
  </acceptance_criteria>
  <done>All Phase 3 dependencies added, Subscription data layer compiles with Hive typeId 1, subscription-userinfo parser implemented, CAMERA permission in manifest, Hive adapter registered and box opened in main.dart</done>
</task>

<task type="auto">
  <name>Task 2: Add all Phase 3 l10n keys to 4 locale ARB files</name>
  <files>
    lib/core/l10n/app_en.arb,
    lib/core/l10n/app_fa.arb,
    lib/core/l10n/app_ru.arb,
    lib/core/l10n/app_zh.arb
  </files>
  <read_first>
    lib/core/l10n/app_en.arb,
    lib/core/l10n/app_fa.arb,
    lib/core/l10n/app_ru.arb,
    lib/core/l10n/app_zh.arb,
    l10n.yaml,
    .planning/phases/03-subscriptions-server-intelligence/03-UI-SPEC.md
  </read_first>
  <action>
Add ALL Phase 3 l10n keys to each ARB file. The keys are from the UI-SPEC copywriting contract. After adding, run `flutter gen-l10n` to regenerate Dart files.

**English (app_en.arb) — add these entries before the closing `}`:**
```json
"addSubscription": "Add Subscription",
"noLogsYet": "No logs yet",
"noLogsBody": "Connect to a server to see logs",
"subscriptionFetchError": "Failed to fetch subscription. Check the URL and try again.",
"latencyTestFailed": "Couldn't reach server",
"qrUnrecognized": "Couldn't recognize this QR code",
"cameraPermissionDenied": "Camera permission required to scan QR codes. Enable in Settings.",
"deleteServersTitle": "Delete {count} servers?",
"@deleteServersTitle": { "placeholders": { "count": { "type": "int" } } },
"deleteServersBody": "This will permanently remove the selected servers. Servers from subscriptions will reappear on next refresh.",
"deleteServersConfirm": "Delete {count}",
"@deleteServersConfirm": { "placeholders": { "count": { "type": "int" } } },
"keepServers": "Keep Servers",
"subscriptionRefreshSuccess": "Updated {count} subscriptions",
"@subscriptionRefreshSuccess": { "placeholders": { "count": { "type": "int" } } },
"subscriptionRefreshNoChange": "All up to date",
"subscriptionRefreshFail": "Failed to update {name}. Check your connection.",
"@subscriptionRefreshFail": { "placeholders": { "name": { "type": "String" } } },
"qrSubscriptionPrompt": "This looks like a subscription URL. Add as subscription?",
"notNow": "Not Now",
"dismissDialog": "Dismiss",
"sortBy": "Sort",
"sortByName": "Name",
"sortByLatency": "Latency",
"sortByProtocol": "Protocol",
"filterAll": "All",
"filterWorking": "Working",
"filterFailed": "Failed",
"testAllServers": "Test All",
"bestServer": "Best Server",
"selectedCount": "{count} selected",
"@selectedCount": { "placeholders": { "count": { "type": "int" } } },
"selectAll": "Select All",
"scanQrTitle": "Scan QR Code",
"scanQrInstruction": "Point camera at a QR code",
"shareServer": "Share Server",
"copyLink": "Copy Link",
"linkCopied": "Link copied",
"shareLink": "Share Link",
"exportLogs": "Export Logs",
"viewLogs": "View Logs",
"autoScroll": "Auto-scroll",
"linesCount": "{count} lines",
"@linesCount": { "placeholders": { "count": { "type": "int" } } },
"autoUpdateOnLaunch": "Auto-update on app launch",
"userAgentHint": "Leave empty for default browser UA",
"subscriptionUrl": "URL",
"subscriptionName": "Name",
"subscriptionInfoFormat": "{count} servers",
"@subscriptionInfoFormat": { "placeholders": { "count": { "type": "int" } } },
"importedServersCount": "Imported {count} servers",
"@importedServersCount": { "placeholders": { "count": { "type": "int" } } },
"logFilterAll": "All",
"logFilterInfo": "Info",
"logFilterWarning": "Warning",
"logFilterError": "Error",
"diagnosticsSection": "Diagnostics"
```

**Persian (app_fa.arb) — add corresponding keys with Persian translations:**
```json
"addSubscription": "افزودن اشتراک",
"noLogsYet": "هنوز لاگی نیست",
"noLogsBody": "برای مشاهده لاگ‌ها به سرور متصل شوید",
"subscriptionFetchError": "دریافت اشتراک ناموفق بود. آدرس را بررسی کنید.",
"latencyTestFailed": "سرور در دسترس نیست",
"qrUnrecognized": "محتوای QR قابل شناسایی نیست",
"cameraPermissionDenied": "برای اسکن QR به دسترسی دوربین نیاز است. در تنظیمات فعال کنید.",
"deleteServersTitle": "حذف {count} سرور؟",
"@deleteServersTitle": { "placeholders": { "count": { "type": "int" } } },
"deleteServersBody": "سرورهای انتخاب شده حذف خواهند شد. سرورهای اشتراکی در بروزرسانی بعدی بازمی‌گردند.",
"deleteServersConfirm": "حذف {count}",
"@deleteServersConfirm": { "placeholders": { "count": { "type": "int" } } },
"keepServers": "نگه داشتن",
"subscriptionRefreshSuccess": "{count} اشتراک بروزرسانی شد",
"@subscriptionRefreshSuccess": { "placeholders": { "count": { "type": "int" } } },
"subscriptionRefreshNoChange": "همه بروز هستند",
"subscriptionRefreshFail": "بروزرسانی {name} ناموفق بود.",
"@subscriptionRefreshFail": { "placeholders": { "name": { "type": "String" } } },
"qrSubscriptionPrompt": "این یک آدرس اشتراک به نظر می‌رسد. افزودن به عنوان اشتراک؟",
"notNow": "الان نه",
"dismissDialog": "بستن",
"sortBy": "مرتب‌سازی",
"sortByName": "نام",
"sortByLatency": "تأخیر",
"sortByProtocol": "پروتکل",
"filterAll": "همه",
"filterWorking": "فعال",
"filterFailed": "ناموفق",
"testAllServers": "تست همه",
"bestServer": "بهترین سرور",
"selectedCount": "{count} انتخاب شده",
"@selectedCount": { "placeholders": { "count": { "type": "int" } } },
"selectAll": "انتخاب همه",
"scanQrTitle": "اسکن QR",
"scanQrInstruction": "دوربین را به سمت QR بگیرید",
"shareServer": "اشتراک‌گذاری سرور",
"copyLink": "کپی لینک",
"linkCopied": "لینک کپی شد",
"shareLink": "اشتراک‌گذاری لینک",
"exportLogs": "خروجی لاگ‌ها",
"viewLogs": "مشاهده لاگ‌ها",
"autoScroll": "اسکرول خودکار",
"linesCount": "{count} خط",
"@linesCount": { "placeholders": { "count": { "type": "int" } } },
"autoUpdateOnLaunch": "بروزرسانی خودکار هنگام اجرا",
"userAgentHint": "خالی بگذارید برای UA پیش‌فرض",
"subscriptionUrl": "آدرس",
"subscriptionName": "نام",
"subscriptionInfoFormat": "{count} سرور",
"@subscriptionInfoFormat": { "placeholders": { "count": { "type": "int" } } },
"importedServersCount": "{count} سرور وارد شد",
"@importedServersCount": { "placeholders": { "count": { "type": "int" } } },
"logFilterAll": "همه",
"logFilterInfo": "اطلاعات",
"logFilterWarning": "هشدار",
"logFilterError": "خطا",
"diagnosticsSection": "عیب‌یابی"
```

**Russian (app_ru.arb) — add corresponding keys with Russian translations:**
```json
"addSubscription": "Добавить подписку",
"noLogsYet": "Логов пока нет",
"noLogsBody": "Подключитесь к серверу для просмотра логов",
"subscriptionFetchError": "Не удалось получить подписку. Проверьте URL.",
"latencyTestFailed": "Сервер недоступен",
"qrUnrecognized": "Не удалось распознать QR-код",
"cameraPermissionDenied": "Для сканирования QR требуется доступ к камере. Включите в настройках.",
"deleteServersTitle": "Удалить {count} серверов?",
"@deleteServersTitle": { "placeholders": { "count": { "type": "int" } } },
"deleteServersBody": "Выбранные серверы будут удалены. Серверы из подписок вернутся при обновлении.",
"deleteServersConfirm": "Удалить {count}",
"@deleteServersConfirm": { "placeholders": { "count": { "type": "int" } } },
"keepServers": "Оставить",
"subscriptionRefreshSuccess": "Обновлено {count} подписок",
"@subscriptionRefreshSuccess": { "placeholders": { "count": { "type": "int" } } },
"subscriptionRefreshNoChange": "Всё актуально",
"subscriptionRefreshFail": "Не удалось обновить {name}.",
"@subscriptionRefreshFail": { "placeholders": { "name": { "type": "String" } } },
"qrSubscriptionPrompt": "Похоже на URL подписки. Добавить как подписку?",
"notNow": "Не сейчас",
"dismissDialog": "Отмена",
"sortBy": "Сортировка",
"sortByName": "По имени",
"sortByLatency": "По задержке",
"sortByProtocol": "По протоколу",
"filterAll": "Все",
"filterWorking": "Рабочие",
"filterFailed": "Нерабочие",
"testAllServers": "Тест всех",
"bestServer": "Лучший сервер",
"selectedCount": "{count} выбрано",
"@selectedCount": { "placeholders": { "count": { "type": "int" } } },
"selectAll": "Выбрать все",
"scanQrTitle": "Сканировать QR",
"scanQrInstruction": "Наведите камеру на QR-код",
"shareServer": "Поделиться сервером",
"copyLink": "Копировать ссылку",
"linkCopied": "Ссылка скопирована",
"shareLink": "Поделиться",
"exportLogs": "Экспорт логов",
"viewLogs": "Просмотр логов",
"autoScroll": "Автопрокрутка",
"linesCount": "{count} строк",
"@linesCount": { "placeholders": { "count": { "type": "int" } } },
"autoUpdateOnLaunch": "Обновлять при запуске",
"userAgentHint": "Оставьте пустым для стандартного UA",
"subscriptionUrl": "URL",
"subscriptionName": "Название",
"subscriptionInfoFormat": "{count} серверов",
"@subscriptionInfoFormat": { "placeholders": { "count": { "type": "int" } } },
"importedServersCount": "Импортировано {count} серверов",
"@importedServersCount": { "placeholders": { "count": { "type": "int" } } },
"logFilterAll": "Все",
"logFilterInfo": "Инфо",
"logFilterWarning": "Предупр.",
"logFilterError": "Ошибки",
"diagnosticsSection": "Диагностика"
```

**Chinese (app_zh.arb) — add corresponding keys with Chinese translations:**
```json
"addSubscription": "添加订阅",
"noLogsYet": "暂无日志",
"noLogsBody": "连接服务器后查看日志",
"subscriptionFetchError": "获取订阅失败，请检查URL",
"latencyTestFailed": "无法连接服务器",
"qrUnrecognized": "无法识别此二维码",
"cameraPermissionDenied": "扫描二维码需要相机权限，请在设置中启用",
"deleteServersTitle": "删除 {count} 个服务器？",
"@deleteServersTitle": { "placeholders": { "count": { "type": "int" } } },
"deleteServersBody": "选中的服务器将被永久删除。订阅服务器会在下次刷新时恢复。",
"deleteServersConfirm": "删除 {count} 个",
"@deleteServersConfirm": { "placeholders": { "count": { "type": "int" } } },
"keepServers": "保留",
"subscriptionRefreshSuccess": "已更新 {count} 个订阅",
"@subscriptionRefreshSuccess": { "placeholders": { "count": { "type": "int" } } },
"subscriptionRefreshNoChange": "全部已是最新",
"subscriptionRefreshFail": "更新 {name} 失败",
"@subscriptionRefreshFail": { "placeholders": { "name": { "type": "String" } } },
"qrSubscriptionPrompt": "这看起来是订阅链接。是否添加为订阅？",
"notNow": "暂不",
"dismissDialog": "取消",
"sortBy": "排序",
"sortByName": "名称",
"sortByLatency": "延迟",
"sortByProtocol": "协议",
"filterAll": "全部",
"filterWorking": "可用",
"filterFailed": "不可用",
"testAllServers": "全部测试",
"bestServer": "最佳服务器",
"selectedCount": "已选 {count} 个",
"@selectedCount": { "placeholders": { "count": { "type": "int" } } },
"selectAll": "全选",
"scanQrTitle": "扫描二维码",
"scanQrInstruction": "将摄像头对准二维码",
"shareServer": "分享服务器",
"copyLink": "复制链接",
"linkCopied": "链接已复制",
"shareLink": "分享链接",
"exportLogs": "导出日志",
"viewLogs": "查看日志",
"autoScroll": "自动滚动",
"linesCount": "{count} 行",
"@linesCount": { "placeholders": { "count": { "type": "int" } } },
"autoUpdateOnLaunch": "启动时自动更新",
"userAgentHint": "留空使用默认浏览器UA",
"subscriptionUrl": "URL",
"subscriptionName": "名称",
"subscriptionInfoFormat": "{count} 个服务器",
"@subscriptionInfoFormat": { "placeholders": { "count": { "type": "int" } } },
"importedServersCount": "已导入 {count} 个服务器",
"@importedServersCount": { "placeholders": { "count": { "type": "int" } } },
"logFilterAll": "全部",
"logFilterInfo": "信息",
"logFilterWarning": "警告",
"logFilterError": "错误",
"diagnosticsSection": "诊断"
```

After adding all keys, run `flutter gen-l10n` to regenerate the Dart localization files.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter gen-l10n && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app_en.arb contains "addSubscription"
    - app_en.arb contains "deleteServersTitle"
    - app_en.arb contains "scanQrTitle"
    - app_en.arb contains "exportLogs"
    - app_en.arb contains "viewLogs"
    - app_en.arb contains "bestServer"
    - app_en.arb contains "subscriptionInfoFormat"
    - app_en.arb contains "logFilterError"
    - app_en.arb contains "diagnosticsSection"
    - app_fa.arb contains "addSubscription"
    - app_ru.arb contains "addSubscription"
    - app_zh.arb contains "addSubscription"
    - `flutter gen-l10n` completes without errors
    - `flutter analyze --no-fatal-infos` shows no errors
  </acceptance_criteria>
  <done>All ~46 l10n keys added to all 4 locale ARB files with proper placeholder metadata, Dart localization files regenerated, project compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTTP → App | Subscription URLs fetched from untrusted remote servers |
| Camera Input → App | QR codes from untrusted physical sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | subscription_repository_impl.dart | mitigate | Validate each parsed ServerConfig via existing ServerRepositoryImpl._validateAndConvert before persisting |
| T-03-02 | Denial of Service | subscription_service.dart (Plan 05) | mitigate | Set HTTP response timeout to 15s and body size check (~5MB max) |
| T-03-03 | Information Disclosure | subscription_model.dart | accept | Subscription URLs stored locally in Hive — device-level encryption is user's responsibility |
</threat_model>

<verification>
- `flutter pub get` succeeds with all new dependencies resolved
- `dart run build_runner build --delete-conflicting-outputs` generates all .g.dart and .freezed.dart files
- `flutter analyze --no-fatal-infos` shows 0 errors
- `flutter gen-l10n` regenerates localization Dart files
- Hive typeId 1 does not collide (ServerConfigModel is typeId 0)
</verification>

<success_criteria>
- All 6 new pub dependencies in pubspec.yaml and resolved in pubspec.lock
- kotlinx-coroutines-android in Gradle dependencies
- CAMERA permission + camera feature in AndroidManifest.xml
- Subscription entity/model/datasource/repository compiles and follows existing ServerConfig pattern
- subscription-userinfo header parser handles the standard format
- Hive adapter registered and subscriptions box opened in main.dart
- 46+ l10n keys in all 4 ARB files with regenerated Dart files
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscriptions-server-intelligence/03-01-SUMMARY.md`
</output>
