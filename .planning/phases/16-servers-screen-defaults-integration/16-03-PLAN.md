---
phase: 16-servers-screen-defaults-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - lib/features/dashboard/data/mappers/default_server_item_mapper.dart
  - lib/features/dashboard/presentation/providers/default_servers_provider.dart
  - lib/features/server/presentation/widgets/server_list_default_servers_section.dart
  - test/features/dashboard/presentation/providers/default_servers_provider_test.dart
  - test/features/server/presentation/screens/server_list_screen_defaults_test.dart
  - test/features/server/presentation/screens/server_list_screen_regression_test.dart
autonomous: true
gap_closure: true
requirements:
  - SRVD-01
  - SRVD-02
  - SRVD-03
  - SRVD-04
must_haves:
  truths:
    - "Each default key is resolved via its `subscription_url`, and servers are rendered under that key as a collapsible subgroup."
    - "Default section and each default subgroup start expanded when Servers screen opens, and can be collapsed/expanded."
    - "Default-row tap keeps existing select + conditional reconnect parity."
    - "Imported/custom group collapse/select behavior remains unchanged."
  artifacts:
    - path: "lib/features/dashboard/presentation/providers/default_servers_provider.dart"
      provides: "Per-key subscription_url resolution pipeline and grouped default row materialization"
    - path: "lib/features/server/presentation/widgets/server_list_default_servers_section.dart"
      provides: "Nested collapsible default subgroups with compact rows"
    - path: "test/features/dashboard/presentation/providers/default_servers_provider_test.dart"
      provides: "Regression proof that provider uses subscription_url results (not flat keyBody-only mapping)"
    - path: "test/features/server/presentation/screens/server_list_screen_defaults_test.dart"
      provides: "Widget coverage for subgroup rendering and collapse/expand behavior"
    - path: "test/features/server/presentation/screens/server_list_screen_regression_test.dart"
      provides: "Regression protection for imported groups while defaults groups are present"
  key_links:
    - from: "default_servers_provider.dart"
      to: "subscription_service.dart"
      via: "per-key fetch using key.subscriptionUrl"
      pattern: "fetch\\(Subscription\\(.*url: key.subscriptionUrl"
    - from: "default_servers_provider.dart"
      to: "default_server_item_mapper.dart"
      via: "map resolved subscription servers into DefaultServerItem rows"
      pattern: "mapResolved|mapAll"
    - from: "server_list_default_servers_section.dart"
      to: "defaultServersProvider"
      via: "group-by-subscriptionUrl collapsible render"
      pattern: "groupBy.*subscriptionUrl|collapsed"
---

<objective>
Close remaining Phase 16 UAT gap by making default keys behave like imported subscription groups: resolve each `key.subscription_url`, render grouped server rows, and keep collapsible behavior parity.

Purpose: Match user-reported expected behavior and eliminate flat default-row rendering caused by keyBody-only mapping.
Output: Provider resolution pipeline, grouped defaults UI, and regression tests proving closure.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-servers-screen-defaults-integration/16-UAT.md
@.planning/phases/16-servers-screen-defaults-integration/16-CONTEXT.md
@.planning/phases/16-servers-screen-defaults-integration/16-01-SUMMARY.md
@.planning/phases/16-servers-screen-defaults-integration/16-02-SUMMARY.md
@lib/features/dashboard/presentation/providers/default_servers_provider.dart
@lib/features/server/data/services/subscription_service.dart
@lib/features/server/presentation/widgets/server_list_default_servers_section.dart

<interfaces>
From `subscription_service.dart`:
```dart
Future<SubscriptionFetchResult> fetch(Subscription subscription)
```

From `default_servers_provider.dart`:
```dart
class DefaultServersState {
  final List<DefaultServerItem> items;
}
```

From `server_list_default_servers_section.dart`:
```dart
Future<void> _onTapDefaultServer(DefaultServerItem item)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve each default key through subscription_url in provider mapping pipeline</name>
  <files>lib/features/dashboard/presentation/providers/default_servers_provider.dart, lib/features/dashboard/data/mappers/default_server_item_mapper.dart, test/features/dashboard/presentation/providers/default_servers_provider_test.dart</files>
  <behavior>
    - For every fetched key, provider resolves `key.subscriptionUrl` and maps returned servers into connectable default rows (UAT gap root cause).
    - Mapping no longer relies on flat keyBody parsing as primary source; keyBody fallback is used only when subscription fetch fails for that key.
    - Row IDs remain deterministic and preserve D-07 tap parity compatibility.
  </behavior>
  <action>In `DefaultServersNotifier`, replace sync `_mapItems(keys)` with async per-key resolution path using `SubscriptionService.fetch` and a synthetic `Subscription` model built from each key (`id` deterministic from key id, `name` from key.name, `url` from key.subscriptionUrl). Map fetched servers to `DefaultServerItem` rows grouped under that key; preserve key metadata (`status`, traffic, expiry, active). Keep safe fallback via mapper for fetch failures, but do not use keyBody-first logic. Add/adjust provider tests to assert per-key subscription_url fetch behavior and multi-server expansion from fetched subscription response (per D-01 and D-03 requirements context).</action>
  <verify>
    <automated>flutter test test/features/dashboard/presentation/providers/default_servers_provider_test.dart</automated>
  </verify>
  <done>Provider emits rows sourced from each key.subscription_url and tests prove subscription_url-driven expansion behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render default servers as collapsible per-key grouped lists in Servers screen section</name>
  <files>lib/features/server/presentation/widgets/server_list_default_servers_section.dart, test/features/server/presentation/screens/server_list_screen_defaults_test.dart</files>
  <behavior>
    - Default section shows grouped rows per key/subscription URL with independent subgroup collapse/expand controls.
    - Section and all subgroups start expanded on screen open (D-04), while defaults remain hidden in multi-select mode (D-02).
    - Group rendering uses compact row style and keeps selected-state visibility (D-05, D-06).
  </behavior>
  <action>Refactor `ServerListDefaultServersSection` to group `defaultServersState.items` by source subscription/key and render subgroup headers with counts plus collapse toggles (parity with imported group behavior), while preserving existing top-level defaults section toggle. Maintain current row tap parity callback path unchanged. Add widget tests that assert grouped rendering under default section, subgroup collapse/expand behavior, and expanded-on-open behavior after screen rebuild.</action>
  <verify>
    <automated>flutter test test/features/server/presentation/screens/server_list_screen_defaults_test.dart</automated>
  </verify>
  <done>Servers screen defaults render as collapsible grouped lists per key subscription, with expanded-by-default behavior and compact selected rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression safety for imported groups and gap-closure verification run</name>
  <files>test/features/server/presentation/screens/server_list_screen_regression_test.dart, test/features/server/presentation/screens/server_list_screen_defaults_test.dart</files>
  <behavior>
    - Imported/custom groups still collapse/expand and multi-select behavior remains unchanged (SRVD-04).
    - Defaults grouped behavior does not break existing default-row tap parity expectations (SRVD-03).
  </behavior>
  <action>Extend regression tests to cover imported-group behavior while grouped defaults are visible; keep prior assertions for multi-select hidden defaults and imported interactions. Execute focused test suite plus analyzer on modified production files to confirm closure stability.</action>
  <verify>
    <automated>flutter test test/features/server/presentation/screens/server_list_screen_defaults_test.dart && flutter test test/features/server/presentation/screens/server_list_screen_regression_test.dart && flutter test test/features/dashboard/presentation/providers/default_servers_provider_test.dart && flutter analyze lib/features/dashboard/presentation/providers/default_servers_provider.dart lib/features/server/presentation/widgets/server_list_default_servers_section.dart lib/features/dashboard/data/mappers/default_server_item_mapper.dart</automated>
  </verify>
  <done>Gap closure is validated by passing grouped-default, provider, and imported-regression checks with no analyzer regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Default keys API data → subscription URL fetcher | Untrusted remote URLs and payloads are used to build connectable server rows |
| Defaults UI tap → connection state transitions | User interaction can trigger disconnect/connect actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | T | `default_servers_provider.dart` URL resolution | mitigate | Parse server payloads only through existing `SubscriptionService` + parser pipeline; skip malformed results and avoid direct string trust. |
| T-16-10 | D | per-key fetch fan-out | mitigate | Bound requests to current key list per refresh and reuse existing timeout/size limits from `SubscriptionService`. |
| T-16-11 | S | grouped row identity | mitigate | Keep deterministic IDs scoped to key + row index so selection/connect targets canonical rows. |
| T-16-12 | I | subscription_url rendering in UI | accept | Do not display raw URL in rows; show only group/title labels and existing safe metadata. |
</threat_model>

<verification>
- `flutter test test/features/dashboard/presentation/providers/default_servers_provider_test.dart`
- `flutter test test/features/server/presentation/screens/server_list_screen_defaults_test.dart`
- `flutter test test/features/server/presentation/screens/server_list_screen_regression_test.dart`
- `flutter analyze lib/features/dashboard/presentation/providers/default_servers_provider.dart lib/features/dashboard/data/mappers/default_server_item_mapper.dart lib/features/server/presentation/widgets/server_list_default_servers_section.dart`
</verification>

<success_criteria>
- Remaining UAT issue is closed: defaults use each key.subscription_url and render collapsible grouped server lists.
- Defaults section and default subgroups are expanded on open and support collapse/expand.
- Default tap parity and imported-group behavior remain intact under regression tests.
- Phase 16 closure remains scoped; no deferred ideas are implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/16-servers-screen-defaults-integration/16-03-SUMMARY.md`
</output>
