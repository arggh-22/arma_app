---
phase: 01-foundation-config-import
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/core/l10n/app_en.arb
  - lib/core/l10n/app_fa.arb
  - lib/core/l10n/app_ru.arb
  - lib/core/l10n/app_zh.arb
  - lib/core/error/failures.dart
  - lib/core/error/exceptions.dart
  - lib/features/server/domain/entities/server_config.dart
  - lib/features/server/data/models/server_config_model.dart
  - lib/features/server/data/datasources/server_local_datasource.dart
  - lib/features/server/domain/repositories/server_repository.dart
  - lib/features/server/data/repositories/server_repository_impl.dart
  - lib/features/settings/data/datasources/settings_local_datasource.dart
  - lib/features/settings/presentation/providers/theme_provider.dart
  - lib/features/settings/presentation/providers/locale_provider.dart
  - lib/features/server/presentation/providers/server_list_provider.dart
  - lib/features/server/presentation/providers/active_server_provider.dart
  - lib/main.dart
  - lib/app.dart
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - UI-03
must_haves:
  truths:
    - "Server configs persist in Hive across app restarts"
    - "Theme and language preferences persist via shared_preferences"
    - "App displays localized strings in English, Persian, Russian, and Chinese"
    - "RTL layout activates when Persian locale is selected"
    - "Server list provider exposes reactive list of saved configs"
    - "Active server provider tracks the currently selected server"
  artifacts:
    - path: "lib/features/server/domain/entities/server_config.dart"
      provides: "Freezed ServerConfig entity with all protocol fields"
      contains: "@freezed"
    - path: "lib/features/server/data/models/server_config_model.dart"
      provides: "Hive-annotated model with explicit field indices and gaps"
      contains: "@HiveType"
    - path: "lib/core/l10n/app_en.arb"
      provides: "English localization source strings"
      contains: "noServersYet"
    - path: "lib/features/settings/presentation/providers/theme_provider.dart"
      provides: "Riverpod provider for ThemeMode persistence"
      contains: "@riverpod"
  key_links:
    - from: "lib/app.dart"
      to: "lib/features/settings/presentation/providers/theme_provider.dart"
      via: "ref.watch for themeMode"
      pattern: "themeMode"
    - from: "lib/app.dart"
      to: "AppLocalizations"
      via: "localizationsDelegates"
      pattern: "localizationsDelegates"
    - from: "lib/features/server/data/repositories/server_repository_impl.dart"
      to: "lib/features/server/data/datasources/server_local_datasource.dart"
      via: "datasource injection"
      pattern: "ServerLocalDatasource"
---

<objective>
Build the complete domain model layer, data persistence layer, state management providers, and localization system. After this plan, the app can store/retrieve server configs in Hive, persist user preferences, and display UI text in 4 languages with RTL support for Persian.

Purpose: The data layer and localization are the backbone for all feature screens — server list, settings, and import flows all depend on these models, repositories, and providers.

Output: ServerConfig domain entity, Hive storage with schema-safe indices, all Riverpod providers, ARB files for 4 languages, and AppLocalizations wired into MaterialApp.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-config-import/01-CONTEXT.md
@.planning/phases/01-foundation-config-import/01-RESEARCH.md
@.planning/phases/01-foundation-config-import/01-UI-SPEC.md
@.planning/phases/01-foundation-config-import/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — key types/contracts the executor needs -->

From lib/core/constants/protocol_constants.dart:
```dart
enum ProtocolType { vless, vmess, trojan, shadowsocks, hysteria2 }
// has .label, .scheme, and static fromScheme(String)
```

From lib/core/theme/app_theme.dart:
```dart
class AppTheme {
  static ThemeData light();
  static ThemeData dark();
}
```

From lib/app.dart:
```dart
class ArmaApp extends ConsumerWidget {
  // MaterialApp.router with theme: AppTheme.light(), darkTheme: AppTheme.dark()
  // themeMode: ThemeMode.system — to be replaced by provider
  // No l10n wiring yet — will be added in this plan
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create localization ARB files and error classes</name>
  <files>lib/core/l10n/app_en.arb, lib/core/l10n/app_fa.arb, lib/core/l10n/app_ru.arb, lib/core/l10n/app_zh.arb, lib/core/error/failures.dart, lib/core/error/exceptions.dart</files>
  <read_first>.planning/phases/01-foundation-config-import/01-UI-SPEC.md, l10n.yaml</read_first>
  <action>
1. **Create `lib/core/l10n/app_en.arb`** (English — template ARB file) with all UI-SPEC copywriting strings as ARB keys:
   ```json
   {
     "@@locale": "en",
     "appName": "Arma VPN",
     "dashboard": "Dashboard",
     "servers": "Servers",
     "routing": "Routing",
     "settings": "Settings",
     "importServer": "Import Server",
     "noServersYet": "No servers yet",
     "noServersBody": "Import your first server config to get started",
     "importFromClipboard": "Import from Clipboard",
     "clipboard": "Clipboard",
     "pasteConfig": "Paste Config",
     "scanQr": "Scan QR",
     "parseErrorInvalidLink": "Couldn't parse config — check the link format and try again",
     "parseErrorUnsupported": "Unsupported protocol — only VLESS, VMess, Trojan, Shadowsocks, and Hysteria2 links are supported",
     "parseErrorEmptyClipboard": "Nothing on clipboard — copy a server config link first",
     "importSuccess": "Server imported successfully",
     "duplicateServer": "This server is already imported",
     "disabledConnect": "Connection available in next update",
     "disabledQrScan": "QR scanning coming soon",
     "deleteServerTitle": "Delete server?",
     "deleteServerBody": "This will permanently remove {serverName}.",
     "@deleteServerBody": { "placeholders": { "serverName": { "type": "String" } } },
     "deleteConfirm": "Delete",
     "deleteCancel": "Keep Server",
     "pasteConfigTitle": "Paste Config",
     "pasteConfigHint": "Paste a share link (vless://, vmess://, etc.) or raw JSON config",
     "pasteConfigAction": "Import Config",
     "pasteConfigEmpty": "Paste a config to continue",
     "themeSystem": "System",
     "themeLight": "Light",
     "themeDark": "Dark",
     "themeTitle": "Theme",
     "languageTitle": "Language",
     "routingPlaceholder": "Custom routing rules coming in a future update",
     "bypassLan": "Bypass LAN",
     "bypassLanSubtitle": "Don't route local network traffic (192.168.x.x, 10.x.x.x) through proxy",
     "notConnected": "Not Connected",
     "noServerSelected": "No server selected",
     "generalSection": "General",
     "aboutSection": "About",
     "version": "Version",
     "openSourceLicenses": "Open Source Licenses",
     "languageChanged": "Language changed to {language}",
     "@languageChanged": { "placeholders": { "language": { "type": "String" } } },
     "downloadSpeed": "↓ {speed}",
     "@downloadSpeed": { "placeholders": { "speed": { "type": "String" } } },
     "uploadSpeed": "↑ {speed}",
     "@uploadSpeed": { "placeholders": { "speed": { "type": "String" } } },
     "viewAction": "View",
     "retryAction": "Retry"
   }
   ```

2. **Create `lib/core/l10n/app_fa.arb`** (Persian — RTL):
   ```json
   {
     "@@locale": "fa",
     "appName": "آرما VPN",
     "dashboard": "داشبورد",
     "servers": "سرورها",
     "routing": "مسیریابی",
     "settings": "تنظیمات",
     "importServer": "وارد کردن سرور",
     "noServersYet": "هنوز سروری نیست",
     "noServersBody": "اولین پیکربندی سرور خود را وارد کنید",
     "importFromClipboard": "وارد کردن از کلیپ‌بورد",
     "clipboard": "کلیپ‌بورد",
     "pasteConfig": "پیکربندی را بچسبانید",
     "scanQr": "اسکن QR",
     "parseErrorInvalidLink": "پیکربندی قابل تجزیه نیست — فرمت لینک را بررسی و دوباره امتحان کنید",
     "parseErrorUnsupported": "پروتکل پشتیبانی نمی‌شود — فقط لینک‌های VLESS، VMess، Trojan، Shadowsocks و Hysteria2 پشتیبانی می‌شوند",
     "parseErrorEmptyClipboard": "کلیپ‌بورد خالی است — ابتدا یک لینک پیکربندی سرور کپی کنید",
     "importSuccess": "سرور با موفقیت وارد شد",
     "duplicateServer": "این سرور قبلاً وارد شده است",
     "disabledConnect": "اتصال در به‌روزرسانی بعدی فعال می‌شود",
     "disabledQrScan": "اسکن QR به زودی اضافه می‌شود",
     "deleteServerTitle": "حذف سرور؟",
     "deleteServerBody": "این عمل {serverName} را برای همیشه حذف می‌کند.",
     "deleteConfirm": "حذف",
     "deleteCancel": "نگه داشتن",
     "pasteConfigTitle": "پیکربندی را بچسبانید",
     "pasteConfigHint": "یک لینک اشتراکی (vless://، vmess:// و غیره) یا پیکربندی JSON بچسبانید",
     "pasteConfigAction": "وارد کردن پیکربندی",
     "pasteConfigEmpty": "برای ادامه یک پیکربندی بچسبانید",
     "themeSystem": "سیستم",
     "themeLight": "روشن",
     "themeDark": "تاریک",
     "themeTitle": "تم",
     "languageTitle": "زبان",
     "routingPlaceholder": "قوانین مسیریابی سفارشی در به‌روزرسانی آینده اضافه می‌شود",
     "bypassLan": "عبور از LAN",
     "bypassLanSubtitle": "ترافیک شبکه محلی (192.168.x.x, 10.x.x.x) از طریق پروکسی ارسال نشود",
     "notConnected": "متصل نیست",
     "noServerSelected": "سروری انتخاب نشده",
     "generalSection": "عمومی",
     "aboutSection": "درباره",
     "version": "نسخه",
     "openSourceLicenses": "مجوزهای متن‌باز",
     "languageChanged": "زبان به {language} تغییر کرد",
     "downloadSpeed": "↓ {speed}",
     "uploadSpeed": "↑ {speed}",
     "viewAction": "مشاهده",
     "retryAction": "تلاش مجدد"
   }
   ```

3. **Create `lib/core/l10n/app_ru.arb`** (Russian):
   ```json
   {
     "@@locale": "ru",
     "appName": "Arma VPN",
     "dashboard": "Главная",
     "servers": "Серверы",
     "routing": "Маршрутизация",
     "settings": "Настройки",
     "importServer": "Импорт сервера",
     "noServersYet": "Серверов пока нет",
     "noServersBody": "Импортируйте первую конфигурацию сервера",
     "importFromClipboard": "Импорт из буфера обмена",
     "clipboard": "Буфер обмена",
     "pasteConfig": "Вставить конфигурацию",
     "scanQr": "Сканировать QR",
     "parseErrorInvalidLink": "Не удалось разобрать конфигурацию — проверьте формат ссылки и попробуйте снова",
     "parseErrorUnsupported": "Протокол не поддерживается — поддерживаются только ссылки VLESS, VMess, Trojan, Shadowsocks и Hysteria2",
     "parseErrorEmptyClipboard": "Буфер обмена пуст — сначала скопируйте ссылку конфигурации сервера",
     "importSuccess": "Сервер успешно импортирован",
     "duplicateServer": "Этот сервер уже импортирован",
     "disabledConnect": "Подключение будет доступно в следующем обновлении",
     "disabledQrScan": "Сканирование QR скоро появится",
     "deleteServerTitle": "Удалить сервер?",
     "deleteServerBody": "Это навсегда удалит {serverName}.",
     "deleteConfirm": "Удалить",
     "deleteCancel": "Оставить",
     "pasteConfigTitle": "Вставить конфигурацию",
     "pasteConfigHint": "Вставьте ссылку (vless://, vmess:// и т.д.) или JSON конфигурацию",
     "pasteConfigAction": "Импортировать",
     "pasteConfigEmpty": "Вставьте конфигурацию для продолжения",
     "themeSystem": "Системная",
     "themeLight": "Светлая",
     "themeDark": "Тёмная",
     "themeTitle": "Тема",
     "languageTitle": "Язык",
     "routingPlaceholder": "Пользовательские правила маршрутизации появятся в будущем обновлении",
     "bypassLan": "Обход LAN",
     "bypassLanSubtitle": "Не направлять трафик локальной сети (192.168.x.x, 10.x.x.x) через прокси",
     "notConnected": "Не подключено",
     "noServerSelected": "Сервер не выбран",
     "generalSection": "Общие",
     "aboutSection": "О приложении",
     "version": "Версия",
     "openSourceLicenses": "Лицензии открытого кода",
     "languageChanged": "Язык изменён на {language}",
     "downloadSpeed": "↓ {speed}",
     "uploadSpeed": "↑ {speed}",
     "viewAction": "Просмотр",
     "retryAction": "Повторить"
   }
   ```

4. **Create `lib/core/l10n/app_zh.arb`** (Chinese Simplified):
   ```json
   {
     "@@locale": "zh",
     "appName": "Arma VPN",
     "dashboard": "仪表盘",
     "servers": "服务器",
     "routing": "路由",
     "settings": "设置",
     "importServer": "导入服务器",
     "noServersYet": "暂无服务器",
     "noServersBody": "导入您的第一个服务器配置开始使用",
     "importFromClipboard": "从剪贴板导入",
     "clipboard": "剪贴板",
     "pasteConfig": "粘贴配置",
     "scanQr": "扫描二维码",
     "parseErrorInvalidLink": "无法解析配置 — 请检查链接格式后重试",
     "parseErrorUnsupported": "不支持的协议 — 仅支持 VLESS、VMess、Trojan、Shadowsocks 和 Hysteria2 链接",
     "parseErrorEmptyClipboard": "剪贴板为空 — 请先复制服务器配置链接",
     "importSuccess": "服务器导入成功",
     "duplicateServer": "此服务器已导入",
     "disabledConnect": "连接功能将在下一版本中提供",
     "disabledQrScan": "二维码扫描即将推出",
     "deleteServerTitle": "删除服务器？",
     "deleteServerBody": "这将永久删除 {serverName}。",
     "deleteConfirm": "删除",
     "deleteCancel": "保留",
     "pasteConfigTitle": "粘贴配置",
     "pasteConfigHint": "粘贴分享链接（vless://、vmess:// 等）或 JSON 配置",
     "pasteConfigAction": "导入配置",
     "pasteConfigEmpty": "请粘贴配置以继续",
     "themeSystem": "跟随系统",
     "themeLight": "浅色",
     "themeDark": "深色",
     "themeTitle": "主题",
     "languageTitle": "语言",
     "routingPlaceholder": "自定义路由规则将在未来版本中推出",
     "bypassLan": "绕过局域网",
     "bypassLanSubtitle": "不通过代理路由本地网络流量 (192.168.x.x, 10.x.x.x)",
     "notConnected": "未连接",
     "noServerSelected": "未选择服务器",
     "generalSection": "通用",
     "aboutSection": "关于",
     "version": "版本",
     "openSourceLicenses": "开源许可证",
     "languageChanged": "语言已更改为{language}",
     "downloadSpeed": "↓ {speed}",
     "uploadSpeed": "↑ {speed}",
     "viewAction": "查看",
     "retryAction": "重试"
   }
   ```

5. **Create `lib/core/error/failures.dart`:**
   ```dart
   import 'package:equatable/equatable.dart';

   sealed class Failure extends Equatable {
     final String message;
     const Failure(this.message);
     @override
     List<Object?> get props => [message];
   }

   class ParseFailure extends Failure {
     const ParseFailure(super.message);
   }

   class StorageFailure extends Failure {
     const StorageFailure(super.message);
   }

   class ClipboardFailure extends Failure {
     const ClipboardFailure(super.message);
   }
   ```

6. **Create `lib/core/error/exceptions.dart`:**
   ```dart
   class ParseException implements Exception {
     final String message;
     const ParseException(this.message);
     @override
     String toString() => 'ParseException: $message';
   }

   class StorageException implements Exception {
     final String message;
     const StorageException(this.message);
     @override
     String toString() => 'StorageException: $message';
   }
   ```

7. **Run `flutter gen-l10n`** to generate AppLocalizations. Verify the generated file exists.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter gen-l10n 2>&1 && ls .dart_tool/flutter_gen/gen_l10n/ 2>/dev/null | head -5 && echo "---" && grep "noServersYet" lib/core/l10n/app_en.arb && grep "@@locale" lib/core/l10n/app_fa.arb && grep "@@locale" lib/core/l10n/app_ru.arb && grep "@@locale" lib/core/l10n/app_zh.arb && grep "sealed class Failure" lib/core/error/failures.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/l10n/app_en.arb contains `"@@locale": "en"` and at least 35 string keys
    - lib/core/l10n/app_fa.arb contains `"@@locale": "fa"` and same keys in Persian
    - lib/core/l10n/app_ru.arb contains `"@@locale": "ru"` and same keys in Russian
    - lib/core/l10n/app_zh.arb contains `"@@locale": "zh"` and same keys in Chinese
    - All ARB files contain `deleteServerBody` with `{serverName}` placeholder
    - lib/core/error/failures.dart contains `sealed class Failure`
    - lib/core/error/failures.dart contains `ParseFailure`, `StorageFailure`, `ClipboardFailure`
    - lib/core/error/exceptions.dart contains `ParseException` and `StorageException`
    - `flutter gen-l10n` exits successfully and generates AppLocalizations
  </acceptance_criteria>
  <done>4-language ARB files with all UI-SPEC copywriting strings generated and AppLocalizations code generated; error/failure sealed classes ready for use across features</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ServerConfig domain entity and Hive data layer</name>
  <files>lib/features/server/domain/entities/server_config.dart, lib/features/server/data/models/server_config_model.dart, lib/features/server/data/datasources/server_local_datasource.dart, lib/features/server/domain/repositories/server_repository.dart, lib/features/server/data/repositories/server_repository_impl.dart</files>
  <read_first>lib/core/constants/protocol_constants.dart, lib/core/error/failures.dart, lib/core/error/exceptions.dart, .planning/phases/01-foundation-config-import/01-RESEARCH.md</read_first>
  <behavior>
    - Test: ServerConfig entity can be created with required fields (id, name, protocol, address, port, addedAt)
    - Test: ServerConfig.copyWith produces new instance with changed fields
    - Test: ServerConfig.toJson / fromJson round-trips correctly
    - Test: ServerConfigModel maps to/from ServerConfig domain entity
    - Test: ServerConfigModel has correct HiveField indices with gaps
  </behavior>
  <action>
1. **Create `lib/features/server/domain/entities/server_config.dart`:**
   A `@freezed` class with all protocol-relevant fields:
   ```dart
   import 'package:freezed_annotation/freezed_annotation.dart';
   import 'package:arma_proxy_vpn_client/core/constants/protocol_constants.dart';

   part 'server_config.freezed.dart';
   part 'server_config.g.dart';

   @freezed
   class ServerConfig with _$ServerConfig {
     const factory ServerConfig({
       required String id,
       required String name,
       required ProtocolType protocol,
       required String address,
       required int port,
       String? uuid,
       String? password,
       @Default('none') String encryption,
       @Default('tcp') String network,  // tcp, ws, grpc, h2
       @Default('none') String security, // none, tls, reality
       String? sni,
       String? host,
       String? path,
       String? alpn,
       String? fingerprint,
       String? flow,  // for VLESS XTLS
       @Default(0) int alterId,  // for VMess
       String? serviceName,  // for gRPC
       String? authority,    // for gRPC
       String? publicKey,    // for Reality
       String? shortId,      // for Reality
       String? spiderX,      // for Reality
       String? method,       // for Shadowsocks (encryption method)
       String? obfs,         // for Hysteria2
       String? obfsPassword, // for Hysteria2
       String? subscriptionId,
       @Default('Manual') String groupName,
       required DateTime addedAt,
     }) = _ServerConfig;

     factory ServerConfig.fromJson(Map<String, dynamic> json) =>
         _$ServerConfigFromJson(json);
   }
   ```

2. **Create `lib/features/server/data/models/server_config_model.dart`:**
   Hive model with explicit `@HiveField` indices and GAPS for future schema evolution (per Pitfall 2):
   ```dart
   import 'package:hive_ce/hive_ce.dart';

   part 'server_config_model.g.dart';

   @HiveType(typeId: 0)
   class ServerConfigModel extends HiveObject {
     @HiveField(0) final String id;
     @HiveField(1) final String name;
     @HiveField(2) final int protocolIndex;
     // GAP: 3-4 reserved
     @HiveField(5) final String address;
     @HiveField(6) final int port;
     // GAP: 7-9 reserved
     @HiveField(10) final String? uuid;
     @HiveField(11) final String? password;
     @HiveField(12) final String encryption;
     @HiveField(13) final String network;
     @HiveField(14) final String security;
     @HiveField(15) final String? sni;
     // GAP: 16-19 reserved
     @HiveField(20) final String? host;
     @HiveField(21) final String? path;
     @HiveField(22) final String? alpn;
     @HiveField(23) final String? fingerprint;
     @HiveField(24) final String? flow;
     @HiveField(25) final int alterId;
     // GAP: 26-29 reserved
     @HiveField(30) final String? serviceName;
     @HiveField(31) final String? authority;
     @HiveField(32) final String? publicKey;
     @HiveField(33) final String? shortId;
     @HiveField(34) final String? spiderX;
     @HiveField(35) final String? method;
     // GAP: 36-39 reserved
     @HiveField(40) final String? obfs;
     @HiveField(41) final String? obfsPassword;
     @HiveField(42) final String? subscriptionId;
     @HiveField(43) final String groupName;
     @HiveField(44) final int addedAtMillis;

     ServerConfigModel({
       required this.id,
       required this.name,
       required this.protocolIndex,
       required this.address,
       required this.port,
       this.uuid,
       this.password,
       this.encryption = 'none',
       this.network = 'tcp',
       this.security = 'none',
       this.sni,
       this.host,
       this.path,
       this.alpn,
       this.fingerprint,
       this.flow,
       this.alterId = 0,
       this.serviceName,
       this.authority,
       this.publicKey,
       this.shortId,
       this.spiderX,
       this.method,
       this.obfs,
       this.obfsPassword,
       this.subscriptionId,
       this.groupName = 'Manual',
       required this.addedAtMillis,
     });
   }
   ```

   Add a companion extension `ServerConfigModelMapper` at the bottom of this file (or in a separate mapper file) with:
   - `ServerConfig toDomain()` — maps model to domain entity
   - `static ServerConfigModel fromDomain(ServerConfig config)` — maps domain to model

3. **Create `lib/features/server/domain/repositories/server_repository.dart`:**
   Abstract interface:
   ```dart
   import 'package:arma_proxy_vpn_client/features/server/domain/entities/server_config.dart';

   abstract class ServerRepository {
     Future<List<ServerConfig>> getAllConfigs();
     Future<ServerConfig?> getConfigById(String id);
     Future<void> saveConfig(ServerConfig config);
     Future<void> deleteConfig(String id);
     Future<bool> exists(String id);
   }
   ```

4. **Create `lib/features/server/data/datasources/server_local_datasource.dart`:**
   ```dart
   import 'package:hive_ce/hive_ce.dart';
   import 'package:arma_proxy_vpn_client/features/server/data/models/server_config_model.dart';
   import 'package:arma_proxy_vpn_client/core/error/exceptions.dart';

   class ServerLocalDatasource {
     final Box<ServerConfigModel> _box;
     ServerLocalDatasource(this._box);

     List<ServerConfigModel> getAll() => _box.values.toList();

     ServerConfigModel? getById(String id) {
       try {
         return _box.values.firstWhere((m) => m.id == id);
       } catch (_) {
         return null;
       }
     }

     Future<void> save(ServerConfigModel model) async {
       await _box.put(model.id, model);
     }

     Future<void> delete(String id) async {
       await _box.delete(id);
     }

     bool exists(String id) => _box.containsKey(id);
   }
   ```

5. **Create `lib/features/server/data/repositories/server_repository_impl.dart`:**
   Implements `ServerRepository`, wraps `ServerLocalDatasource`, catches exceptions and converts to failures.

6. **Run `dart run build_runner build --delete-conflicting-outputs`** to generate `.freezed.dart`, `.g.dart`, and Hive TypeAdapter files.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -10 && echo "---" && ls lib/features/server/domain/entities/server_config.freezed.dart 2>/dev/null && ls lib/features/server/data/models/server_config_model.g.dart 2>/dev/null && flutter analyze lib/features/server/ lib/core/error/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/server/domain/entities/server_config.dart contains `@freezed` and `class ServerConfig`
    - lib/features/server/domain/entities/server_config.dart contains fields: `id`, `name`, `protocol`, `address`, `port`, `uuid`, `password`, `encryption`, `network`, `security`, `sni`, `flow`, `alterId`, `publicKey`, `shortId`, `method`, `obfs`, `subscriptionId`, `groupName`, `addedAt`
    - lib/features/server/data/models/server_config_model.dart contains `@HiveType(typeId: 0)`
    - lib/features/server/data/models/server_config_model.dart contains `@HiveField(0)` through `@HiveField(44)` with gaps at indices 3-4, 7-9, 16-19, 26-29, 36-39
    - lib/features/server/data/models/server_config_model.dart contains `toDomain()` method
    - lib/features/server/domain/repositories/server_repository.dart contains `abstract class ServerRepository`
    - lib/features/server/data/datasources/server_local_datasource.dart contains `class ServerLocalDatasource`
    - lib/features/server/data/repositories/server_repository_impl.dart contains `class ServerRepositoryImpl implements ServerRepository`
    - Generated files exist: server_config.freezed.dart, server_config.g.dart, server_config_model.g.dart
    - `flutter analyze` reports zero errors on server/ and error/ directories
  </acceptance_criteria>
  <done>ServerConfig freezed entity with all protocol fields, Hive model with schema-safe indices, local datasource, repository abstraction and implementation, all generated code compiling clean</done>
</task>

<task type="auto">
  <name>Task 3: Create settings datasource, all providers, wire l10n and theme to app</name>
  <files>lib/features/settings/data/datasources/settings_local_datasource.dart, lib/features/settings/presentation/providers/theme_provider.dart, lib/features/settings/presentation/providers/locale_provider.dart, lib/features/server/presentation/providers/server_list_provider.dart, lib/features/server/presentation/providers/active_server_provider.dart, lib/main.dart, lib/app.dart</files>
  <read_first>lib/main.dart, lib/app.dart, lib/features/server/domain/entities/server_config.dart, lib/features/server/domain/repositories/server_repository.dart, lib/features/server/data/repositories/server_repository_impl.dart, lib/features/server/data/datasources/server_local_datasource.dart, lib/features/server/data/models/server_config_model.dart</read_first>
  <action>
1. **Create `lib/features/settings/data/datasources/settings_local_datasource.dart`:**
   ```dart
   import 'package:shared_preferences/shared_preferences.dart';

   class SettingsLocalDatasource {
     static const _themeKey = 'theme_mode';
     static const _localeKey = 'locale';
     static const _activeServerKey = 'active_server_id';

     final SharedPreferences _prefs;
     SettingsLocalDatasource(this._prefs);

     // Theme: 0=system, 1=light, 2=dark
     int getThemeMode() => _prefs.getInt(_themeKey) ?? 0;
     Future<void> setThemeMode(int mode) => _prefs.setInt(_themeKey, mode);

     String getLocale() => _prefs.getString(_localeKey) ?? 'en';
     Future<void> setLocale(String locale) => _prefs.setString(_localeKey, locale);

     String? getActiveServerId() => _prefs.getString(_activeServerKey);
     Future<void> setActiveServerId(String? id) async {
       if (id == null) {
         await _prefs.remove(_activeServerKey);
       } else {
         await _prefs.setString(_activeServerKey, id);
       }
     }
   }
   ```

2. **Create `lib/features/settings/presentation/providers/theme_provider.dart`:**
   Use `@riverpod` code generation:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:riverpod_annotation/riverpod_annotation.dart';
   import 'package:shared_preferences/shared_preferences.dart';
   import 'package:arma_proxy_vpn_client/features/settings/data/datasources/settings_local_datasource.dart';

   part 'theme_provider.g.dart';

   @riverpod
   class ThemeNotifier extends _$ThemeNotifier {
     late SettingsLocalDatasource _datasource;

     @override
     ThemeMode build() {
       // Initialize synchronously from prefs (loaded at app start)
       final prefs = ref.watch(sharedPreferencesProvider);
       _datasource = SettingsLocalDatasource(prefs);
       final modeIndex = _datasource.getThemeMode();
       return ThemeMode.values[modeIndex];
     }

     Future<void> setThemeMode(ThemeMode mode) async {
       await _datasource.setThemeMode(mode.index);
       state = mode;
     }
   }

   // Provider for SharedPreferences instance (override in main.dart)
   @Riverpod(keepAlive: true)
   SharedPreferences sharedPreferences(ref) {
     throw UnimplementedError('Must be overridden with actual SharedPreferences instance');
   }
   ```

3. **Create `lib/features/settings/presentation/providers/locale_provider.dart`:**
   ```dart
   import 'dart:ui';
   import 'package:riverpod_annotation/riverpod_annotation.dart';
   import 'package:arma_proxy_vpn_client/features/settings/data/datasources/settings_local_datasource.dart';
   import 'package:arma_proxy_vpn_client/features/settings/presentation/providers/theme_provider.dart';

   part 'locale_provider.g.dart';

   @riverpod
   class LocaleNotifier extends _$LocaleNotifier {
     late SettingsLocalDatasource _datasource;

     @override
     Locale build() {
       final prefs = ref.watch(sharedPreferencesProvider);
       _datasource = SettingsLocalDatasource(prefs);
       final localeCode = _datasource.getLocale();
       return Locale(localeCode);
     }

     Future<void> setLocale(Locale locale) async {
       await _datasource.setLocale(locale.languageCode);
       state = locale;
     }
   }

   // Supported locales list
   const supportedLocales = [
     Locale('en'),
     Locale('fa'),
     Locale('ru'),
     Locale('zh'),
   ];

   // Display names for language selector
   const localeDisplayNames = {
     'en': 'English',
     'fa': 'فارسی',
     'ru': 'Русский',
     'zh': '中文',
   };
   ```

4. **Create `lib/features/server/presentation/providers/server_list_provider.dart`:**
   ```dart
   import 'package:riverpod_annotation/riverpod_annotation.dart';
   import 'package:arma_proxy_vpn_client/features/server/domain/entities/server_config.dart';
   import 'package:arma_proxy_vpn_client/features/server/data/datasources/server_local_datasource.dart';
   import 'package:arma_proxy_vpn_client/features/server/data/models/server_config_model.dart';
   import 'package:arma_proxy_vpn_client/features/server/data/repositories/server_repository_impl.dart';
   import 'package:arma_proxy_vpn_client/features/server/domain/repositories/server_repository.dart';
   import 'package:hive_ce/hive_ce.dart';

   part 'server_list_provider.g.dart';

   @Riverpod(keepAlive: true)
   ServerRepository serverRepository(ref) {
     final box = Hive.box<ServerConfigModel>('configs');
     final datasource = ServerLocalDatasource(box);
     return ServerRepositoryImpl(datasource);
   }

   @riverpod
   class ServerListNotifier extends _$ServerListNotifier {
     @override
     Future<List<ServerConfig>> build() async {
       final repository = ref.watch(serverRepositoryProvider);
       return repository.getAllConfigs();
     }

     Future<void> addServer(ServerConfig config) async {
       final repository = ref.read(serverRepositoryProvider);
       await repository.saveConfig(config);
       ref.invalidateSelf();
     }

     Future<void> deleteServer(String id) async {
       final repository = ref.read(serverRepositoryProvider);
       await repository.deleteConfig(id);
       ref.invalidateSelf();
     }
   }
   ```

5. **Create `lib/features/server/presentation/providers/active_server_provider.dart`:**
   ```dart
   import 'package:riverpod_annotation/riverpod_annotation.dart';
   import 'package:arma_proxy_vpn_client/features/server/domain/entities/server_config.dart';
   import 'package:arma_proxy_vpn_client/features/server/presentation/providers/server_list_provider.dart';
   import 'package:arma_proxy_vpn_client/features/settings/data/datasources/settings_local_datasource.dart';
   import 'package:arma_proxy_vpn_client/features/settings/presentation/providers/theme_provider.dart';

   part 'active_server_provider.g.dart';

   @riverpod
   class ActiveServerNotifier extends _$ActiveServerNotifier {
     late SettingsLocalDatasource _datasource;

     @override
     ServerConfig? build() {
       final prefs = ref.watch(sharedPreferencesProvider);
       _datasource = SettingsLocalDatasource(prefs);
       final activeId = _datasource.getActiveServerId();
       if (activeId == null) return null;

       final serversAsync = ref.watch(serverListNotifierProvider);
       return serversAsync.whenOrNull(
         data: (servers) {
           try {
             return servers.firstWhere((s) => s.id == activeId);
           } catch (_) {
             return null;
           }
         },
       );
     }

     Future<void> selectServer(ServerConfig? server) async {
       await _datasource.setActiveServerId(server?.id);
       state = server;
     }
   }
   ```

6. **Update `lib/main.dart`** to add Hive initialization and SharedPreferences loading:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:hive_ce_flutter/hive_ce_flutter.dart';
   import 'package:shared_preferences/shared_preferences.dart';
   import 'package:arma_proxy_vpn_client/app.dart';
   import 'package:arma_proxy_vpn_client/features/server/data/models/server_config_model.dart';
   import 'package:arma_proxy_vpn_client/features/settings/presentation/providers/theme_provider.dart';

   void main() async {
     WidgetsFlutterBinding.ensureInitialized();

     // Initialize Hive
     await Hive.initFlutter();
     Hive.registerAdapter(ServerConfigModelAdapter());
     await Hive.openBox<ServerConfigModel>('configs');

     // Load SharedPreferences
     final prefs = await SharedPreferences.getInstance();

     runApp(
       ProviderScope(
         overrides: [
           sharedPreferencesProvider.overrideWithValue(prefs),
         ],
         child: const ArmaApp(),
       ),
     );
   }
   ```

7. **Update `lib/app.dart`** to wire theme, locale, and localization:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:flutter_localizations/flutter_localizations.dart';
   import 'package:flutter_gen/gen_l10n/app_localizations.dart';
   import 'package:arma_proxy_vpn_client/core/theme/app_theme.dart';
   import 'package:arma_proxy_vpn_client/core/router/app_router.dart';
   import 'package:arma_proxy_vpn_client/features/settings/presentation/providers/theme_provider.dart';
   import 'package:arma_proxy_vpn_client/features/settings/presentation/providers/locale_provider.dart';

   class ArmaApp extends ConsumerWidget {
     const ArmaApp({super.key});

     @override
     Widget build(BuildContext context, WidgetRef ref) {
       final themeMode = ref.watch(themeNotifierProvider);
       final locale = ref.watch(localeNotifierProvider);

       return MaterialApp.router(
         title: 'Arma VPN',
         theme: AppTheme.light(),
         darkTheme: AppTheme.dark(),
         themeMode: themeMode,
         locale: locale,
         localizationsDelegates: AppLocalizations.localizationsDelegates,
         supportedLocales: AppLocalizations.supportedLocales,
         routerConfig: goRouter,
         debugShowCheckedModeBanner: false,
       );
     }
   }
   ```

8. **Run `dart run build_runner build --delete-conflicting-outputs`** to generate all provider `.g.dart` files.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -10 && echo "---" && flutter analyze lib/ 2>&1 | tail -10 && grep "ThemeNotifier" lib/features/settings/presentation/providers/theme_provider.dart && grep "LocaleNotifier" lib/features/settings/presentation/providers/locale_provider.dart && grep "ServerListNotifier" lib/features/server/presentation/providers/server_list_provider.dart && grep "Hive.initFlutter" lib/main.dart && grep "localizationsDelegates" lib/app.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/settings/data/datasources/settings_local_datasource.dart contains `class SettingsLocalDatasource` with `getThemeMode`, `setThemeMode`, `getLocale`, `setLocale`, `getActiveServerId`, `setActiveServerId`
    - lib/features/settings/presentation/providers/theme_provider.dart contains `@riverpod` and `class ThemeNotifier`
    - lib/features/settings/presentation/providers/locale_provider.dart contains `@riverpod` and `class LocaleNotifier`
    - lib/features/settings/presentation/providers/locale_provider.dart contains `supportedLocales` list with 4 locales and `localeDisplayNames` map
    - lib/features/server/presentation/providers/server_list_provider.dart contains `@riverpod` and `class ServerListNotifier` with `addServer` and `deleteServer` methods
    - lib/features/server/presentation/providers/active_server_provider.dart contains `@riverpod` and `class ActiveServerNotifier` with `selectServer` method
    - lib/main.dart contains `Hive.initFlutter()`, `Hive.registerAdapter(ServerConfigModelAdapter())`, `Hive.openBox`, `SharedPreferences.getInstance()`
    - lib/main.dart contains `sharedPreferencesProvider.overrideWithValue`
    - lib/app.dart contains `ref.watch(themeNotifierProvider)` and `ref.watch(localeNotifierProvider)`
    - lib/app.dart contains `localizationsDelegates: AppLocalizations.localizationsDelegates`
    - lib/app.dart contains `supportedLocales: AppLocalizations.supportedLocales`
    - Generated .g.dart files exist for all providers
    - `flutter analyze lib/` reports zero errors
  </acceptance_criteria>
  <done>Complete data layer with settings persistence via shared_preferences, all Riverpod providers (theme, locale, server list, active server) generated and wired, Hive initialized in main.dart, AppLocalizations wired into MaterialApp.router with 4-language support and RTL for Persian</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hive storage ↔ app | Data read from local Hive boxes could be corrupted or tampered with |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | ServerConfigModel (Hive) | mitigate | Validate all fields after reading from Hive — null-check required fields (id, name, address, port), validate protocolIndex is in ProtocolType enum range, skip corrupted records with debugPrint warning |
| T-01-02-02 | Information Disclosure | SettingsLocalDatasource | accept | Shared_preferences stores theme/locale/active server ID — no sensitive data. Passwords and UUIDs are in Hive, which stores on device-local filesystem with no encryption (acceptable for Phase 1 — same as V2rayNG and Hiddify). |
</threat_model>

<verification>
1. `dart run build_runner build --delete-conflicting-outputs` succeeds
2. `flutter gen-l10n` generates AppLocalizations for all 4 locales
3. `flutter analyze lib/` reports zero errors
4. All generated `.g.dart` and `.freezed.dart` files exist
5. ServerConfig entity has all required protocol fields
6. Hive model has field index gaps at 3-4, 7-9, 16-19, 26-29, 36-39
</verification>

<success_criteria>
- ServerConfig freezed entity covers all 5 protocol types with their specific fields
- Hive model uses explicit field indices with gaps for safe schema evolution
- All 4 ARB files contain complete UI-SPEC copywriting strings
- AppLocalizations generated and wired into MaterialApp
- Theme and locale providers persist user choices via shared_preferences
- Server list and active server providers expose reactive state from Hive storage
- All code generation completes without errors
- `flutter analyze` clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-config-import/01-02-SUMMARY.md`
</output>
