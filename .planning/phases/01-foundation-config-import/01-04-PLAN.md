---
phase: 01-foundation-config-import
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/features/dashboard/presentation/screens/dashboard_screen.dart
  - lib/features/dashboard/presentation/widgets/connect_button.dart
  - lib/features/dashboard/presentation/widgets/active_server_card.dart
  - lib/features/dashboard/presentation/widgets/traffic_stats_placeholder.dart
  - lib/features/settings/presentation/screens/settings_screen.dart
  - lib/features/routing/presentation/screens/routing_screen.dart
autonomous: true
requirements:
  - UI-01
  - STOR-02
  - SERV-02
must_haves:
  truths:
    - "Dashboard shows a teal circular connect button (disabled placeholder) with power icon"
    - "Dashboard shows the currently selected server name and protocol badge, or 'No server selected'"
    - "Settings screen has functional theme toggle (System/Light/Dark) that persists"
    - "Settings screen has functional language selector with 4 languages that persists"
    - "Routing screen shows Bypass LAN toggle and placeholder card"
    - "Theme changes apply immediately across the entire app"
    - "Language changes apply immediately with correct RTL for Persian"
  artifacts:
    - path: "lib/features/dashboard/presentation/widgets/connect_button.dart"
      provides: "Circular 120dp teal connect button (Phase 1 placeholder)"
      contains: "120"
    - path: "lib/features/settings/presentation/screens/settings_screen.dart"
      provides: "Settings with theme toggle and language selector"
      contains: "SegmentedButton"
    - path: "lib/features/routing/presentation/screens/routing_screen.dart"
      provides: "Routing with bypass LAN toggle"
      contains: "SwitchListTile"
  key_links:
    - from: "lib/features/settings/presentation/screens/settings_screen.dart"
      to: "lib/features/settings/presentation/providers/theme_provider.dart"
      via: "ref.read(themeNotifierProvider.notifier).setThemeMode"
      pattern: "themeNotifierProvider"
    - from: "lib/features/dashboard/presentation/widgets/active_server_card.dart"
      to: "lib/features/server/presentation/providers/active_server_provider.dart"
      via: "ref.watch(activeServerNotifierProvider)"
      pattern: "activeServerNotifierProvider"
---

<objective>
Build the Dashboard, Settings, and Routing screens with full functionality for Phase 1. Dashboard shows the placeholder connect button and active server card. Settings provides working theme and language controls. Routing shows the bypass LAN toggle and future-update placeholder.

Purpose: These three screens complete the navigation shell with real content — the user can now explore the app, change theme/language, and see the dashboard layout.

Output: 3 functional screens that compile, display correctly in both themes, and persist user preferences.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-config-import/01-CONTEXT.md
@.planning/phases/01-foundation-config-import/01-UI-SPEC.md
@.planning/phases/01-foundation-config-import/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — providers and types used by these screens -->

From lib/features/settings/presentation/providers/theme_provider.dart:
```dart
@riverpod
class ThemeNotifier extends _$ThemeNotifier {
  ThemeMode build(); // returns current ThemeMode
  Future<void> setThemeMode(ThemeMode mode);
}
// Provider: themeNotifierProvider
```

From lib/features/settings/presentation/providers/locale_provider.dart:
```dart
@riverpod
class LocaleNotifier extends _$LocaleNotifier {
  Locale build(); // returns current Locale
  Future<void> setLocale(Locale locale);
}
// Provider: localeNotifierProvider
const supportedLocales = [Locale('en'), Locale('fa'), Locale('ru'), Locale('zh')];
const localeDisplayNames = {'en': 'English', 'fa': 'فارسی', 'ru': 'Русский', 'zh': '中文'};
```

From lib/features/server/presentation/providers/active_server_provider.dart:
```dart
@riverpod
class ActiveServerNotifier extends _$ActiveServerNotifier {
  ServerConfig? build(); // returns currently selected server or null
  Future<void> selectServer(ServerConfig? server);
}
// Provider: activeServerNotifierProvider
```

From lib/features/server/domain/entities/server_config.dart:
```dart
class ServerConfig { String id, name; ProtocolType protocol; String address; int port; ... }
```

From lib/core/constants/protocol_constants.dart:
```dart
enum ProtocolType { vless, vmess, trojan, shadowsocks, hysteria2 }
// .label → 'VLESS', 'VMess', etc.
```

From lib/core/theme/app_colors.dart:
```dart
class AppColors {
  static Color protocolColor(ProtocolType type);
}
```

From generated AppLocalizations:
```dart
AppLocalizations.of(context)!.notConnected  // "Not Connected"
AppLocalizations.of(context)!.noServerSelected  // "No server selected"
// ... all strings from ARB files
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard screen with connect button, active server card, and traffic stats</name>
  <files>lib/features/dashboard/presentation/screens/dashboard_screen.dart, lib/features/dashboard/presentation/widgets/connect_button.dart, lib/features/dashboard/presentation/widgets/active_server_card.dart, lib/features/dashboard/presentation/widgets/traffic_stats_placeholder.dart</files>
  <read_first>lib/features/dashboard/presentation/screens/dashboard_screen.dart, lib/features/server/presentation/providers/active_server_provider.dart, lib/core/theme/app_colors.dart, lib/core/constants/protocol_constants.dart, .planning/phases/01-foundation-config-import/01-UI-SPEC.md</read_first>
  <action>
1. **Replace `lib/features/dashboard/presentation/screens/dashboard_screen.dart`** (overwrite Plan 01 placeholder):
   - `ConsumerWidget` named `DashboardScreen`
   - `AppBar` with title from `AppLocalizations.of(context)!.appName` using `titleLarge` — no back button, no trailing actions
   - Body: `Center` → `Column(mainAxisAlignment: MainAxisAlignment.center)` containing:
     1. `ConnectButton()` widget
     2. `Gap(16)` (from gap package)
     3. Connection state label: `Text(AppLocalizations.of(context)!.notConnected)` with style `titleMedium`, color `onSurfaceVariant`
     4. `Gap(24)`
     5. `ActiveServerCard()` widget
     6. `Gap(16)`
     7. `TrafficStatsPlaceholder()` widget

2. **Create `lib/features/dashboard/presentation/widgets/connect_button.dart`:**
   - `StatelessWidget` named `ConnectButton` (per D-03 — custom circular button, Phase 1 placeholder)
   - Build: `GestureDetector` wrapping a `Container`:
     - Size: `120` width × `120` height (120dp diameter per UI-SPEC)
     - Decoration: `BoxDecoration(shape: BoxShape.circle, color: Theme.of(context).colorScheme.primary.withOpacity(0.5))` — 50% opacity for disabled state
     - Child: `Icon(Icons.power_settings_new, color: Theme.of(context).colorScheme.onPrimary, size: 48)`
   - `onTap`: Show SnackBar with `AppLocalizations.of(context)!.disabledConnect`, duration `AppConstants.snackBarDurationShort`
   - Add `Semantics(label: 'Connect button, disabled', child: ...)`

3. **Create `lib/features/dashboard/presentation/widgets/active_server_card.dart`:**
   - `ConsumerWidget` named `ActiveServerCard`
   - Watch `activeServerNotifierProvider` for the selected server
   - Build a `Card` with `surfaceContainerLow` fill, 12dp radius, 16dp padding:
     - If server is null: `Text(AppLocalizations.of(context)!.noServerSelected)` in `bodyMedium`, `onSurfaceVariant` color
     - If server exists: `Row` → protocol badge `Container` (small colored chip with `AppColors.protocolColor(server.protocol)` background, `labelMedium` white text showing `server.protocol.label`) → `Gap(8)` → `Column(crossAxisAlignment: CrossAxisAlignment.start)` → server name `titleMedium` + server address `bodyMedium`/`onSurfaceVariant`

4. **Create `lib/features/dashboard/presentation/widgets/traffic_stats_placeholder.dart`:**
   - `StatelessWidget` named `TrafficStatsPlaceholder`
   - Build a `Row(mainAxisAlignment: MainAxisAlignment.center)` with two text blocks:
     - `Text(AppLocalizations.of(context)!.downloadSpeed('0 B/s'))` in `bodyMedium`, `onSurfaceVariant`
     - `Gap(32)`
     - `Text(AppLocalizations.of(context)!.uploadSpeed('0 B/s'))` in `bodyMedium`, `onSurfaceVariant`

All widgets use `const` constructors where possible, `super.key` syntax, and import from `package:arma_proxy_vpn_client/...`.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter analyze lib/features/dashboard/ 2>&1 | tail -5 && grep "120" lib/features/dashboard/presentation/widgets/connect_button.dart && grep "Icons.power_settings_new" lib/features/dashboard/presentation/widgets/connect_button.dart && grep "activeServerNotifierProvider" lib/features/dashboard/presentation/widgets/active_server_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/dashboard/presentation/screens/dashboard_screen.dart contains `ConsumerWidget` and `DashboardScreen`
    - lib/features/dashboard/presentation/screens/dashboard_screen.dart contains `AppLocalizations.of(context)` (uses l10n, not hardcoded strings)
    - lib/features/dashboard/presentation/widgets/connect_button.dart contains width/height of `120` and `Icons.power_settings_new`
    - lib/features/dashboard/presentation/widgets/connect_button.dart contains `withOpacity(0.5)` for disabled state
    - lib/features/dashboard/presentation/widgets/connect_button.dart contains `disabledConnect` snackbar text
    - lib/features/dashboard/presentation/widgets/active_server_card.dart contains `ref.watch(activeServerNotifierProvider)`
    - lib/features/dashboard/presentation/widgets/active_server_card.dart contains `noServerSelected` fallback
    - lib/features/dashboard/presentation/widgets/traffic_stats_placeholder.dart contains `downloadSpeed` and `uploadSpeed`
    - `flutter analyze lib/features/dashboard/` reports zero errors
  </acceptance_criteria>
  <done>Dashboard screen with 120dp circular teal connect button (disabled at 50% opacity showing snackbar on tap), active server card showing selected server or "No server selected", and static traffic stats placeholder — all using localized strings</done>
</task>

<task type="auto">
  <name>Task 2: Build Settings screen and Routing screen</name>
  <files>lib/features/settings/presentation/screens/settings_screen.dart, lib/features/routing/presentation/screens/routing_screen.dart</files>
  <read_first>lib/features/settings/presentation/screens/settings_screen.dart, lib/features/routing/presentation/screens/routing_screen.dart, lib/features/settings/presentation/providers/theme_provider.dart, lib/features/settings/presentation/providers/locale_provider.dart, .planning/phases/01-foundation-config-import/01-UI-SPEC.md</read_first>
  <action>
1. **Replace `lib/features/settings/presentation/screens/settings_screen.dart`** (overwrite Plan 01 placeholder):
   - `ConsumerWidget` named `SettingsScreen`
   - `AppBar` with title `AppLocalizations.of(context)!.settings` using `titleLarge`
   - Body: `ListView` with the following items:

   **General section:**
   - Section header: `Padding(padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8))` → `Text(AppLocalizations.of(context)!.generalSection)` with `labelLarge` style, `primary` color

   **Theme selector:**
   - `ListTile` with `Icons.brightness_6` leading icon
   - Title: `AppLocalizations.of(context)!.themeTitle` in `titleMedium`
   - Subtitle: `SegmentedButton<ThemeMode>` with 3 segments:
     - `ButtonSegment(value: ThemeMode.system, label: Text(l10n.themeSystem))`
     - `ButtonSegment(value: ThemeMode.light, label: Text(l10n.themeLight))`
     - `ButtonSegment(value: ThemeMode.dark, label: Text(l10n.themeDark))`
   - `selected: {ref.watch(themeNotifierProvider)}`
   - `onSelectionChanged: (values) => ref.read(themeNotifierProvider.notifier).setThemeMode(values.first)`

   **Language selector:**
   - `ListTile` with `Icons.language` leading icon
   - Title: `AppLocalizations.of(context)!.languageTitle` in `titleMedium`
   - Trailing: current language display name from `localeDisplayNames[ref.watch(localeNotifierProvider).languageCode]`
   - `onTap`: `showModalBottomSheet` with a `Column` of `ListTile` widgets for each locale in `supportedLocales`:
     - Each tile shows the display name and a `Radio` indicator (selected if matches current locale)
     - On tap: `ref.read(localeNotifierProvider.notifier).setLocale(locale)`, then `Navigator.pop(context)`, then show SnackBar with `AppLocalizations.of(context)!.languageChanged(displayName)`

   **About section:**
   - Section header: `Text(AppLocalizations.of(context)!.aboutSection)` same style as General
   - `ListTile`: title "Version" (`l10n.version`), trailing text "1.0.0" (`AppConstants.appVersion`) in `bodyMedium`
   - `ListTile`: title `l10n.openSourceLicenses`, `onTap: () => showLicensePage(context: context, applicationName: AppConstants.appName)`

2. **Replace `lib/features/routing/presentation/screens/routing_screen.dart`** (overwrite Plan 01 placeholder):
   - `StatefulWidget` named `RoutingScreen` (needs local state for toggle)
   - `AppBar` with title `AppLocalizations.of(context)!.routing` using `titleLarge`
   - Body: `ListView` with:
     - `SwitchListTile` for Bypass LAN:
       - Title: `AppLocalizations.of(context)!.bypassLan` in `titleMedium`
       - Subtitle: `AppLocalizations.of(context)!.bypassLanSubtitle` in `bodyMedium`
       - Value: `true` (default ON, stored in local state for Phase 1 — persistence added in Phase 4)
       - `onChanged`: toggle local state
     - `Gap(16)`
     - `Card` with 12dp radius, 48dp vertical padding, centered:
       - `Text(AppLocalizations.of(context)!.routingPlaceholder)` in `bodyMedium`, `onSurfaceVariant` color

All strings use `AppLocalizations.of(context)!` — no hardcoded English.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter analyze lib/features/settings/ lib/features/routing/ 2>&1 | tail -5 && grep "SegmentedButton" lib/features/settings/presentation/screens/settings_screen.dart && grep "themeNotifierProvider" lib/features/settings/presentation/screens/settings_screen.dart && grep "localeNotifierProvider" lib/features/settings/presentation/screens/settings_screen.dart && grep "SwitchListTile" lib/features/routing/presentation/screens/routing_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/settings/presentation/screens/settings_screen.dart contains `ConsumerWidget` and `SettingsScreen`
    - lib/features/settings/presentation/screens/settings_screen.dart contains `SegmentedButton<ThemeMode>` with 3 segments (system, light, dark)
    - lib/features/settings/presentation/screens/settings_screen.dart contains `ref.watch(themeNotifierProvider)` and `ref.read(themeNotifierProvider.notifier).setThemeMode`
    - lib/features/settings/presentation/screens/settings_screen.dart contains `showModalBottomSheet` for language selection
    - lib/features/settings/presentation/screens/settings_screen.dart contains `localeNotifierProvider` for reading and writing locale
    - lib/features/settings/presentation/screens/settings_screen.dart contains `showLicensePage`
    - lib/features/settings/presentation/screens/settings_screen.dart uses `AppLocalizations.of(context)` (NOT hardcoded strings)
    - lib/features/routing/presentation/screens/routing_screen.dart contains `SwitchListTile` and `bypassLan`
    - lib/features/routing/presentation/screens/routing_screen.dart contains `routingPlaceholder`
    - `flutter analyze lib/features/settings/ lib/features/routing/` reports zero errors
  </acceptance_criteria>
  <done>Settings screen with functional theme toggle (SegmentedButton: System/Light/Dark), language selector (ModalBottomSheet with 4 languages), and About section; Routing screen with Bypass LAN toggle and placeholder card — all using localized strings, theme/language changes apply immediately and persist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in Plan 04 | Screens display data from local providers. No external input processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | N/A | UI screens | accept | These screens read from local Riverpod providers and display data. No user input is processed beyond theme/language selection (enum values, not free text). No threats at ASVS L1. |
</threat_model>

<verification>
1. `flutter analyze lib/features/dashboard/ lib/features/settings/ lib/features/routing/` — zero errors
2. Dashboard shows connect button, active server card, traffic stats
3. Settings theme toggle changes app theme immediately
4. Settings language selector changes locale immediately (Persian triggers RTL)
5. Routing shows bypass LAN toggle and placeholder
</verification>

<success_criteria>
- Dashboard has 120dp circular teal connect button at 50% opacity (Phase 1 disabled state) per D-03
- Dashboard shows active server name + protocol badge or "No server selected"
- Settings theme toggle uses SegmentedButton with System/Light/Dark, persists choice
- Settings language selector opens bottom sheet with English/Persian/Russian/Chinese
- Theme changes apply instantly across all screens
- Language changes apply instantly, Persian activates RTL layout
- Routing has Bypass LAN toggle (default ON) and placeholder card
- All strings are localized via AppLocalizations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-config-import/01-04-SUMMARY.md`
</output>
