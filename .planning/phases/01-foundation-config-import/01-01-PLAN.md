---
phase: 01-foundation-config-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - l10n.yaml
  - analysis_options.yaml
  - android/app/build.gradle.kts
  - lib/main.dart
  - lib/app.dart
  - lib/core/theme/app_theme.dart
  - lib/core/theme/app_colors.dart
  - lib/core/constants/app_constants.dart
  - lib/core/constants/protocol_constants.dart
  - lib/core/router/app_router.dart
  - lib/shared/widgets/navigation_shell.dart
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "App launches with teal Material 3 theme and shows bottom navigation with 4 tabs"
    - "Tapping each bottom nav tab navigates to a different screen placeholder"
    - "Tab state is preserved when switching between tabs (no rebuild)"
    - "flutter analyze reports zero errors"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies declared"
      contains: "flutter_riverpod"
    - path: "lib/app.dart"
      provides: "MaterialApp.router root widget with theme and router"
      contains: "MaterialApp.router"
    - path: "lib/core/theme/app_theme.dart"
      provides: "Light and dark ThemeData with teal seed"
      contains: "0xFF00897B"
    - path: "lib/core/router/app_router.dart"
      provides: "GoRouter with StatefulShellRoute for 4 tabs"
      contains: "StatefulShellRoute"
    - path: "lib/shared/widgets/navigation_shell.dart"
      provides: "Bottom NavigationBar scaffold"
      contains: "NavigationBar"
  key_links:
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "ProviderScope wrapping ArmaApp"
      pattern: "ProviderScope.*ArmaApp"
    - from: "lib/app.dart"
      to: "lib/core/router/app_router.dart"
      via: "routerConfig reference"
      pattern: "routerConfig"
    - from: "lib/core/router/app_router.dart"
      to: "lib/shared/widgets/navigation_shell.dart"
      via: "ShellRoute builder"
      pattern: "NavigationShell"
---

<objective>
Set up the entire project foundation from the default Flutter scaffold: install all Phase 1 dependencies, configure the Android project, create the Material 3 theme system with teal/cyan seed color, build the go_router navigation with bottom nav shell, and rewrite the app entry points.

Purpose: Everything else in Phase 1 depends on this infrastructure — theme, router, Riverpod, and navigation shell must exist before any feature screen or data layer can be built.

Output: A running app with 4-tab bottom navigation, teal Material 3 theme, and placeholder screens that compiles and navigates cleanly.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-config-import/01-CONTEXT.md
@.planning/phases/01-foundation-config-import/01-RESEARCH.md
@.planning/phases/01-foundation-config-import/01-UI-SPEC.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, configure Android, scaffold directories</name>
  <files>pubspec.yaml, l10n.yaml, analysis_options.yaml, android/app/build.gradle.kts</files>
  <read_first>pubspec.yaml, android/app/build.gradle.kts, analysis_options.yaml, .planning/phases/01-foundation-config-import/01-RESEARCH.md</read_first>
  <action>
1. **Replace pubspec.yaml** with the following dependencies (keep `name: arma_proxy_vpn_client`, `version: 1.0.0+1`, `sdk: ^3.11.4`):

   dependencies:
   - `flutter: {sdk: flutter}`
   - `flutter_localizations: {sdk: flutter}`
   - `flutter_riverpod: ^3.3.1`
   - `riverpod_annotation: ^4.0.2`
   - `go_router: ^17.2.0`
   - `hive_ce: ^2.19.3`
   - `hive_ce_flutter: ^2.3.4`
   - `freezed_annotation: ^3.1.0`
   - `json_annotation: ^4.11.0`
   - `intl: ^0.20.2`
   - `shared_preferences: ^2.5.5`
   - `uuid: ^4.5.3`
   - `equatable: ^2.0.8`
   - `gap: ^3.0.1`

   dev_dependencies:
   - `flutter_test: {sdk: flutter}`
   - `flutter_lints: ^6.0.0`
   - `riverpod_generator: ^4.0.3`
   - `riverpod_lint: ^3.1.3`
   - `custom_lint: ^0.8.1`
   - `freezed: ^3.2.5`
   - `json_serializable: ^6.13.1`
   - `hive_ce_generator: ^1.11.1`
   - `build_runner: ^2.13.1`
   - `mockito: ^5.6.4`

   Under `flutter:` section, set `generate: true` (enables flutter gen-l10n) and keep `uses-material-design: true`. Remove `cupertino_icons`.

2. **Create l10n.yaml** in project root:
   ```yaml
   arb-dir: lib/core/l10n
   template-arb-file: app_en.arb
   output-localization-file: app_localizations.dart
   ```

3. **Update analysis_options.yaml** — keep the existing `include: package:flutter_lints/flutter.yaml` and add:
   ```yaml
   analyzer:
     plugins:
       - custom_lint
   ```

4. **Update android/app/build.gradle.kts:**
   - Change `namespace` from `"com.example.arma_proxy_vpn_client"` to `"com.arma.vpn"`
   - Change `applicationId` from `"com.example.arma_proxy_vpn_client"` to `"com.arma.vpn"`
   - Change `minSdk = flutter.minSdkVersion` to `minSdk = 24`

5. **Create directory scaffold** (empty directories with placeholder `.gitkeep` or just ensure they exist):
   ```
   lib/core/constants/
   lib/core/error/
   lib/core/router/
   lib/core/theme/
   lib/core/l10n/
   lib/core/utils/
   lib/features/dashboard/presentation/screens/
   lib/features/dashboard/presentation/widgets/
   lib/features/server/domain/entities/
   lib/features/server/domain/repositories/
   lib/features/server/data/models/
   lib/features/server/data/datasources/
   lib/features/server/data/repositories/
   lib/features/server/data/parsers/
   lib/features/server/presentation/providers/
   lib/features/server/presentation/screens/
   lib/features/server/presentation/widgets/
   lib/features/routing/presentation/screens/
   lib/features/settings/domain/entities/
   lib/features/settings/data/datasources/
   lib/features/settings/presentation/providers/
   lib/features/settings/presentation/screens/
   lib/shared/widgets/
   ```

6. **Run `flutter pub get`** to install all dependencies and verify resolution.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter pub get 2>&1 | tail -5 && echo "---" && grep "flutter_riverpod" pubspec.yaml && grep "go_router" pubspec.yaml && grep "hive_ce:" pubspec.yaml && grep "generate: true" pubspec.yaml && grep 'applicationId = "com.arma.vpn"' android/app/build.gradle.kts && grep "minSdk = 24" android/app/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `flutter_riverpod: ^3.3.1`
    - pubspec.yaml contains `go_router: ^17.2.0`
    - pubspec.yaml contains `hive_ce: ^2.19.3`
    - pubspec.yaml contains `generate: true` under flutter section
    - pubspec.yaml does NOT contain `cupertino_icons`
    - l10n.yaml exists at project root with `arb-dir: lib/core/l10n`
    - android/app/build.gradle.kts contains `applicationId = "com.arma.vpn"`
    - android/app/build.gradle.kts contains `namespace = "com.arma.vpn"`
    - android/app/build.gradle.kts contains `minSdk = 24`
    - analysis_options.yaml contains `custom_lint` under plugins
    - `flutter pub get` exits with code 0
    - directories lib/core/theme/, lib/core/router/, lib/features/server/domain/entities/ exist
  </acceptance_criteria>
  <done>All Phase 1 dependencies installed, Android project configured with com.arma.vpn and minSdk 24, directory scaffold created, l10n.yaml in place</done>
</task>

<task type="auto">
  <name>Task 2: Create theme system and constants</name>
  <files>lib/core/theme/app_theme.dart, lib/core/theme/app_colors.dart, lib/core/constants/app_constants.dart, lib/core/constants/protocol_constants.dart</files>
  <read_first>pubspec.yaml, .planning/phases/01-foundation-config-import/01-UI-SPEC.md, .planning/phases/01-foundation-config-import/01-RESEARCH.md</read_first>
  <action>
1. **Create `lib/core/theme/app_theme.dart`:**
   - Class `AppTheme` with two static methods: `light()` and `dark()`, both returning `ThemeData`.
   - Seed color: `const Color(0xFF00897B)` (Teal 600) per D-01.
   - Both themes: `useMaterial3: true`, `colorScheme: ColorScheme.fromSeed(seedColor: _seedColor, brightness: ...)`.
   - Light theme `cardTheme`: elevation 1, `RoundedRectangleBorder` with `BorderRadius.all(Radius.circular(12))`.
   - Dark theme `cardTheme`: elevation 0, `RoundedRectangleBorder` with `BorderRadius.all(Radius.circular(12))` + `side: BorderSide(color: colorScheme.outlineVariant)` — use the dark colorScheme's `outlineVariant` for the border color.
   - Private `static const _seedColor = Color(0xFF00897B);`.

2. **Create `lib/core/theme/app_colors.dart`:**
   - Class `AppColors` with static const Color fields for protocol badges:
     - `static const vless = Color(0xFF00897B);` // Teal 600
     - `static const vmess = Color(0xFF1565C0);` // Blue 800
     - `static const trojan = Color(0xFFE65100);` // Orange 900
     - `static const shadowsocks = Color(0xFF6A1B9A);` // Purple 800
     - `static const hysteria2 = Color(0xFF2E7D32);` // Green 800
   - Static method `Color protocolColor(ProtocolType type)` that maps each enum value to its color (import from protocol_constants.dart).

3. **Create `lib/core/constants/protocol_constants.dart`:**
   - Enum `ProtocolType` with values: `vless, vmess, trojan, shadowsocks, hysteria2`.
   - Each value has a `String get label` (display name): `'VLESS', 'VMess', 'Trojan', 'SS', 'Hysteria2'`.
   - Each value has a `String get scheme` (URI scheme): `'vless', 'vmess', 'trojan', 'ss', 'hysteria2'`.
   - Add a `static ProtocolType? fromScheme(String scheme)` factory method that matches the scheme string (also match `'hy2'` for hysteria2).

4. **Create `lib/core/constants/app_constants.dart`:**
   - Class `AppConstants` with:
     - `static const appName = 'Arma VPN';`
     - `static const appVersion = '1.0.0';`
     - `static const maxServerNameLength = 50;`
     - `static const snackBarDurationShort = Duration(seconds: 3);`
     - `static const snackBarDurationDefault = Duration(seconds: 4);`
     - `static const snackBarDurationLong = Duration(seconds: 6);`

All files use `package:flutter/material.dart` for Color. Use `const` constructors. Follow `snake_case.dart` file naming.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter analyze lib/core/theme/ lib/core/constants/ 2>&1 | tail -5 && grep "0xFF00897B" lib/core/theme/app_theme.dart && grep "0xFF1565C0" lib/core/theme/app_colors.dart && grep "ProtocolType" lib/core/constants/protocol_constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/theme/app_theme.dart contains `static const _seedColor = Color(0xFF00897B)`
    - lib/core/theme/app_theme.dart contains `static ThemeData light()` and `static ThemeData dark()`
    - lib/core/theme/app_theme.dart contains `useMaterial3: true` (twice, once per theme)
    - lib/core/theme/app_theme.dart contains `BorderRadius.all(Radius.circular(12))`
    - lib/core/theme/app_colors.dart contains `static const vless = Color(0xFF00897B)`
    - lib/core/theme/app_colors.dart contains all 5 protocol colors (0xFF00897B, 0xFF1565C0, 0xFFE65100, 0xFF6A1B9A, 0xFF2E7D32)
    - lib/core/constants/protocol_constants.dart contains `enum ProtocolType`
    - lib/core/constants/protocol_constants.dart contains `vless, vmess, trojan, shadowsocks, hysteria2`
    - lib/core/constants/protocol_constants.dart contains `fromScheme` method
    - lib/core/constants/app_constants.dart contains `static const appName = 'Arma VPN'`
    - `flutter analyze lib/core/theme/ lib/core/constants/` reports no errors
  </acceptance_criteria>
  <done>Theme system with teal seed color, light/dark ThemeData, protocol badge colors, ProtocolType enum, and app constants all created and passing analysis</done>
</task>

<task type="auto">
  <name>Task 3: Create router, navigation shell, and app entry points</name>
  <files>lib/core/router/app_router.dart, lib/shared/widgets/navigation_shell.dart, lib/main.dart, lib/app.dart</files>
  <read_first>lib/main.dart, lib/core/theme/app_theme.dart, lib/core/constants/app_constants.dart, .planning/phases/01-foundation-config-import/01-UI-SPEC.md, .planning/phases/01-foundation-config-import/01-RESEARCH.md</read_first>
  <action>
1. **Create `lib/core/router/app_router.dart`:**
   - Import `go_router` package.
   - Create a top-level `GoRouter goRouter` (or a Riverpod provider — prefer a simple top-level final for now since router doesn't depend on state).
   - Use `StatefulShellRoute.indexedStack` per D-06 to preserve tab state:
     ```dart
     final goRouter = GoRouter(
       initialLocation: '/dashboard',
       routes: [
         StatefulShellRoute.indexedStack(
           builder: (context, state, navigationShell) =>
             NavigationShell(navigationShell: navigationShell),
           branches: [
             StatefulShellBranch(routes: [
               GoRoute(path: '/dashboard', builder: (context, state) => const DashboardScreen()),
             ]),
             StatefulShellBranch(routes: [
               GoRoute(path: '/servers', builder: (context, state) => const ServerListScreen()),
             ]),
             StatefulShellBranch(routes: [
               GoRoute(path: '/routing', builder: (context, state) => const RoutingScreen()),
             ]),
             StatefulShellBranch(routes: [
               GoRoute(path: '/settings', builder: (context, state) => const SettingsScreen()),
             ]),
           ],
         ),
       ],
     );
     ```
   - For now, create minimal placeholder screen widgets at the bottom of this file or as simple `Scaffold(body: Center(child: Text('Screen Name')))` stubs. These will be replaced by real screens in Plans 04 and 05. Create them as:
     - `DashboardScreen` — `Scaffold(appBar: AppBar(title: Text('Arma VPN')), body: Center(child: Text('Dashboard')))`
     - `ServerListScreen` — `Scaffold(appBar: AppBar(title: Text('Servers')), body: Center(child: Text('Servers')))`
     - `RoutingScreen` — `Scaffold(appBar: AppBar(title: Text('Routing')), body: Center(child: Text('Routing')))`
     - `SettingsScreen` — `Scaffold(appBar: AppBar(title: Text('Settings')), body: Center(child: Text('Settings')))`
   - Put these stubs in separate files under their respective feature dirs (e.g., `lib/features/dashboard/presentation/screens/dashboard_screen.dart`) so later plans can replace them in-place.

2. **Create `lib/shared/widgets/navigation_shell.dart`:**
   - A `StatelessWidget` named `NavigationShell` that takes `StatefulNavigationShell navigationShell` as a required parameter.
   - Build a `Scaffold` with `body: navigationShell` and `bottomNavigationBar: NavigationBar(...)`.
   - `NavigationBar` config per UI-SPEC:
     - `selectedIndex: navigationShell.currentIndex`
     - `onDestinationSelected: (index) => navigationShell.goBranch(index, initialLocation: index == navigationShell.currentIndex)`
     - 4 `NavigationDestination` widgets:
       1. Icon: `Icons.dashboard_outlined` / selected: `Icons.dashboard`, Label: `'Dashboard'`
       2. Icon: `Icons.dns_outlined` / selected: `Icons.dns`, Label: `'Servers'`
       3. Icon: `Icons.alt_route_outlined` / selected: `Icons.alt_route`, Label: `'Routing'`
       4. Icon: `Icons.settings_outlined` / selected: `Icons.settings`, Label: `'Settings'`
     - Background: use theme's `surfaceContainerLow` via `Theme.of(context).colorScheme.surfaceContainerLow` if available, otherwise let M3 default handle it.

3. **Rewrite `lib/main.dart`** (completely replace the counter app):
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:arma_proxy_vpn_client/app.dart';

   void main() {
     WidgetsFlutterBinding.ensureInitialized();
     runApp(const ProviderScope(child: ArmaApp()));
   }
   ```
   Note: Hive initialization will be added in Plan 02.

4. **Create `lib/app.dart`:**
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:arma_proxy_vpn_client/core/theme/app_theme.dart';
   import 'package:arma_proxy_vpn_client/core/router/app_router.dart';

   class ArmaApp extends ConsumerWidget {
     const ArmaApp({super.key});

     @override
     Widget build(BuildContext context, WidgetRef ref) {
       return MaterialApp.router(
         title: 'Arma VPN',
         theme: AppTheme.light(),
         darkTheme: AppTheme.dark(),
         themeMode: ThemeMode.system,  // Will be replaced by provider in Plan 02
         routerConfig: goRouter,
         debugShowCheckedModeBanner: false,
       );
     }
   }
   ```
   Note: Localization delegates and locale will be added in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/artush/StudioProjects/arma_proxy_vpn_client && flutter analyze lib/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/main.dart contains `ProviderScope(child: ArmaApp())`
    - lib/main.dart contains `WidgetsFlutterBinding.ensureInitialized()`
    - lib/main.dart does NOT contain `MyApp` or `MyHomePage` or `_counter`
    - lib/app.dart contains `MaterialApp.router`
    - lib/app.dart contains `AppTheme.light()` and `AppTheme.dark()`
    - lib/app.dart contains `routerConfig: goRouter`
    - lib/core/router/app_router.dart contains `StatefulShellRoute.indexedStack`
    - lib/core/router/app_router.dart contains `/dashboard` and `/servers` and `/routing` and `/settings`
    - lib/shared/widgets/navigation_shell.dart contains `NavigationBar`
    - lib/shared/widgets/navigation_shell.dart contains `NavigationDestination` (4 destinations)
    - lib/shared/widgets/navigation_shell.dart contains `Icons.dashboard` and `Icons.dns` and `Icons.alt_route` and `Icons.settings`
    - Placeholder screens exist at lib/features/dashboard/presentation/screens/dashboard_screen.dart, lib/features/server/presentation/screens/server_list_screen.dart, lib/features/routing/presentation/screens/routing_screen.dart, lib/features/settings/presentation/screens/settings_screen.dart
    - `flutter analyze lib/` reports no errors (warnings acceptable)
  </acceptance_criteria>
  <done>App launches with ProviderScope, MaterialApp.router with teal theme, go_router with StatefulShellRoute.indexedStack providing 4-tab bottom navigation (Dashboard, Servers, Routing, Settings), each tab shows placeholder content and preserves state when switching</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in Plan 01 | This plan is pure infrastructure — no user input, no data storage, no external communication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | N/A | accept | Plan 01 handles no user input and no external data. No threats applicable to this infrastructure setup. |
</threat_model>

<verification>
1. `flutter pub get` succeeds without errors
2. `flutter analyze lib/` reports zero errors
3. App launches in debug mode (if emulator/device available) showing bottom nav with 4 tabs
4. Tapping each tab navigates to a different placeholder screen
5. Theme uses teal/cyan color scheme (not default purple)
</verification>

<success_criteria>
- All Phase 1 dependencies are installed and resolved
- Android project configured with com.arma.vpn applicationId and minSdk 24
- Material 3 theme with teal seed color (0xFF00897B) provides light and dark ThemeData
- GoRouter with StatefulShellRoute.indexedStack provides 4-tab navigation
- NavigationShell shows Material 3 NavigationBar with correct icons and labels
- main.dart uses ProviderScope and ArmaApp with MaterialApp.router
- All code passes `flutter analyze` with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-config-import/01-01-SUMMARY.md`
</output>
