---
phase: 08-api-client-device-auth
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - lib/app.dart
  - lib/features/api/presentation/providers/auth_bootstrap_provider.dart
  - lib/features/api/presentation/providers/auth_bootstrap_provider.g.dart
  - test/features/api/presentation/providers/auth_bootstrap_provider_test.dart
autonomous: true
gap_closure: true
requirements:
  - API-01
must_haves:
  truths:
    - "Device authentication is triggered automatically from app startup path."
    - "Startup bootstrap uses Riverpod providers and is idempotent per app process."
    - "App UI render is not blocked by startup auth bootstrap."
  artifacts:
    - path: "lib/features/api/presentation/providers/auth_bootstrap_provider.dart"
      provides: "Single startup bootstrap entrypoint for auth + initial key prefetch"
    - path: "lib/app.dart"
      provides: "Lifecycle wiring that triggers bootstrap once"
  key_links:
    - from: "lib/app.dart"
      to: "authBootstrapProvider"
      via: "post-frame provider read"
      pattern: "authBootstrapProvider\\.future"
---

<objective>
Close verification gap #1 by wiring first-launch auth trigger into the real app lifecycle.
</objective>

<context>
@.planning/phases/08-api-client-device-auth/08-VERIFICATION.md
@lib/app.dart
@lib/features/api/presentation/providers/auth_provider.dart
@lib/features/api/presentation/providers/default_server_keys_provider.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add startup auth bootstrap provider</name>
  <read_first>
    - lib/features/api/presentation/providers/auth_provider.dart
    - lib/features/api/presentation/providers/default_server_keys_provider.dart
    - .planning/phases/08-api-client-device-auth/08-VERIFICATION.md
  </read_first>
  <files>
    - lib/features/api/presentation/providers/auth_bootstrap_provider.dart
    - test/features/api/presentation/providers/auth_bootstrap_provider_test.dart
  </files>
  <action>Create keep-alive `authBootstrapProvider` that resolves `authTokenProvider` and prewarms `defaultServerKeysProvider`, with an idempotency guard so repeated reads do not trigger repeated startup auth calls.</action>
  <acceptance_criteria>
    - Provider resolves auth token through existing provider graph (no direct API client construction).
    - Provider performs at most one startup auth sequence per ProviderContainer lifecycle.
    - Bootstrap errors are surfaced as typed provider errors without crashing startup.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/presentation/providers/auth_bootstrap_provider_test.dart -r compact</automated>
  </verify>
  <done>Bootstrap provider performs one startup auth sequence via provider graph and remains idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bootstrap trigger into app startup</name>
  <read_first>
    - lib/app.dart
    - lib/features/api/presentation/providers/auth_bootstrap_provider.dart
  </read_first>
  <files>
    - lib/app.dart
    - test/features/api/presentation/providers/auth_bootstrap_provider_test.dart
  </files>
  <action>In `ArmaApp.initState`, add a post-frame one-time trigger to read `authBootstrapProvider.future`. Keep `MaterialApp` rendering immediate and do not block startup on network completion.</action>
  <acceptance_criteria>
    - Startup trigger exists in app lifecycle (not screen-level UI callback).
    - Trigger runs once and remains compatible with existing subscription refresh behavior.
    - `authBootstrapProvider` can be manually re-run in tests via provider refresh.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/presentation/providers/auth_bootstrap_provider_test.dart -r compact && flutter analyze lib/app.dart lib/features/api/presentation/providers/auth_bootstrap_provider.dart</automated>
  </verify>
  <done>App startup path triggers auth bootstrap once without blocking initial UI rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app lifecycle -> auth providers | Startup trigger initiates auth flow automatically |
| provider orchestration -> remote API | Startup bootstrap reaches untrusted network data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-GAP-01 | D | startup bootstrap | mitigate | idempotency guard prevents request storms |
| T-08-GAP-02 | I | startup error/logging | mitigate | no plaintext token/HWID logging in bootstrap path |
</threat_model>

<output>
After completion, create `.planning/phases/08-api-client-device-auth/08-04-SUMMARY.md`
</output>
