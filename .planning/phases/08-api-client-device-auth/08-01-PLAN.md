---
phase: 08-api-client-device-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/config/app_config.dart
  - lib/features/api/domain/entities/auth_state.dart
  - lib/features/api/domain/entities/default_server_key.dart
  - lib/features/api/data/models/device_auth_response.dart
  - lib/features/api/data/models/default_server_key_model.dart
autonomous: true
requirements:
  - API-01
  - SEC-01
must_haves:
  truths:
    - "Device-auth and key API payloads decode into stable typed entities that downstream providers can consume without runtime shape errors."
    - "Phase 08 API calls read base URL, API-key header, and timeout values from one shared config source."
    - "Build dependencies include packages required for platform device ID lookup and UUID fallback generation."
  artifacts:
    - path: "lib/features/api/domain/entities/auth_state.dart"
      provides: "Canonical auth token/device state contract"
    - path: "lib/features/api/domain/entities/default_server_key.dart"
      provides: "Canonical VPN key contract for Phase 09 consumption"
    - path: "lib/config/app_config.dart"
      provides: "Single source of truth for API base URL, key header, and timeout values"
  key_links:
    - from: "lib/features/api/data/models/device_auth_response.dart"
      to: "lib/features/api/domain/entities/auth_state.dart"
      via: "fromJson/toDomain mapping"
      pattern: "toDomain\\("
    - from: "lib/features/api/data/models/default_server_key_model.dart"
      to: "lib/features/api/domain/entities/default_server_key.dart"
      via: "model mapper"
      pattern: "toDomain\\("
---

<objective>
Define the Phase 08 contract layer first so all later auth/API work is built against stable types and constants.

Purpose: prevent ad-hoc field naming and token handling drift across providers and data sources.
Output: auth/key entities, DTO mappings, and centralized API config constants.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-api-client-device-auth/08-CONTEXT.md
@.planning/phases/08-api-client-device-auth/08-RESEARCH.md
@docs/api_documentation.md
@lib/features/server/presentation/providers/subscription_provider.dart
@lib/core/constants/app_constants.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 08 auth and server-key contracts</name>
  <read_first>
    - lib/features/server/domain/entities/server_config.dart
    - .planning/phases/08-api-client-device-auth/08-CONTEXT.md
    - docs/api_documentation.md
  </read_first>
  <files>
    - lib/features/api/domain/entities/auth_state.dart
    - lib/features/api/domain/entities/default_server_key.dart
    - lib/features/api/data/models/device_auth_response.dart
    - lib/features/api/data/models/default_server_key_model.dart
  </files>
  <behavior>
    - Test model JSON decode for `/auth/device/` maps token/is_guest/user_id fields correctly.
    - Test key model JSON decode maps `key_body`, `expire_date`, `status`, `used_traffic`, `data_limit`.
  </behavior>
  <action>Create immutable contracts and DTO mappers for device auth and key payloads per D-01 and D-05, with explicit field names matching API docs (`device_id`, `os_type`, `app_version`, `Authorization: Token ...`). Do not parse token as JWT and do not include Phase 09 deferred fields (announcement display, guest UX).</action>
  <acceptance_criteria>
    - `AuthState` includes token, expiresAt, isAuthenticated, isGuest, userId, deviceId.
    - `DefaultServerKey` includes id, name, keyBody, subscriptionUrl, expireDate, isActive, status, usedTraffic, dataLimit.
    - DTO files expose deterministic `fromJson` and `toDomain` mapping methods.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/models -r compact</automated>
  </verify>
  <done>Contracts compile and decoded sample API payloads map into domain entities without `dynamic` casting in call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Add centralized API config and dependency declarations</name>
  <read_first>
    - pubspec.yaml
    - lib/core/constants/app_constants.dart
    - .planning/phases/08-api-client-device-auth/08-CONTEXT.md
  </read_first>
  <files>
    - pubspec.yaml
    - lib/config/app_config.dart
  </files>
  <action>Add `device_info_plus`, `flutter_secure_storage`, and `uuid` dependencies, then create `AppConfig` constants for API base URL placeholder, `X-API-Key` placeholder, connect timeout 5s, read timeout 10s, and transient retry delay 1s per D-04, D-06, and D-07. Keep placeholder values exactly in config (not hardcoded in providers/services).</action>
  <acceptance_criteria>
    - `pubspec.yaml` includes `device_info_plus`, `flutter_secure_storage`, and `uuid` (per D-07 fallback requirement).
    - `lib/config/app_config.dart` defines API URL, API key header name/value placeholders, and timeout constants.
    - No other file defines duplicate API base URL or API key constants.
  </acceptance_criteria>
  <verify>
    <automated>flutter pub get && flutter analyze lib/config/app_config.dart lib/features/api/domain/entities lib/features/api/data/models</automated>
  </verify>
  <done>Phase 08 code can import one config source and one auth contract set without duplicated literals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device local storage -> app runtime | Encrypted auth/token material crosses persistent boundary |
| app -> VPN API | Untrusted remote payload enters typed model layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | API DTO mapping | mitigate | Strict typed DTOs and explicit key mapping methods; reject malformed JSON shape at decode boundary |
| T-08-02 | I | App config/token contract | mitigate | Centralize token/API constants in `AppConfig`; prohibit plaintext token parsing/logging paths |
</threat_model>

<verification>
- `flutter pub get`
- `flutter analyze lib/config/app_config.dart lib/features/api`
</verification>

<success_criteria>
- Contract files exist and compile.
- API request/response field names are fixed and reusable.
- Dependencies required by locked decisions are declared.
</success_criteria>

<output>
After completion, create `.planning/phases/08-api-client-device-auth/08-api-client-device-auth-01-SUMMARY.md`
</output>
