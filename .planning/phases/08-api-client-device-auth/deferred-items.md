# Deferred Items

- Pre-existing unstaged changes were present outside 08-03 scope and were not modified:
  - `.gitignore`
  - `lib/features/server/presentation/providers/subscription_provider.dart`
  - multiple non-API generated provider files and `.planning/debug/` artifacts
