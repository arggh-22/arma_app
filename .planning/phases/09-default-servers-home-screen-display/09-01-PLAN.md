---
phase: 09-default-servers-home-screen-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/api/data/models/default_server_cache_model.dart
  - lib/features/api/data/datasources/default_server_cache_datasource.dart
  - lib/features/api/presentation/providers/default_server_cache_provider.dart
  - lib/features/dashboard/domain/entities/default_server_item.dart
  - lib/features/dashboard/data/mappers/default_server_item_mapper.dart
  - test/features/api/data/models/default_server_cache_model_test.dart
  - test/features/api/data/datasources/default_server_cache_datasource_test.dart
  - test/features/dashboard/data/mappers/default_server_item_mapper_test.dart
autonomous: true
requirements:
  - API-02
  - REL-01
must_haves:
  truths:
    - "Default server payloads can be persisted and restored locally for offline rendering."
    - "Default server API payload fields (`id`, `name`, `keyBody`, `subscriptionUrl`, `expireDate`, `status`, `usedTraffic`, `dataLimit`, `isActive`) are preserved through cache and mapper contracts."
    - "Default server API payload is converted into a deterministic, connect-ready item contract."
  artifacts:
    - path: "lib/features/api/data/datasources/default_server_cache_datasource.dart"
      provides: "Hive-backed cache read/write contract for default servers"
    - path: "lib/features/dashboard/domain/entities/default_server_item.dart"
      provides: "Dashboard item contract carrying connect + metadata fields including `subscriptionUrl` and `expireDate`"
    - path: "lib/features/dashboard/data/mappers/default_server_item_mapper.dart"
      provides: "Mapper from DefaultServerKey to UI/connection-ready item"
  key_links:
    - from: "default_server_item_mapper.dart"
      to: "share_link_parser.dart"
      via: "key_body -> ServerConfig parse"
      pattern: "ShareLinkParser\\.parse"
    - from: "default_server_cache_datasource.dart"
      to: "default_server_cache_model.dart"
      via: "serialized cache envelope"
      pattern: "DefaultServerCacheModel"
---

<objective>
Define the Phase 09 data foundation: persistent default-server cache plus deterministic key-to-item mapping used by dashboard UI and connection flows.

Purpose: satisfy API-02 parsing stability and REL-01 offline readiness before UI wiring.
Output: cache datasource/provider and mapping contracts with automated tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
@.planning/phases/09-default-servers-home-screen-display/09-RESEARCH.md
@lib/features/api/domain/entities/default_server_key.dart
@lib/features/server/data/parsers/share_link_parser.dart
@lib/features/server/domain/entities/server_config.dart

<interfaces>
From `lib/features/api/domain/entities/default_server_key.dart`:
```dart
const factory DefaultServerKey({
  required int id,
  required String name,
  required String keyBody,
  required String subscriptionUrl,
  required DateTime expireDate,
  required String status,
  required int usedTraffic,
  required int dataLimit,
  required bool isActive,
}) = _DefaultServerKey;
```

From `lib/features/server/data/parsers/share_link_parser.dart`:
```dart
static ServerConfig? parse(String input);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistent default-server cache contracts (D-16, D-17)</name>
  <read_first>
    - .planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
    - lib/features/api/domain/entities/default_server_key.dart
    - lib/features/api/presentation/providers/default_server_keys_provider.dart
  </read_first>
  <files>
    - lib/features/api/data/models/default_server_cache_model.dart
    - lib/features/api/data/datasources/default_server_cache_datasource.dart
    - lib/features/api/presentation/providers/default_server_cache_provider.dart
    - test/features/api/data/models/default_server_cache_model_test.dart
    - test/features/api/data/datasources/default_server_cache_datasource_test.dart
  </files>
  <behavior>
    - Cache stores key list plus fetch metadata needed to label offline data.
    - Cache serialization/deserialization preserves every API-02 key field, including `subscriptionUrl` and `expireDate`.
    - Cache read returns empty-safe result when box is missing/corrupted.
    - Cache write replaces previous snapshot atomically.
  </behavior>
  <action>Create a Hive-backed cache envelope for default servers with `fetchedAt` metadata and serialized `DefaultServerKey` list, explicitly retaining API-02 fields `id`, `name`, `keyBody`, `subscriptionUrl`, `expireDate`, `status`, `usedTraffic`, `dataLimit`, and `isActive`, then expose it through a Riverpod provider for dashboard consumers. Keep this cache scoped to default-server API data only (no auto-update scheduler or interval settings per deferred ideas). Implement per D-16/D-17 so offline/no-cache states can be distinguished later.</action>
  <acceptance_criteria>
    - Cache model includes timestamp and full key payload list with explicit `subscriptionUrl` and `expireDate` fields.
    - Datasource exposes read/write/clear APIs with deterministic null-safe behavior.
    - Tests cover first-run empty cache, persisted restore path, and round-trip preservation of `subscriptionUrl` + `expireDate`.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/api/data/models/default_server_cache_model_test.dart test/features/api/data/datasources/default_server_cache_datasource_test.dart -r compact</automated>
  </verify>
  <done>Default-server cache contracts exist, are wired through provider, and pass automated persistence tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create deterministic default-server item mapper (D-05, D-06, D-08, D-15)</name>
  <read_first>
    - lib/features/server/data/parsers/share_link_parser.dart
    - lib/features/server/domain/entities/server_config.dart
    - .planning/phases/09-default-servers-home-screen-display/09-CONTEXT.md
  </read_first>
  <files>
    - lib/features/dashboard/domain/entities/default_server_item.dart
    - lib/features/dashboard/data/mappers/default_server_item_mapper.dart
    - test/features/dashboard/data/mappers/default_server_item_mapper_test.dart
  </files>
  <behavior>
    - Mapper preserves API status and traffic fields for UI badge/progress rendering.
    - Mapper carries forward `subscriptionUrl` and `expireDate` for downstream provider/UI reliability logic.
    - Mapper converts `keyBody` into `ServerConfig` for existing connection pipeline use.
    - Mapper forces deterministic server id (`default-api-{key.id}`) to keep active-server path stable.
  </behavior>
  <action>Add a dashboard-facing `DefaultServerItem` contract and mapper that transforms `DefaultServerKey` into UI + connection-ready data, including parsed `ServerConfig` from `keyBody` while preserving `subscriptionUrl` and `expireDate` from API-02. Keep status and traffic values intact (D-05/D-06/D-08), and normalize parsed server IDs so selections can route through `activeServerProvider` without a parallel default-server state (D-15).</action>
  <acceptance_criteria>
    - Mapping preserves status text, traffic counters, `subscriptionUrl`, and `expireDate` exactly from API entity.
    - Invalid `keyBody` does not crash mapping and is represented as non-connectable item.
    - Deterministic id strategy is covered by tests.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/dashboard/data/mappers/default_server_item_mapper_test.dart -r compact</automated>
  </verify>
  <done>Dashboard layer has a tested, deterministic item contract for rendering and connection actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VPN API payload -> local cache | Untrusted remote data is persisted and later rendered offline |
| key_body string -> parser | Untrusted serialized server config is transformed into connectable runtime object |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | `default_server_cache_datasource.dart` | mitigate | Strict model serialization/deserialization with defensive empty/corrupt fallback behavior |
| T-09-02 | D | `default_server_item_mapper.dart` | mitigate | Reject/guard malformed `keyBody` parse results so bad payload cannot crash dashboard |
| T-09-03 | S | mapper id normalization | mitigate | Deterministic `default-api-{id}` identity avoids ambiguous selection targets in active-server flow |
</threat_model>

<verification>
- `flutter test test/features/api/data/models/default_server_cache_model_test.dart -r compact`
- `flutter test test/features/api/data/datasources/default_server_cache_datasource_test.dart -r compact`
- `flutter test test/features/dashboard/data/mappers/default_server_item_mapper_test.dart -r compact`
</verification>

<success_criteria>
- Default server snapshots can be persisted/reloaded with fetch metadata.
- Key payloads convert into deterministic, connection-ready dashboard items.
- Data-layer contracts are test-covered and safe against malformed input.
</success_criteria>

<output>
After completion, create `.planning/phases/09-default-servers-home-screen-display/09-default-servers-home-screen-display-01-SUMMARY.md`
</output>
