---
phase: 15.1-fix-tgapi-01-bearer-authorization-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/api/data/datasources/api_client.dart
  - test/features/api/data/datasources/api_client_test.dart
  - .planning/v1.3-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - TGAPI-01
must_haves:
  truths:
    - "Telegram link requests send `Authorization: Bearer <token>` to `/auth/telegram/link/`."
    - "Existing `/keys/` contract remains unchanged (`Authorization: Token <token>`)."
    - "Regression evidence is replayable from focused automated tests and reflected in milestone audit status."
  artifacts:
    - path: "lib/features/api/data/datasources/api_client.dart"
      provides: "Correct auth-header contract for `linkTelegram`"
      contains: "Authorization: Bearer"
    - path: "test/features/api/data/datasources/api_client_test.dart"
      provides: "Regression-proof API client contract tests"
      contains: "linkTelegram sends bearer auth"
    - path: ".planning/v1.3-MILESTONE-AUDIT.md"
      provides: "Milestone blocker status after TGAPI-01 fix"
      contains: "TGAPI-01"
  key_links:
    - from: "lib/features/api/data/datasources/api_client.dart"
      to: "/auth/telegram/link/"
      via: "linkTelegram request header mapping"
      pattern: "Authorization.*Bearer"
    - from: "test/features/api/data/datasources/api_client_test.dart"
      to: "ApiClient.linkTelegram"
      via: "request-contract assertion"
      pattern: "Bearer bearer-token-123"
    - from: ".planning/v1.3-MILESTONE-AUDIT.md"
      to: "targeted TGAPI regression test run"
      via: "integration/flow evidence references"
      pattern: "api_client_test.dart|TGAPI-01"
---

<objective>
Eliminate the TGAPI-01 authorization-header regression in Telegram linking and lock it with focused automated coverage.

Purpose: Restore contract compliance (`Bearer` for Telegram link) without changing other API auth behavior.
Output: Fixed `ApiClient.linkTelegram`, strengthened regression checks, and synced milestone audit evidence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@lib/features/api/data/datasources/api_client.dart
@test/features/api/data/datasources/api_client_test.dart
@test/features/api/data/repositories/telegram_link_repository_impl_test.dart
@test/features/api/presentation/providers/telegram_link_provider_test.dart

<interfaces>
From `lib/features/api/data/datasources/api_client.dart`:
```dart
Future<TelegramLinkResponse> linkTelegram({
  required String token,
  required String telegramId,
})
```

From `lib/features/api/data/models/device_auth_response.dart`:
```dart
class DeviceAuthApiFields {
  static const authorization = 'Authorization';
  static const authorizationTokenPrefix = 'Token';
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix `linkTelegram` header prefix to satisfy TGAPI-01 contract</name>
  <files>lib/features/api/data/datasources/api_client.dart</files>
  <behavior>
    - Test 1: Telegram link request uses `Authorization: Bearer &lt;token&gt;`.
    - Test 2: Existing key-fetch auth remains `Authorization: Token &lt;token&gt;`.
  </behavior>
  <action>Update `ApiClient.linkTelegram` to send `Bearer` auth header (per TGAPI-01) while leaving `getKeys` token behavior unchanged. Do not alter retry/error/redaction behavior or endpoint/path/body format.</action>
  <verify>
    <automated>flutter test test/features/api/data/datasources/api_client_test.dart -r compact</automated>
  </verify>
  <done>`linkTelegram` request contract matches TGAPI-01 exactly and all ApiClient datasource tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden regression coverage around Telegram link auth contract</name>
  <files>test/features/api/data/datasources/api_client_test.dart</files>
  <behavior>
    - Test 1: `linkTelegram` test fails if header reverts to `Token`.
    - Test 2: `getKeys` test still enforces `Token` prefix.
  </behavior>
  <action>Keep/add explicit contract assertions so test failures pinpoint header-prefix drift (`Bearer` for linkTelegram, `Token` for getKeys). Ensure coverage remains focused on API contract behavior and does not duplicate repository/provider logic.</action>
  <verify>
    <automated>flutter test test/features/api/data/datasources/api_client_test.dart -r compact</automated>
  </verify>
  <done>Datasource tests provide clear regression protection for divergent auth-header contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run targeted Telegram-link suites and sync milestone audit status</name>
  <files>.planning/v1.3-MILESTONE-AUDIT.md</files>
  <action>Run focused automated suites (`api_client_test.dart`, `telegram_link_repository_impl_test.dart`, `telegram_link_provider_test.dart`) and update `v1.3-MILESTONE-AUDIT.md` so TGAPI-01 no longer reports the `Token` vs `Bearer` integration gap once tests pass.</action>
  <verify>
    <automated>flutter test test/features/api/data/datasources/api_client_test.dart test/features/api/data/repositories/telegram_link_repository_impl_test.dart test/features/api/presentation/providers/telegram_link_provider_test.dart -r compact</automated>
  </verify>
  <done>Milestone audit reflects TGAPI-01 regression closure with replayable test evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app API client → Telegram link endpoint | Auth token leaves trusted app memory and crosses network boundary via Authorization header. |
| automated test evidence → milestone audit status | Test outcomes are promoted into milestone-level closure claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-01 | S | `ApiClient.linkTelegram` auth header | mitigate | Enforce explicit `Bearer` prefix in implementation and datasource contract tests. |
| T-15.1-02 | T | `.planning/v1.3-MILESTONE-AUDIT.md` gap status | mitigate | Only mark TGAPI-01 closed after targeted automated suites pass in the same run. |
| T-15.1-03 | R | regression-closure claim | mitigate | Keep replayable command output references in audit evidence to support re-verification. |
</threat_model>

<verification>
- flutter test test/features/api/data/datasources/api_client_test.dart -r compact
- flutter test test/features/api/data/datasources/api_client_test.dart test/features/api/data/repositories/telegram_link_repository_impl_test.dart test/features/api/presentation/providers/telegram_link_provider_test.dart -r compact
</verification>

<success_criteria>
- `ApiClient.linkTelegram` sends `Authorization: Bearer <token>` and contract test passes.
- `getKeys` continues using `Authorization: Token <token>` with passing test coverage.
- `v1.3-MILESTONE-AUDIT.md` no longer reports TGAPI-01 header mismatch as an open blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-fix-tgapi-01-bearer-authorization-regression/15.1-01-SUMMARY.md`
</output>
